# 任务书 · markets 国际版 K 线图展示优化（2026-08-16 10:52 雷总指令）

## 背景
雷总反馈：**markets AI 行情官国际版 K 线图展示的点数太多，手机版不清晰**。要求参考 AI 行情官国内版（p\a1），把 K 线、量能图调整到看得清晰的程度，更新后由副脑04 部署。

## 主脑已定位（供参考，快速入手）
- **后端 K 线接口**：`p\markets\api\server\app\main.py` L293 `api_kline`：`count: int = Query(500, ge=10, le=1500)` —— **默认 500 根**，上限 1500
- **数据源**：`p\markets\api\server\app\providers_us.py`：fetch_tencent_kline / fetch_em_kline / fetch_sina_kline 均 `count: int = 500`
- **前端图表**：`p\markets\web\app.html`（LightweightCharts 4.2.0，web 目录）
- **国内版参考**：`p\a1`（K 线/量能图展示方式，请行情官对比其默认天数/显示数量）

## 请行情官执行
1. **对比确认**：markets 国际版 vs 国内版（p\a1）的 K 线默认展示天数/根数差异
2. **调整方案**：把默认 count 降到合适值（参考国内版，如 120~250 根左右，让手机版清晰可读），并同步调整前端图表显示（可见区间/缩放默认值），量能图同样处理
3. **手机版适配**：确认窄屏下 K 线密度、量能图高度/间距清晰
4. **验证**：本地/04 测试接口返回根数 + 页面效果（截图确认）

## 完成后
1. 回执 ✅/⚠️（改了什么参数、效果对比）
2. **通知主脑 → 副脑04 部署**（04 负责 markets 生产 18012 部署）
