# 副脑03 更新指令 · 复盘/手机版同步 + 今日大盘紧凑化

> 发令：2026-08-28  
> 目标提交：`3acc36e`（信息含「sync desktop+mobile」或「复盘」）  
> 进程：NSSM / `AI24X-a1-api`（禁止 pm2）

## 一、本包内容

| 模块 | 内容 |
|------|------|
| 桌面 `web/gd.html` + `bjscreener.js/css` | 今日大盘紧凑行；指数序上证→深证→创业板→北证50；主攻/观察/回避图标；去掉冗余说明句 |
| 手机 `web/m/gd.html` | **已与桌面口径同步**（indexes、行布局、图标、主线） |
| API `daily_report.py` / `main.py` / `bj_screener.py` | brief 主线跟复盘 `mainlines.json`；`today_missing` 当日归档不误判 |

## 二、主脑侧（本机已 commit，待 push）

```powershell
cd E:\AI24X\ai24x-website\ai24x01
git log -1 --oneline   # 应见 3acc36e
git push origin master
# 归档定型数据（复盘/掘金）→ 03：
powershell -ExecutionPolicy Bypass -File p\a1\scripts\sync_final_to_03.ps1
```

若不能 push master，可改 scp 七文件（见第三节备选）。

## 三、副脑03 执行（PowerShell · 整段）

```powershell
$ErrorActionPreference = 'Stop'
$PSNativeCommandUseErrorActionPreference = $false
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$repo = 'C:\ai24x01'

Write-Host '=== 1/4 拉码 ===' -ForegroundColor Cyan
Set-Location $repo
# 若工作区脏：先备份再丢弃本包相关文件本地改动
git checkout -- p/a1/api/server/app/bj_screener.py p/a1/api/server/app/daily_report.py p/a1/api/server/app/main.py p/a1/web/css/bjscreener.css p/a1/web/gd.html p/a1/web/js/bjscreener.js p/a1/web/m/gd.html 2>$null
git pull origin master
if (-not $?) { Write-Host '!! 拉码失败'; -ForegroundColor Red; exit 1 }
git log -1 --oneline
if (-not (git log -1 --oneline | Select-String '复盘|desktop\+mobile|3acc36e')) {
  Write-Host '!! 未拉到目标提交，先别继续' -ForegroundColor Red; exit 1
}

Write-Host '=== 2/4 重启 API ===' -ForegroundColor Cyan
Restart-Service AI24X-a1-api
Start-Sleep -Seconds 4

Write-Host '=== 3/4 health ===' -ForegroundColor Cyan
try {
  $r = Invoke-WebRequest 'http://127.0.0.1:8001/health' -UseBasicParsing -TimeoutSec 8
  Write-Host "health=$($r.StatusCode) $($r.Content)"
} catch { Write-Host "health FAIL $($_.Exception.Message)" -ForegroundColor Red }

Write-Host '=== 4/4 静态页指纹 ===' -ForegroundColor Cyan
$m = Select-String -Path 'C:\ai24x01\p\a1\web\m\gd.html' -Pattern 'ic-dot gold|PRIMARY|mkt-row' -SimpleMatch:$false
Write-Host "m/gd 命中条数=$($m.Count)（应>0）"
$j = Select-String -Path 'C:\ai24x01\p\a1\web\js\bjscreener.js' -Pattern '上证指数.*深证成指|PRIMARY'
Write-Host "bjscreener.js PRIMARY 命中=$($j.Count)"
Write-Host '=== 完成。公网 Ctrl+F5：https://a.ai24x.com/gd.html 与 /m/gd.html ===' -ForegroundColor Green
```

### 备选：无 git push 时 scp（主脑执行）

```powershell
$H='Administrator@123.207.199.238'
$L='E:\AI24X\ai24x-website\ai24x01\p\a1'
$R='C:/ai24x01/p/a1'
@(
  'api/server/app/bj_screener.py','api/server/app/daily_report.py','api/server/app/main.py',
  'web/css/bjscreener.css','web/gd.html','web/js/bjscreener.js','web/m/gd.html'
) | ForEach-Object {
  scp -o ConnectTimeout=20 "$L\$_".Replace('/','\') "${H}:$R/$_"
}
ssh $H "powershell -NoProfile -Command Restart-Service AI24X-a1-api"
```

## 四、验收

- 手机 `/m/gd.html`：今日大盘四指数紧凑行 + 主攻金点 / 观察👀 / 回避玫点  
- 桌面 `/gd.html`：同上；北证50 涨跌非 0（当日有行情时）  
- brief=`1` 主线与复盘 `mainlines.json` 一致  

## 五、回滚

```powershell
cd C:\ai24x01
git log -5 --oneline
git checkout <上一提交> -- p/a1/web/gd.html p/a1/web/m/gd.html p/a1/web/js/bjscreener.js p/a1/web/css/bjscreener.css p/a1/api/server/app/daily_report.py p/a1/api/server/app/main.py p/a1/api/server/app/bj_screener.py
Restart-Service AI24X-a1-api
```
