# AI24X 子项目 · AI行情官 a1 · 项目记忆

> **2026-07-16 晚**：公网 BK 信号已修复并验证。完整记忆见 `memory/projects/a1.md`、`memory/daily/2026-07-16.md`。HEAD **`cf70831`**。

## 一句话目标
可运营的 A股行情标注工具：登录 → 搜索 → K线/信号；次数/VIP/邀请后端化。叙事：数据可视化。

## 启动（本地）
- 前端：`http://127.0.0.1:18001/`（勿改成生产 8001）
- 后端：`http://127.0.0.1:18011/`；生产 PM2 `a-api-8001`→8001

## 关键坑（2026-07-16 实战）
1. Windows 信号缓存 key 不能含 `:`（`ths:xxxx`→NTFS ADS）
2. BK 公网：图有信号无 → chart→`/api/signals/compute`；time 必须 `YYYY-MM-DD`
3. 搜索默认 BK，同花顺选项排后并标注；BK≠THS
4. 拼音搜索依赖 `pypinyin`

## 推送
须用户确认后双推 gitee + origin；输出核对码。

## 2026-08-05 备份与安全合规优化（1.02 版基线）
- 备份（代码+PG+env）：E:\AI24X\bak\AI行情官1.02版-20260805_074312（可回滚，见其中 ROLLBACK.md；脚本 scripts/backup/backup_a1_102.ps1）
- 安全：登录/注册/重置/发码 增加 IP+手机号限流；OTP 连续错误5次作废（sms_local/admin_otp）；密码最短 8 位
- 密钥：JWT/ADMIN_KEY 已轮换为强随机；旧 JWT 保留在 AI24X_JWT_SECRET_PREV 过渡；AI24X_ENV=prod
- 脱钩：identity_api_base 已清空（.env + admin_config），不再依赖主站；SMS 本地腾讯云直发
- 合规：伙伴计划默认"权益回馈"模式（partner_payout_mode，不可提现；后台可切回 cash 试点）；前端隐藏提现入口
- 合规：新增 web/terms.html 用户协议 + web/privacy.html 隐私政策，页脚已接入
- 修复：admin_config 定价误为联调测试价（月1元等），已改回正式价（体验9.9/月99/年999/成长299/专业999）
- 待办：15 笔按测试价实付的历史订单需人工对账处理；JWT_SECRET_PREV 在所有用户重新登录后清除；Redis 限流迁移；付费数据源接入

## 2026-08-05 追加：管理登录改为「密钥+短信」双因素
- 按需求：AI24X_ADMIN_KEY 改回 iamlei（不再用长随机串）；管理浏览器登录开启短信 OTP（admin_browser_otp_enabled=1）
- 管理 OTP 发送已接入本站本地短信通道（tencent），不再依赖主站；新增 sms_local.send_local_sms_code
- 登录流程：输入密钥 iamlei + 手机号(白名单 18958992226) + 短信验证码；会话 8 小时
- 安全提示：密钥为弱口令，靠 OTP 二次验证兜底；X-Admin-Key 直调 API 不受 OTP 保护，勿外泄；后续建议改回强密钥+OTP

## 2026-08-06 追加：DeepSeek涨价事件调研结论（投资研究，非产品开发）
- 事件：DeepSeek 官方预告近期整体上调 API 定价、涨幅较大（峰谷定价高峰2倍、V4-Flash已调价、V4 Pro临近）；智谱Q1提价83%用量仍+400%。
- 8/6 盘面：算力租赁领涨为中嘉博创(直线涨停)、利通电子(+8.76%)、优刻得、中贝通信；浪潮信息-0.53%、并行科技-3.03%未获资金认可。
- 最终锁定 2 只：**利通电子(603629)**（业绩+题材龙头，H1净利+1173%~1368%，4天3板后8/6放量92.7亿，收122.52）+ **优刻得(688158)**（正宗云厂商，市值166亿20cm弹性，突破后缩量回踩，收35.24）。宏景科技(301396)因透支/机构目标价已破/套牢盘重列为备选。
- 报告已归档至调研报告库：`p/a1/调研报告/01-事件驱动研究/2026-08-06-DeepSeek涨价受益标的/`（报告md + K线图 + 数据/ + 工具/）。
- 调研报告库：`p/a1/调研报告/`（分类：01事件驱动/02行业/03个股/04技术面/05复盘/99模板，命名规范见其README.md）。新调研报告一律按规范归档。
- 2026-08-06 晚：分享卡全套已完成——token（user3/sohodiy，邀请码BWPX3Z8B）已存 `工具/token.txt`（有效期约7天至8/13）；两张卡已带 信号+评分+邀请码 生成并嵌入分享版（HTML 2.44MB / PDF 8页）。注册数在 account.html 邀请记录可查；扫码次数无埋点，注册率需另会话加埋点。
- 邀请码调研（BWPX3Z8B 归属 user_id=3）：注册数可查（account.html 邀请记录 / admin agent/rank direct_invites）；但 `/i/CODE` 落地页无点击/扫码埋点，注册率=注册/扫码暂无法自动统计，需另会话加埋点（勿擅改产品代码）。匿名查个股每天每 IP 仅 3 次（_anon_daily_can_consume），出卡务必用登录 token。
- 分享卡生成管线：`工具/make_share_card.js`（Playwright+Edge 自动化 demo.html 信号分享卡，含K线信号+评分）；评分/信号需登录 token（localStorage `ai24x_a_token`，放 `工具/token.txt` 或环境变量 `AI24X_TOKEN`）；游客每日次数有限。分享版由 `make_share.py` 自动嵌入 `分享卡_*.png`。
- 技术坑已固化：PowerShell管道中文变?（写UTF-8文件再执行）；matplotlib用simhei.ttf；RapidOCR验证；view_image本环境不可用。
## 2026-08-06 追加：数据库独立后本地身份修复批次（已推 Gitee；副脑03 已部署验收通过）
- 背景：数据库/身份已独立（auth_local_enabled=1），管理端改密仍走主站 identity → 必 503。
- 修复：管理端改密、绑定手机/邮箱均加本地兜底（写本地 users.password_hash / phone / email）；密码规则统一至少 8 位。
- 后台：用户详情双列网格 + 窄屏适配 + 分组默认面板键修正。
- 自选评分列表：技术信号默认 5 条 + 「更多▾」展开全部；列宽代码 74 / 名称 120 / 风险 200，表格 fixed 布局防横向溢出。
- 数据提醒：18900000000 / 18057785991 / 18900000004 无 password_hash，需短信「忘记密码」补设。
- 副脑03 回执（2026-08-06）：拉码 613d072 ✅ / 重启 AI24X-a1-api ✅ / health(8001)=200 ✅ / 后台 user-detail=True ✅ / account 与评分榜 watchscore.css?v=4=True ✅；health(18011) 连接拒绝为正常（本地开发端口，生产走 8001 NSSM），无需处理。
- 部署脚本教训（三份「副脑03-更新指令」文档脚本已统一修正）：① 副脑03 只有 origin（gitee.com/ai24x/ai24x-website），无 gitee remote，`git pull gitee master` 必 fatal → 直接用 `git pull origin master`；② PS7.3+ 在 `$ErrorActionPreference='Stop'` 下 git 失败会直接中断脚本 → 先设 `$PSNativeCommandUseErrorActionPreference=$false` 再靠 `$?` 判断；③ git 中文输出 GBK/UTF-8 乱码导致 Select-String 匹配失败 → 开头先 `[Console]::OutputEncoding=[System.Text.Encoding]::UTF8`。
- 注：origin 现含更早一批 `adfa414`（TokenLab price_monitor 兜底，主脑监控侧，非行情官业务），下次 a1 拉码会自动带上，不影响 AI24X-a1-api。
## 2026-08-06 追加：修复用户中心「当前账号/套餐」不显示 + 首页登录注册卡片对齐 + 评分榜展开行对齐（已推 Gitee 889a9c3，待副脑03 部署）
- 根因：`account.html` 的 `accountLineText` 调用了 `maskWlPhone/maskWlEmail`，但两函数全项目未定义（613d072 引入），抛 ReferenceError 被 `catch(eU){}` 吞掉 → `setAccountIdentityVisible(true)` 未执行 → identity 恒隐藏，而配额照常填充（qPlan 有值但「当前账号/套餐」不显示）。
- 修复：account.html 补齐 `maskWlPhone`（手机 3+****+4 脱敏）与 `maskWlEmail`（soh***iy@qq.com）；`planTitle` 增加 `PLAN_TITLE_FALLBACK` 静态中文兜底（free→免费版 / vip_trial_99→VIP体验卡 / vip_month→VIP月卡 / vip_year_999→VIP年卡 / agent_growth/agent_pro→伙伴计划档）。
- 验证（本地 18001+18011，无头 Edge + CDP 注入 token）：VIP 账号显示「当前账号：手机 180****5991 ｜ 今日剩余 148 ｜ 本周剩余 498 ｜ 套餐 VIP年卡」；免费账号显示「…套餐 免费版」；无 JS 异常。
- 教训：try/catch 吞异常会掩盖「前置渲染函数未定义」类问题，成功回调里渲染前置步骤不应整段 try/catch。

- 首页登录/注册卡片：`.auth-card` 改 flex 一行居中；radio `input[type="radio"]` 完整重置（15×15、min/max-width、padding/border=0、box-sizing），规避 base.css/tool.css 全局 input 规则（padding 10px 12px / min-width:180px）把圆点撑变形导致与文字漂移。CDP 实测桌面1280/手机375/320 圆点与文字中心 yDiff=0。
- 评分榜「更多▾」展开行：`wl-detail-row` 内改 `wl-detail-grid`（grid-template-columns 34px 74px 120px 60px 1fr 200px），技术信号放第5列、风险提示放第6列，与上方缩略文字垂直对齐（实测对齐差 0px）。
- 副脑03 指令文档：`docs/联调/副脑03-更新指令-当前账号修复与登录注册卡片对齐与评分榜展开对齐-20260806.md`（纯前端，无需重启后端）。

