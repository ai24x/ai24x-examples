# AI24X 03 更新指令 · HEAD=993e297（复盘并入18011-掘金主线修正-历史归档每日一条双列左对齐-页面降噪折叠-同花顺fuyao通道）

## 背景
老板确认「03更新」。本批为 AI行情官 a1 项目：
1. 复盘模块并入产品后端 18011（/api/report/*），非 VIP 风控降级（仅大盘信号+市场宽度，无荐股导向）；报告资金面明细默认折叠；历史归档页宽度统一 1360px
2. 掘金：主线板块成分强制入池+主推主线优先；历史归档按扫描日去重（每日 1 条）；历史列表「沪深京|北证」双列左对齐；板块排行与市场情绪默认折叠降噪；扫描进度接口
3. 同花顺 fuyao(hithink-finance) 免费增强通道（默认关闭，可后台热开）

## 任务
1. 拉码：git pull origin master（origin=gitee.com/ai24x/ai24x-website）→ 目标 HEAD = 993e297；git log -1 确认一致
2. 备份（部署前必做）：pg_dump AI行情官库 ai24x_a_cn → D:\backup\ai24x_a\ 按时间戳命名
3. 重启后端：nssm restart AI24X-a1-api（仅 NSSM，未动 PM2）
4. 验收：
   a. https://a.ai24x.com/health → 200 {"ok":true}
   b. /docs 可开
   c. 登录正常
   d. demo.html 查询：上证指数 / 北证50 / 浪潮信息 000977
   e. 本批新功能（浏览器 Ctrl+F5）：
      - 掘金 gd.html：历史归档「沪深京|北证」双列、每日 1 条、行内日期与标的全左对齐；板块排行与市场情绪默认折叠
      - 复盘 /daily/：资金面明细默认折叠「点击展开明细」；往期报告列表无本地路径文本；历史归档 view.html 宽度与其它栏目一致
      - 免费账号看复盘：仅大盘信号+市场宽度，无板块资金流/个股/主线泄漏
   f. /api/bj/history?market=all 与 market=bj → 每天仅 1 条（去重生效）
5. 启用同花顺 fuyao 增强通道（admin_config 热改，无需重启；仅影响「同花顺情绪面/交叉验证」与复盘 2.6 市场情绪，Key 失效时自动显示未启用、不影响其它功能）：
   psql 对库 ai24x_a_cn 执行：
   INSERT INTO admin_config(key,value,updated_at) VALUES('AI24X_FUYAO_ENABLED','1',(EXTRACT(EPOCH FROM NOW())*1000)::bigint) ON CONFLICT(key) DO UPDATE SET value='1', updated_at=(EXTRACT(EPOCH FROM NOW())*1000)::bigint;
   INSERT INTO admin_config(key,value,updated_at) VALUES('AI24X_FUYAO_API_KEY','sk-fuyao-9WbC-PO3JNsva7YEONf_7BdVfz9dVf1d',(EXTRACT(EPOCH FROM NOW())*1000)::bigint) ON CONFLICT(key) DO UPDATE SET value='sk-fuyao-9WbC-PO3JNsva7YEONf_7BdVfz9dVf1d', updated_at=(EXTRACT(EPOCH FROM NOW())*1000)::bigint;
   验收：GET /api/ths/sentiment（带 VIP token）→ enabled=true、has_key=true
6. 飞书私信 + 群回执（✅ 完成页 / ⚠️ 问题页），附 NSSM 服务状态与 health

## 回滚预案
git revert 993e297 + nssm restart AI24X-a1-api；若已启用 fuyao，删除 admin_config 两条 key（AI24X_FUYAO_ENABLED / AI24X_FUYAO_API_KEY）即可恢复默认关闭。
