# AI24X 04 更新指令 · HEAD=2878e47（GSC 收录修复：canonical 归一 + noindex + sitemap 补全 + nginx 软 404）

## 背景
Google Search Console 报「Duplicate, Google chose different canonical than user」+「Alternate page with proper canonical tag」。
根因已定位：①首页 canonical 指向 /index.html 而谷歌用 /；②AI行情官.灯塔版V1.02.html canonical 跨域指向 a.ai24x.com 带参数；③partner.html 缺 canonical、404.html 可被收录；④nginx 软 404（不存在路径返回 200）。

## 任务
1. git pull → 目标 HEAD = 2878e47（远程 master 最新）
2. 本轮含 web 静态修复 + nginx 配置修复；core 按惯例重启（health 200）
3. web 层验收：
   a. /index.html → canonical 为 https://www.ai24x.com/（og:url 同步）
   b. /AI%E8%A1%8C%E6%83%85%E5%AE%98.%E7%81%AF%E5%A1%94%E7%89%88V1.02.html → 含 meta robots noindex,follow（跳转保留）
   c. /partner.html → 新增 canonical https://www.ai24x.com/partner.html
   d. /404.html → 新增 meta robots noindex
   e. /sitemap.xml → 首页 loc 为 https://www.ai24x.com/；新增 guides/codex.html 与 partner.html；XML 解析合法（34 条）
4. nginx 软 404 修复（关键）：
   a. 先备份当前 nginx 站点配置（如 C:\nginx\conf\... 或 /etc/nginx/... 按实际路径，备份为 .bak-20260809）
   b. 将 404 处理改为真实 404 状态码：error_page 404 /404.html; → error_page 404 =404 /404.html;（或等价写法，确认当前配置再改）
   c. nginx -t 通过后 reload
   d. 验收：curl -s -o /dev/null -w "%{http_code}" https://www.ai24x.com/this-page-does-not-exist-xyz123 → 期望 404（修复前为 200）；https://www.ai24x.com/ 与 /index.html 仍 200
5. 飞书私信 + 群回执（✅ 完成项 / ⚠️ 问题项）

- 回滚预案：git revert 2878e47 + 重启 core；nginx 还原 .bak 配置 + reload
