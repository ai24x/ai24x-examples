# 任务书：AI24X-core 假死根因排查（2026-09-02 20:42 主脑派发）

## 背景（一句话）
20:36 api.ai24x.com 用户中心报 "Cannot reach the API"，主脑实测 health 超时；SSH 重启 AI24X-core（端口 8002，nssm 服务 AI24X-core）后 20:39:32 恢复，公网 200 ✅。但日志显示故障前存在 142s/29s/22s 等超长耗时请求，疑似慢请求堆积拖死服务，需排查根因防复发。

## 任务清单（04 执行）
1. **翻日志定位假死前兆**：ai24x-core.err.log / out.log 故障前（约 20:00-20:39）的异常——超长耗时请求集中在哪些端点（/v1/responses? /v1/chat/completions?）、对应哪些上游模型（vip-gpt56-luna 等 142s 请求的上游是谁）、uvicorn worker 数/线程池配置
2. **查上游超时配置**：api 代码里 httpx/上游请求 timeout 设置（超时值多大？是否无超时导致挂起）；OpenRouter/上游慢响应时是否重试堆积
3. **查并发配置**：uvicorn 启动参数（nssm AppParameters 只有 --host 127.0.0.1 --port 8002，无 --workers → 单 worker？）、asyncio 线程池大小、是否有请求队列上限
4. **给出加固方案**（供司令/平台官改代码）：
   - 上游请求加合理超时（如 connect 10s / read 60s 或按模型区分）
   - 慢请求降级/快速失败，不无限挂起
   - 必要时 uvicorn 加 workers 或限流
   - 健康检查加强（假死可被监控发现并自动重启：如心跳脚本检测 /health 超时 → 自动重启服务）
5. **回执**：✅ 根因结论 + 加固方案 / ⚠️ 卡点

## 验收标准
- 根因明确（慢请求来源 + 为什么拖死服务）
- 加固方案可执行（本地改 → 测试 → 部署，正规流程）
- 回执落盘 04 回复目录

## 截止
明日 12:00 前回执（不紧急，服务已恢复）
