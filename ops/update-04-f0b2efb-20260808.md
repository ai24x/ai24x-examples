# AI24X 04 更新指令 · HEAD=f0b2efb（guides 接入页补充推理模型 max_tokens 提示 + 超值包白名单说明）

## 任务
1. git pull → 目标 HEAD = f0b2efb（远程 master 最新，与本地一致）
2. 本轮为纯静态页面 + 语言包变更，无 Python 运行时改动：可不重启 core（如按惯例重启也无害，health 200 即可）
3. 验收清单：
   a. GET /guides/index.html → 200；页面「VIP named models」卡片下方新增 callout 提示区（data-i18n-html="page.guides.tips"）：中文含「推理型模型…max_tokens…超值包…白名单 13 款…全量 VIP 会员…24 款」；英文含 Two things to know before calling named models
   b. GET /guides/codex.html → 200；第 7 节踩坑列表新增 tip7（推理型模型 vip-gpt5 需 max_tokens ≥ 400）与 tip8（超值包 ≠ 全量 VIP）两条
   c. GET /guides/openai-sdk.html → 200；VIP 点名示例下方新增 vipNote 提示（推理型模型 max_tokens ≥ 400）
   d. 版本：以上 3 页 locales.js 引用为 ?v=20260808d、head i18n-boot 注释为 v20260808d；其余页面维持原版本不受影响
   e. 中英文切换显示正常（?lang=en / ?lang=zh 分别验证 callout 与两条新 tip 文案）；ja/ko/de/fr/es 回落英文属既有策略，非缺译
4. 飞书私信 + 群回执（✅ 完成项 / ⚠️ 问题项）

- 回滚预案：git revert f0b2efb + 重启 core（纯静态改动，回滚后刷新页面即恢复）
