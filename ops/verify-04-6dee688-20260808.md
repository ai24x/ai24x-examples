# AI24X 04 复核指令 · 确认当前部署状态（不部署新代码）

背景：最新 web 提交 6dee688（i18n 防闪烁内联）此前已部署并验收。老板要求复核确认当前线上状态。

## 任务（只读，不改配置、不重启）
1. git -C C:\ai24x01 log --oneline -1 → 确认 HEAD = 6dee688
2. 公网 GET /health 与 GET /index.html → 200；index.html 应含 "i18n-boot inline v20260808c"（head 内联脚本）
3. core 服务 health 200 commit=6dee688（如 /health 返回 commit 字段）
4. 飞书私信+群简短回执：HEAD / health / 页面 inline 确认 ✅ 或 ⚠️
