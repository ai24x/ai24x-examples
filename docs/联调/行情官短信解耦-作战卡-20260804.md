# 行情官短信解耦 · 作战卡（权威口径）

> 主脑定稿 · 2026-08-04 · 03/04 配合执行  
> **状态：P0 已收口（2026-08-04 21:53）**  
> 原则：**通道独立、账号暂不拆；不靠重开 04 手机短信救行情官**

---

## 〇、P0 收口结论

| 项 | 结果 |
|---|---|
| 根因 | 04 关 `sms_106_enabled` → A1 `identity_proxy` 发码/注册挂 |
| 解法 | A1 本地 106 直发（`sms_active_provider=local`）+ 04 register 信任跳过；www 短信总开关仍关 |
| Gitee | `48e096f`（主功能）+ `10038f0`（手机号规范化） |
| 真人验收 | 137****8297：106 发码收到 → 注册 → 登录 → `/api/me` |
| id 对齐 | 主站 `auth_users.id=43` = 本库 `ai24x_a_cn.users.id=43` ✅ |
| www | `SMS_106_ENABLED` 仍 false，手机注册仍关 ✅ |

**P0 全绿，可收口。** P1（身份全本地）另开卡，今日不做。

### 收口后待办（非阻塞）

- [ ] `SMS_INTERNAL_KEY` len=9 偏短 → P1 两边同步轮换 32+（安全通道，勿贴群）
- [ ] 协作回执曾暴露 106 密码明文 → 建议改密并只行级写入 admin_config
- [ ] 04 确认测试号 `13900007777` / id=42 已删（若未删补删）
- [ ] 03 确认生产 HEAD 含 `10038f0`（手机号热修）

---

## 一、现状结论（一句话）

今日挂掉根因：04 Token 关了 `sms_106_enabled` → 行情官代理发码/手机注册被连带打死。  
**已解**：03 本地 106 直发 + 04 信任跳过；**注册仍写主站 `auth_users`**；www 手机注册保持关闭。

### 通道真相（主脑确认 · 以业务为准）

| 通道 | 状态 | 行情官本地 `sms_active_provider` |
|---|---|---|
| **106** | **正式主通道（已验收）** | `local` |
| **腾讯云** | 电信签名已过；**移动签名未过** | `tencent`（仅临时） |
| **爱聚合(juhe)** | **备案中** | 暂不用 |

参考资料：
- 03 早期主站备份文档 / 本机 `C:\env-pack\api.env`：键位备查，勿整文件覆盖生产
- 复制下发：`docs/联调/副脑03-04-复制下发-短信解耦-20260804.md`

---

## 二、目标分层

| 阶段 | 目标 | 状态 |
|---|---|---|
| **P0** | 发码本地 106；注册仍进主站；www 只开邮箱 | **✅ 已收口** |
| **P1** | 注册/登录也本地化（与 Token 故障隔离） | 未开工 |
| **不做** | 行情官迁 04；03/04 交叉热依赖 | — |

---

## 三、数据安全红线（迁移/过渡）

### P0——无迁库（已遵守）

1. 未清空业务表；未整文件覆盖 `.env`
2. 用户 id 对齐已实证（43=43）
3. 密钥两边一致；完整值勿写进协作文档

### P1（身份全本地，未开工）——迁库清单预告

| 步骤 | 要求 |
|---|---|
| 导出 | `auth_users`：`id, phone, email, password_hash, *_verified_at, created_at` |
| 导入 | A1 侧扩表，**主键 id 必须原样保留** |
| 窗口 | 短停写；验老用户登录 + 订单仍属原 id |
| 回滚 | 保留 dump；勿删主站账号行 |

---

## 四、分工（收口勾选）

### 副脑03（CN · 行情官）

- [x] 本地直发 + `sms_active_provider=local`（106）
- [x] 密钥与 04 对齐；a1-api 重启
- [x] 106 试发 100；真人注册 id 对齐
- [ ] 确认 HEAD 含 `10038f0`（若仍 48e096f 请补拉）

### 副脑04（SG · Token 正式）

- [x] `api/main.py` 信任跳过 + 备份 + 重启
- [x] `SMS_106_ENABLED=false` 保持
- [x] 无 key 503 / 有 key 可注册
- [ ] 确认测试号 13900007777(id=42) 已清理

### 主脑 / 科设

- [x] 仓库合入并推 Gitee（`48e096f` / `10038f0`）
- [x] P0 作战卡收口
- [ ] P1 身份本地化另开卡（产品确认分账号后再做）

---

## 五、回滚（仍有效）

1. 04：还原 `C:\bak\api-main-pre-sms-trusted-20260804.py` → 重启 core  
2. 03：还原 a1 备份或改回 `identity_proxy`（会再依赖 04 开短信，一般不建议）  
3. **禁止**为救行情官在 04 打开 `SMS_106_ENABLED`，除非产品要开 www 手机注册  

---

## 六、验收标准（P0 已通过）

| 项 | 结果 |
|---|---|
| 发码 | ✅ 106 / 移动号收到 |
| 注册登录 | ✅ |
| id 对齐 | ✅ 43=43 |
| 隔离 | ✅ 04 短信总开关仍关 |
| 安全 | ✅ 无 key 仍 503 |
