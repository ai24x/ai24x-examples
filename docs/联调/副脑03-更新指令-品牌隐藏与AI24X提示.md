# 副脑03 · 品牌隐藏 + AI24X 系统提示 + 路由只读

> 发令：2026-07-30  
> 内容：  
> 1）控制台试调只保留 auto/flash/pro/ultra；结果不展示上游型号  
> 2）chat 加系统提示：自称 AI24X，不报 DeepSeek/小米等  
> 3）API 响应用档位名，`provider=ai24x`（库内仍记上游便于运维）  
> 4）管理台「模型与系统 → 路由状态」可读 OR/直连与 L1–L3  
> 5）若尚未拉：OR 默认 L1=DS Flash / L2=V4-Pro  
> **禁止**整文件覆盖 `.env`；NSSM：`AI24X-core`。静态若 Nginx 直出 `web/`，pull 后硬刷新即可。

## 执行

```powershell
Set-Location C:\ai24x01
git pull origin master
git log -1 --oneline
# 期望含：feat(llm): brand hide + AI24X system prompt

# api\.env 确认（行级核对，勿整文件覆盖）：
# TOKEN_LLM_UPSTREAM=openrouter
# OPENROUTER_API_KEY=...（有余额）
# 不要写 OPENROUTER_MODEL_L1=xiaomi/...（除非要能力向）
# DEEPSEEK_API_KEY 保留作回滚

Restart-Service AI24X-core
Start-Sleep -Seconds 3
curl.exe -sS http://127.0.0.1:8002/v1/models | findstr /i "upstream_mode l1_model"
```

## 验收

1. https://www.ai24x.com/console.html 硬刷新（Ctrl+F5）  
2. 试调 model 下拉只有 auto/flash/pro/ultra  
3. 问「你是什么大模型」→ 宜答 AI24X / 档位，**不宜**主动说 DeepSeek  
4. 返回 JSON 的 `model`/`tier` 为档位名，无 deepseek-v4-flash 字样  
5.（可选）https://www.ai24x.com/token-admin.html → 模型与系统 → 路由状态，能看到 upstream_mode 与各档 model

## 回滚

```
TOKEN_LLM_UPSTREAM=direct
```
→ `Restart-Service AI24X-core`  
（仅回滚上游；品牌隐藏代码随 commit，一般无需回退代码）
