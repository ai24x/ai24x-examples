# 行情官身份本地化 P1 · 作战卡

> 主脑 · 2026-08-04 · 承接短信解耦 P0  
> 目标：**注册/登录/改密不依赖主站**；保留现有 `user_id`；与 www Token **分账号**（同号不同库）

---

## 一、结论口径

| 项 | 口径 |
|---|---|
| 短信 | 继续 `sms_active_provider=local`（106） |
| 账号 | `auth_local_enabled=1` 后本地 `users.password_hash` |
| 与 www | **分账号**：行情官密码不再写 `auth_users`；www 仍用邮箱/主站账号 |
| 老用户 | 必须先 **按 id 导入** 主站 `password_hash`，再开开关 |

---

## 二、代码（已合入仓库，待推 Gitee）

- `p/a1/.../auth_local.py`：本地注册/登录/改密/重置/导入
- `db.py`：`users.password_hash` 列 + CRUD
- `main.py`：`auth_local_enabled` 时走本地；`POST /api/admin/auth_import`
- `admin_ui`：账号模式下拉
- `api/scripts_export_a1_auth_hashes.py`：04 导出脚本
- `requirements.txt`：增加 `passlib`

---

## 三、上线顺序（必须）

1. **本机**验通 → 推 Gitee  
2. **04** 导出切片（勿贴群）→ 安全通道交给 03  
3. **03** 拉码 + `pip install passlib` + 重启 → **先导入** → 再开 `auth_local_enabled=1`  
4. 验收：老用户原密码登录；新用户本地注册；断主站仍可登录  

**禁止**：先开本地开关再导入（老用户全挂）。

---

## 四、复制指令（推 Gitee 后再发）

见下轮消息；先完成本机冒烟。
