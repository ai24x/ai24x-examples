# 副脑03 · 行情官回滚到 7/26 OK 点（27f002b）

> 发令：2026-07-27  
> 主脑本机已把行情官前端恢复到备份点 `27f002b`（对应备份目录 `ai24x01-20260726_112851-OK…`）。  
> **不要整仓 reset**，只回滚行情官相关静态；Token 主站代码保留。

## 为何回滚

- 行情官 **git 代码** 在 7/21～今早之间，实质只有今早一次前端改动（邀请码误报）。  
- 公网「登录已失效」主因仍是 **JWT 签发/验签不一致**，单回滚前端不一定能修好登录；回滚后仍须做 JWT 闭环。

## 执行（PowerShell）

```powershell
Set-Location C:\ai24x01
git fetch origin
git checkout master
git pull origin master

# 确认已含回滚提交（主脑推送后看 log）
git log -5 --oneline -- p/a1/web/index.html

# 只恢复行情官前端到 27f002b（若 pull 后已是回滚结果可跳过）
git checkout 27f002b -- p/a1/web/index.html p/a1/web/account.html p/a1/web/js/shell.js p/a1/web/sw.js
# 若仍托管 p/a 静态登录页：
git checkout 27f002b -- p/a/web/index.html

pm2 restart a1-web-18001
# 进程名以 pm2 list 为准（也可能是 a-web / 其它）

# JWT 闭环（必须）
$env:CORE_ENV = "C:\ai24x01\api\.env"
$env:A1_ENV   = "C:\ai24x01\p\a1\api\server\.env"
$env:A1_ME    = "http://127.0.0.1:8001/api/me"   # 改成实端口
python docs\联调\_jwt_me_roundtrip.py

pm2 restart core-api-8002 --update-env
pm2 restart a-api-8001 --update-env
```

期望：`FILE_MATCH True`、`JOSE_CROSS_DECODE OK`、`LOCAL_ME` 非 401。

## 验收

无痕打开 `https://a.ai24x.com/` → 登录成功，不再「登录已失效」。

## 备份参考（主脑侧已有）

- `E:\AI24X\bak\ai24x01-20260726_112851-OK…`（git tip=`27f002b`）  
- 今早 sleep：`E:\AI24X\bak\ai24x01-sleep-20260727-023603`（HEAD=`50387e4`，a1 网页与 27f002b 同）
