# 行情官短信解耦 · 复制下发指令（03 / 04）

> 主脑整理 · 2026-08-04 · **本机验通并推 Gitee 后再发**  
> 权威卡：`docs/联调/行情官短信解耦-作战卡-20260804.md`  
> 本机清单：`docs/联调/本机自测清单-短信解耦-推Gitee前.md`  
> 口径：副脑 **只拉码 + 行级改配置 + NSSM 重启**，禁止在生产手改业务代码

---

## 通道口径（两边都要知道）

| 通道 | 口径 |
|---|---|
| **106** | 正式主通道 → A1 `sms_active_provider=local`（本站直发） |
| **腾讯** | 电信签名已过；**移动未过** → 仅临时救急，不可全量 |
| **爱聚合** | 备案中 → 勿切 |
| **04 主站短信开关** | **保持关**（`SMS_106_ENABLED=false`），www 继续邮箱注册 |

业务库：`ai24x_a_cn` 本来就在 03；账号密码仍在主站 `auth_users`（P0 不迁表）。

---

## 【复制给副脑04】Token 正式 · 今日卡点

```
【副脑04 · P0 · 行情官注册信任跳过 · 勿开短信总开关】

目标：行情官本站已本地验短信码，注册仍写入主站 auth_users。
04 只需让「带内部密钥的子站注册」跳过 sms_106_enabled 与主站 OTP。
www 手机注册继续关闭。

1) 备份现网
   复制 api\main.py → C:\bak\api-main-pre-sms-trusted-YYYYMMDD.py

2) 更新代码
   从 Gitee 拉取主脑已推送提交（含 auth_register 的 _trusted 跳过）
   只更新 api\main.py（或整仓 pull 后确认该文件已变）
   禁止在 04 上手改业务逻辑；禁止整文件覆盖 .env

3) 配置（行级改 .env，禁止整文件覆盖）
   - SMS_106_ENABLED=false   ← 保持关，不要改成 true
   - system_flags 里 sms_106_enabled 也保持 false（若有）
   - SMS_INTERNAL_KEY 必须与副脑03 A1 的 AI24X_SMS_INTERNAL_KEY 完全一致
     （完整值走安全通道核对，勿贴群；建议与 03 对长度/尾 4 位）

4) 重启
   Restart-Service AI24X-core
   （NSSM；勿用 pm2）

5) 验收（04 本机或公网）
   - 无 X-SMS-Internal-Key 的手机注册 → 仍应 503「手机号注册暂未开放」
   - 有正确 X-SMS-Internal-Key 的注册 → 不应再因短信开关 503
     （可用 03 联调；或内部 curl 测，勿对公网暴露密钥）
   - www 用户侧手机注册仍不可用

6) 回滚
   还原备份 main.py → Restart-Service AI24X-core

完成后回执：备份路径、是否重启、无 key / 有 key 各测一次结果。
```

---

## 【复制给副脑03】行情官 · 通道切回 106 正式

```
【副脑03 · P0 · 行情官短信本站直发 · 正式通道=106】

背景：主站(04) 关短信后，identity_proxy 会挂。
今日方案：A1 本地直发，注册仍写主站；04 另做信任跳过（见 04 指令）。
通道真相：后台一直用 106；腾讯移动签名未过；爱聚合备案中。

一、代码（拉 Gitee，禁止现场改业务代码）
1) git pull（或按主脑指定 commit）后确认：
   - p\a1\api\server\app\sms_local.py 存在
   - main.py：local|juhe|tencent 走 send_local_sms；register 本地验码后带 X-SMS-Internal-Key
   - admin_ui：下拉有「106网关（本站直发 · 正式）」value=local
2) Restart-Service AI24X-a1-api

二、配置（admin_config / 管理台，对照早期通联备份）
1) sms_active_provider = local          ← 正式；若现是 tencent 请改回
2) sms_106_endpoint / account / password / template 填有效 106
   - 模板须含 {code}
   - 对照本机早期备份或 03 上「短信接口配置曾通」文档
   - 发码网关返回 100 才算通；101=账密问题，先修账号
3) AI24X_IDENTITY_API_BASE=https://api.ai24x.com（保持）
4) AI24X_SMS_INTERNAL_KEY 与 04 的 SMS_INTERNAL_KEY 完全一致
5) 腾讯/爱聚合参数可留着备用，但勿切 juhe；tencent 仅 106 故障且仅电信号临时

三、自测（04 信任跳过上线后一起做完整链）
1) 先单独测发码：管理台或 POST /api/auth/sms/send
   - 优先用【移动号】验 106（腾讯移动未过，不能用腾讯冒充全量）
   - 电信号也可再验一次
2) 等 04 回执通过后：发码 → 注册 → 登录 → /api/me
3) 查库：主站 auth_users 新行 id = 本库 ai24x_a_cn.users.id
4) 老用户密码登录仍正常

四、回滚
- sms_active_provider 改回 identity_proxy（会再依赖 04 开短信，一般不建议）
- 或还原 C:\bak\a1-api-pre-smslocal-* 

五、数据红线
- 禁止清空 users / quota / pay_orders
- 禁止整文件覆盖 .env
- 今日不迁 auth_users

完成后回执：provider 当前值、106 试发结果（移动/电信）、是否等 04 联调注册。
```

---

## 你回来后的联调顺序（建议）

1. 先发 **04**（卡点；不发则注册仍 503）  
2. 再确认 **03** 已是 `local` + 106 有效  
3. 你或 03：移动号走完 发码→注册→登录  
4. 抽查：www 手机注册仍关；老用户登录 OK  
