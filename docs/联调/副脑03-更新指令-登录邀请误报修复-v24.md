# 副脑03 · 更新：登录邀请码误报修复（v24）

> 主脑已核实：回滚到 7/26 后，**本地与公网 `index.html` 一致**，因此公网仍会出现「邀请码绑定失败…」（旧逻辑 + `?i=` 链接）。  
> 本次只改前端登录体验，**不是**再整仓回退。生产无 `a1-web` 进程。

```powershell
Set-Location C:\ai24x01
git pull origin master
git log -1 --oneline -- p/a1/web/index.html
# 期望含 inviteCodeForPostAuth；可用：
Select-String -Path p\a1\web\index.html -Pattern "inviteCodeForPostAuth"

# 静态 Nginx 直出，pull 即生效；API 仅当改过 env 才重启
# pm2 restart a-api-8001 --update-env
# pm2 restart core-api-8002 --update-env

# 可选
nginx -s reload
```

验收：无痕打开 `https://a.ai24x.com/index.html?i=BWPX3Z8B` → 登录应显示「登录成功」，**不应**再被邀请码失败盖住。  
若仍出现「登录已失效」→ 另跑 JWT 闭环 `docs\联调\_jwt_me_roundtrip.py`（与邀请码无关）。
