# Token 主站 · 控制台品牌隐藏与管理后台规划（草案）

> 2026-07-30 · 对齐「对外只卖 AI24X 档位，不暴露上游品牌」  
> 执行原则：**先本机跑通 → 再推生产**

---

## 一、用户端：不暴露上游品牌（已部分落地）

| 表面 | 口径 |
|------|------|
| 控制台试调下拉 | 仅 `auto / flash / pro / ultra`（已改） |
| 试调结果 | 展示 `tier` + 回复，不展示 DeepSeek/MiMo/provider（已改） |
| 流水列表 | model 映射为品牌档（已改） |
| 原始 API JSON | 响应 `model`=档位、`provider=ai24x`；上游型号仅库内/管理端可见 |

**高阶 / 后台才显示**：真实上游 id、provider、route_attempts、通道密钥状态、OR/直连切换。

系统提示：默认自称 AI24X；可用 `TOKEN_LLM_SYSTEM_PROMPT` 覆盖。

---

## 二、重要规划排序（本地先做）

### P0（本周 · 主站可运营）

1. **付费闭环本机验收**：微信/支付宝 mock 或沙箱 + PayPal；余额扣减  
2. **PayPal Webhook** 履约兜底（本机 Sandbox 验）  
3. **入门包正式价**（去掉长期 ¥1 联调价，或拆 `debug` SKU）  
4. **控制台/API 品牌字段统一**（API 返回档位名）  

### P1（管理后台 · 对齐行情官壳、内容重做）

现有 `web/token-admin.html` 已有：看板、订单、通道 pill、查用户、加额、流水。建议扩成：

| 模块 | 能力 | 参考 |
|------|------|------|
| **总览** | 今日充值/消耗/新用户/失败单 | 行情官 dashboard |
| **支付通道** | 微信/支付宝/PayPal 就绪态、mode、mock 开关（只读+运维提示，密钥不回显） | a1 短信/支付配置页（脱敏） |
| **用户** | 邮箱/手机查找、钱包、VIP、冻结、手工调账 | a1 用户运营 |
| **订单** | pending 查单履约、退款备注、分页筛选 | 已有加强 |
| **模型路由** | 当前 `TOKEN_LLM_UPSTREAM`、L1/L2/L3 展示名、一键「能力向 MiMo」说明（改 env 仍人工/指令） | 新建；**勿在公网页改密钥** |
| **系统** | 公告、注册开关、日限、Starter 价开关 | 轻量配置表或 env 白名单 |
| **安全** | 继续 `X-SMS-Internal-Key`；后续管理员账号+OTP | a1 admin OTP |

UI：继续「左一级 / 右子导航」壳（已对齐 a1），内容按 Token 业务，不照搬行情官游戏模块。

### P2（后置）

- 副脑04 / 国际主体 PayPal  
- OpenRouter↔direct 热切换 UI（仍建议指令+重启，不网页写 `.env`）  
- 硅基 L0、多租户、代理分销后台  

### 刻意不做

- 用户控制台展示 DeepSeek/小米/Qwen 名称  
- 管理后台网页直接 Write 生产 `.env`  
- 行情官游戏数值搬进 Token 后台  

---

## 三、建议下一迭代（编码）

1. ✅ 控制台品牌隐藏 + 系统提示 + API `model`=档位  
2. ✅ token-admin「路由状态」+ `GET /v1/admin/token/routing`  
3. ⏳ PayPal Webhook + 入门包正式价（本机先）  
4. ⏳ token-admin：通道详情脱敏 + 用户冻结  

---

*主脑草案 · 可随实现改成正式 DEC*
