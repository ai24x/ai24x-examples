# 归类说明（2026-09-08）

本目录是 Cursor 误按「docs/备份 里程碑包」落的完整 dump，**不是正式全量位置**。

**正式全量（按规则）**：
`E:\AI24X\bak\ai24x01-20260908_110434-BYOK基本正常版`

规则见：`docs/备份/备份归类规则.md`

本包仍可作旁路参考（含 rollback 脚本），但回滚优先用 bak 下正式快照。
