# AI24X Markets · Phase 2 任务拆解与实现方案

> 定稿：2026-08-16 ｜ 整理：司令（本机 Codex）｜ 状态：雷总已拍板 C 计划，markets 本周主线
> 关联：《2026-08-15-国际版AI行情工具MVP清单.md》v0.5 ｜ 目标：本周末前完成订阅+PayPal 收钱闭环

## 一、Phase 2 范围（4 块）

| # | 模块 | 端点 | 说明 |
|---|---|---|---|
| 1 | AI 点评（Pro 主力） | `POST /api/ai/brief` | 输入 K线统计/均线/MACD/RSI/位置/量能 → 技术体检式描述；日缓存；免费档日 3 次 |
| 2 | 订阅计费 | `POST /api/subscribe/checkout` + `GET /api/subscribe/status` | $14.9/月、$119/年；一次性买月/年卡，custom_id 标记时长 |
| 3 | PayPal 闭环 | `POST /api/paypal/webhook` | 复用核心层 pay_paypal.py；webhook 验签 → 激活订阅 |
| 4 | 自选 watchlist | `GET/POST/DELETE /api/watchlist` | 免费 3 个 / Pro 50 个 |

## 二、架构决策

- **身份**：复用核心层账号（/api/me 已代验 /v1/user/info），markets 不建独立账户体系
- **数据**：markets 本地 SQLite（`api\server\data\markets.db`）存 订阅订单 / watchlist / 用户订阅状态；AI 点评缓存用文件缓存（沿用 Phase 1 模式）
- **模型**：AI 点评走核心层 `CORE_BASE + /v1/chat/completions`（内部 key，不暴露给前端）
- **支付**：复用核心层 PayPal 凭据；markets 创建订单时带 `custom_id=markets:{user_id}:{months}` 标记，webhook 按 custom_id 激活

## 三、数据模型（SQLite）

```sql
CREATE TABLE IF NOT EXISTS subscriptions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id TEXT NOT NULL,            -- 核心层用户 id
  plan TEXT NOT NULL,               -- monthly | yearly
  status TEXT NOT NULL DEFAULT 'active',  -- active | expired | cancelled
  started_at TEXT NOT NULL,
  expires_at TEXT NOT NULL,
  source TEXT DEFAULT 'paypal',
  created_at TEXT DEFAULT (datetime('now'))
);
CREATE INDEX IF NOT EXISTS idx_sub_user ON subscriptions(user_id);

CREATE TABLE IF NOT EXISTS orders (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  paypal_order_id TEXT UNIQUE,
  custom_id TEXT NOT NULL,
  amount_usd REAL NOT NULL,
  plan TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'created',  -- created | approved | paid | failed
  created_at TEXT DEFAULT (datetime('now')),
  paid_at TEXT
);

CREATE TABLE IF NOT EXISTS watchlist (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id TEXT NOT NULL,
  symbol TEXT NOT NULL,
  created_at TEXT DEFAULT (datetime('now')),
  UNIQUE(user_id, symbol)
);
```

## 四、API 设计

### 1. AI 点评 `POST /api/ai/brief`
- 入参：`{symbol, period="day"}`
- 鉴权：Bearer（免费用户限日 3 次，Pro 不限）
- 处理：拿 signals 数据 → 组装统计（最新价/涨跌幅/MA 关系/MACD 状态/RSI/位置/量能）→ 提示词模板 → 调核心层模型 → 合规黑名单过滤 → 写日缓存
- 出参：`{code:0, data:{symbol, brief, generated_at, cached}}`
- 合规：输出禁词（trade/signal/advisor/tips/picks/broker/buy/sell/买入/卖出/推荐/目标价）；只描述事实，不给建议

### 2. 订阅 `POST /api/subscribe/checkout`
- 入参：`{plan="monthly"|"yearly"}`（Bearer）
- 处理：创建 PayPal order（$14.9/$119）→ 返回 `{approve_url, order_id}`
- `GET /api/subscribe/status`：返回当前用户订阅状态 + 到期日（供前端/后端校验）

### 3. PayPal webhook `POST /api/paypal/webhook`
- 验签（复用 pay_paypal.py 模式）→ 取 custom_id 解析 `markets:{user_id}:{months}` → 激活/续订订阅 → 写订单 paid

### 4. watchlist `GET/POST/DELETE /api/watchlist`
- 免费 3 个 / Pro 50 个（按订阅状态判断）

## 五、任务拆分（司令按序执行）

- [x] T1：`billing.py` —— 订阅计划常量 + SQLite 初始化 + 订阅状态查询/激活/到期校验（✅ 08-16 冒烟通过：激活/续订/自选/订单全绿）
- [x] T2：`watchlist` 端点（SQLite CRUD + 配额）（✅ 08-16 已上线，401 鉴权验证通过）
- [x] T3：`ai_brief.py` —— 提示词模板 + 统计组装 + 合规黑名单 + 日缓存 + 核心层模型调用（✅ 08-16 本机联调通过：AI 模式输出合规描述、日缓存命中不扣额度、免费 3 次/日 403 拦截、401/503 降级正常）
- [x] T4：`/api/subscribe/checkout` + PayPal order 创建（复用 pay_paypal.py 凭据）（✅ 代码完成，待 PayPal 凭据联调）
- [x] T5：`/api/paypal/webhook` 验签 + 激活（✅ 代码完成，待凭据联调）
- [x] T6：前端：定价/订阅按钮 → checkout → PayPal 跳转 → 回跳 capture；Pro 解锁 AI 点评（✅ 08-16 app.html 已加 AI Brief 面板 + Pro 订阅面板 + PayPal 回跳 capture；index.html 定价卡加订阅入口；Playwright 验证登录态/点评生成/额度引导无 JS 报错）
- [ ] T7：本地全流程联调（沙箱 PayPal 或仅 webhook 模拟）+ 04 部署文档（待 PayPal 凭据）

> 08-16 进度快照（第二班）：T1-T6 完成；T7 待 PayPal 凭据（沙箱/生产）后联调部署。18012 已用新代码重启（PID 41540；启动脚本 `scripts\start_local.ps1`）。

## 七、T3/T6 实现备注（08-16 第二班）

### ai_brief.py（T3）
- 数据输入：腾讯→东财→新浪日K→缓存（复用 providers_us）；信号引擎零改动（build_signals_v3 + _sma + 自算 Wilder RSI / 60 位位置 / 量能）
- 模型：核心层 `POST {CORE_BASE}/v1/chat/completions`，`AI24X_MARKETS_MODEL_KEY`（04 生产 .env 配内部 sk- key）+ `AI24X_MARKETS_MODEL`（默认 flash）
- 合规三保险：① prompt 硬约束（禁 buy/sell/hold/target/trade/trading/signal 等，禁个性化）② 输出后置黑名单（中性化 "trading below/at"、"signal line" 等合法技术语后，仍命中即降级）③ 兜底模板 + 全站免责
- 缓存：按 symbol+period+UTC 日 文件缓存，命中不扣额度；失败/无 key 自动降级 deterministic 模板（mode=fallback_error）
- 额度：`billing.consume_ai_brief`（免费 3/日，Pro 不限），`/api/subscribe/status` 返回 `brief_remaining`

### 前端（T6）
- app.html：AI Technical Brief 面板（登录引导/额度显示/升级链接）+ Pro 订阅面板（月/年按钮 → checkout → PayPal 跳转 → 回跳 `?pay=done&token=<orderId>` → capture）
- index.html：定价卡 Pro 月/年 增加 Subscribe 按钮（跳 app.html#sub）

## 六、风险与守则

- 🔴 凭据：PayPal 密钥只进 04 生产 .env，不进 git/ops
- 🟡 免费额度：AI 点评免费档日 3 次，防刷（按 user_id + 日缓存）
- 🟡 合规：AI 输出黑名单 + 全站免责 + 不接实时行情
- 🟢 部署：18012 独立服务，04 节点与海外站同栈，独立回滚

---

*AI24X 指挥中心 · 2026-08-16 · Phase 2 拆解（司令执行 T1-T7，04 负责部署验收）*
