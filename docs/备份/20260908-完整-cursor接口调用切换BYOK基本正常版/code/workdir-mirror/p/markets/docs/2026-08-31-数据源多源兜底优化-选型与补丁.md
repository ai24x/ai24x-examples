# markets 财经数据源多源兜底优化（2026-08-31 雷总拍板）

## 数据源选型
- 免费：Stooq（无key CSV日线，延迟15-30min，限频1req/2s）→ Yahoo/yfinance（无key JSON，延迟15min，需UA）→ AlphaVantage免费档（key，5req/min/500day）｜TwelveData免费档备选
- 付费预留：Polygon（$29/mo起）或 AlphaVantage付费档；开关 MARKETS_US_PAID_PROVIDER 默认空=off

## 补丁（providers_us.py 增量，未本地测试需QA）
1. _PAID_PROVIDER=os.getenv("MARKETS_US_PAID_PROVIDER","").lower(); _FREE_FALLBACK=os.getenv("MARKETS_US_FREE_FALLBACK","1")!="0"; _STOOQ_GATE=_RateGate(2.0,30)
2. 新增 _fetch_stooq_kline(symbol,count)（CSV解析，to_tencent_symbol+.us，失败抛_SourceError走熔断）
3. 新增 _fetch_paid_kline() 对接位：未配置抛 "paid provider not configured"；预留 Polygon MARKETS_POLYGON_KEY
4. get_kline_rows 链路：腾讯→东财→Stooq(免费开关)→缓存；meta.sources_tried + 保留15分钟延迟声明 + meta.is_illustrative 门控（合成数据不上生产）

## 验收（04 部署后）
- 抽样3-5 symbol 免费源正常出数；单源故障自动切换；illustrative 不上生产