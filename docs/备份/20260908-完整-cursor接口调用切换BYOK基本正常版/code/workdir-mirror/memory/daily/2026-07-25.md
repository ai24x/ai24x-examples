# 2026-07-25 作战卡 — Token 平台 v3.3（A+C）

## 上线口径
- **策略**：Phase 1 国内验证 → Phase 2 国际；协议保持 `X-API-Key` + `prompt`
- **总纲**：以 `开发总纲-AI24X-API-v3.3.md` 为准（v3.2 科审版仅对照）
- **范围**：根目录 `api/`（PM2 `core-8000`）；**不改 a1 履约**

## 今日已落地
- [x] 写出 v3.3 执行总纲；v3.2 顶部指向 v3.3
- [x] FREE 网关日限改为 **100**（历史 10000 会在下次 chat 同步纠正）
- [x] 品牌 model：`flash` / `pro` / `ultra` / `free`
- [x] 上游全失败落 stub：**不扣费**（`token_count=0`）
- [x] 日报草稿：`api/scripts_token_daily_report.py`
- [x] 控制台 model 下拉对齐品牌档
- [x] `pm2 restart core-8000 --update-env`

## 21:00 雷总自检
1. 打开 `开发总纲-AI24X-API-v3.3.md` 扫一眼是否 OK
2. http://127.0.0.1:8000/v1/models → 应有 `brand.flash`，`upstream.mode=live`
3. http://127.0.0.1:8000/console.html → 试调选 `flash`
4. **期望**：`provider=deepseek`。若仍是 `stub` + route 里 `401` → **DeepSeek Key 无效，需到 platform.deepseek.com 换新 Key 写入 `api/.env` 后 `pm2 restart core-8000 --update-env`**
5. mock 加油包到账；看余额
6. a1：http://127.0.0.1:18001/ 回归一眼

## P0 阻塞（需人工）
- [x] 有效 DeepSeek API Key（已换新 Key；上游模型改为 `deepseek-v4-flash` / `deepseek-v4-pro`）
- [x] 端到端 `provider=deepseek` 已通（2026-07-25 23:00 左右）

## P1（可后补）
- 邀请反作弊、飞书接日报、`FEISHU_WEBHOOK_URL`
- Phase 2：英文 / PayPal / 海外节点 / OpenAI 兼容
- 可选：`DEEPSEEK_THINKING=1` 开启 v4 推理模式

## 安全边界
- 未改 `p/a1/**`
- `TOKEN_PAY_ENABLED` 生产保持 false，直至独立 notify
- **勿把 DeepSeek Key 提交进 git / 聊天记录外传；若曾泄露建议在平台轮换**

- [x] 注册/登录仅邮箱；去掉 dev_code 文案；创建密钥改为模态卡片

