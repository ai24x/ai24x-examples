## 2026-04-27（副脑01）作战记忆

### 今日目标
- 修复 a1（`p/a1`）支付/伙伴计划体验问题，并完成一次可回滚备份。
- 对主站（8000）+ 子站（a1：18001/18011）做一次“实扫”式风险评估并落地 P0。

---

### 已完成（功能/体验）
- **伙伴专业档仍可购买成长档**：已做“双保险”
  - 前端：专业档有效期内禁用成长档选择并给出提示
  - 后端：若“高档位仍有效”则拒绝购买低档位（报错 `higher_tier_active`；前端已中文化）
- **伙伴扫码支付成功后二维码窗不自动关闭**：已修复
  - 原因：轮询判断用 `tier`（评估信息）而不是 `agent_status`（真实到期）
  - 修复：轮询/快照改用 `d.agent.expires_at/level`，成功时自动 `payOverlayClose()`
- **到期日期展示**
  - `account.html` 增加：`VIP 到期：YYYY-MM-DD`、`伙伴到期：YYYY-MM-DD`
  - 伙伴到期读取兼容：优先 `tier.expires_at`，否则回退 `agent.expires_at`
- **伙伴计划文案收敛**：默认精简，下单更顺
  - 长说明、规则、档位差异收纳到 `details/summary` 的“展开说明”
- **未支付也会过一段时间弹“未检测到支付结果”**：已优化不打扰
  - 只有二维码窗仍打开才会弹超时提醒；用户手动关闭扫码窗会停止轮询且不再弹窗

---

### 已完成（安全加固：按报告 P0 优先）
- **子项目 API 根路由不再泄露后台挂载路径/OTP状态**
  - `GET http://127.0.0.1:18011/` 现在仅返回：`{"ok": true, "service": "ai24x-p-a-api"}`
- **后端兜底安全响应头（防裸奔）**
  - 已加：`X-Content-Type-Options`、`X-Frame-Options`、`Referrer-Policy`、`Permissions-Policy`
  - HSTS：仅在 prod 且 HTTPS/反代标记为 https 时才会加（避免本地 http 误伤）
- **生产环境默认关闭 `/docs`、`/openapi.json`**
  - dev 环境保持开启便于联调；prod 自动关闭

---

### 已重启服务（本机）
- `pm2 restart a1-api-18011`
- `pm2 restart a1-web-18001`

---

### 本次可回滚备份（已完成）
- **备份目录**：`E:\AI24X\bak\ai24x01-20260427-005233\`
  - **代码**：`...\code\`（整目录复制）
  - **PG dump**：`...\db\ai24x_a_pre-20260427-005233.dump`（`pg_dump -Fc -Z 6`）
  - 说明：本机 DSN 来源于 `p/a1/api/server/.env`（预演库 `ai24x_a_pre`）

---

### 明早 9 点后：副脑03（生产）更新最短 runbook（建议）
- **P0：先备份（PG + 代码可追溯）**
  - PG：用副脑03的生产 DSN（例如 `ai24x_a_cn`）执行 `pg_dump -Fc`
  - 代码：建议先把副脑01的更新 push 到 Gitee，并打 tag/发布点（便于回滚）
- **更新代码**
  - `git fetch --all --tags`
  - `git checkout <release-tag 或 main>`
  - `git pull`
- **依赖（requirements 有变才做）**
  - `cd p\\a1\\api\\server; pip install -r requirements.txt`
- **重启**
  - `pm2 restart a1-api-18011; pm2 restart a1-web-18001`
- **最小验收（必须全 200）**
  - `http://127.0.0.1:18011/health`
  - `http://127.0.0.1:18001/index.html`
  - `http://127.0.0.1:18011/api/kline?secid=1.000001&period=day&count=10`
- **生产安全项核对**
  - 生产 Nginx（a.ai24x.com 的前端 + /api）补齐安全头（仓库已有模板可迁移）
  - 生产强制开启管理 OTP：`admin_browser_otp_enabled=on` + OTP 白名单（环境变量或库表）

---

### 待办（下一阶段）
- 生产 Nginx 安全头落到 a.ai24x.com（前端 + /api）并验证响应头
- 收敛 `shell.js` 的 `innerHTML`（降低 XSS 风险面）
- 评估用户 token 从 localStorage → 更安全的会话方案（需结合主站/子站域策略）

