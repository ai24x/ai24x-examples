# 作战卡 2026-08-01 · 名模目录对齐 OR 现网

## 已做（本机，未推）
对照 OpenRouter 现网挂牌，VIP 点名与公开档默认全部升到现行主推：

### 中国名模
- Kimi K2 → **K3**（×12）
- MiMo → **v2.5 Pro**（×3）
- MiniMax M2 → **M3**
- Qwen 2.5-72B → **Qwen3.7 Max**
- GLM-4.5 → **GLM-5.2**
- DS V4 Flash/Pro：已是现行；`planned` → `ready`

### 国际名模
- Claude Sonnet 4 → **Sonnet 5**；Haiku 3.5 → **4.5**；新增 **Opus 5**（×22）
- Gemini 2.5 → **3.1 Pro / 3.6 Flash**
- GPT：5.4 / 5 / 5 mini 已就绪；4o 系保留兼容

### 公开档
- ultra 默认：`openai/gpt-5-mini`
- EU 路由：`qwen/qwen3.7-plus`

## 待你下令
- 重启本机 API 验收 `/v1/...` VIP 列表标题
- 是否 commit + 推 Gitee + 副脑03 拉码重启

---

## 补丁（同日）支付 UX + VIP 文案

### 已改（本机）
- 控制台：限购/下单错误 → 支付卡片内红字 + 底部浮层 toast（不再只靠页顶）
- 价格页深链 `?plan=&pay=`：只引导一次并清 URL，**禁止自动 click**（修 Chrome 弹窗拦截死循环）
- 关闭支付卡：作废进行中会话并关掉占位窗
- 「调用名清单」→ **VIP 点名清单**；推荐档优先 + 名模点名；去掉「上游厂商」等开发口吻
- 规则强化 + `python scripts/check_user_facing_copy.py`

### P0 上线（本地先测 → 再推 → 再发副脑04）
1. 本机硬刷新：价格页只有「去购买」；控制台为唯一支付
2. 验：入门包二次购买 → 卡片内 + 底部浮层可见
3. 验：价格页进控制台 → 不高自动弹窗；手动点支付可开
4. 本地 OK 后：commit + 推 Gitee + 发 `docs/联调/副脑04-更新指令-收银台统一控制台-NSSM.md`
