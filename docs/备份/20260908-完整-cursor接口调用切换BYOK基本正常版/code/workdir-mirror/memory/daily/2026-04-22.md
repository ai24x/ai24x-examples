# Daily Log 2026-04-22

## 关键决策

- **DEC-0010**：子项目版本迭代与封存策略：`p/a` 永久封存（只读），后续按阶段使用 `p/a1`、`p/a2`、`p/a3`… 迭代；冻结建议打 tag（如 `a1.0-freeze`）。见 `memory/decisions/DEC-0010-子项目版本迭代与封存策略.md` 与 `docs/VERSIONING.md`。
- **DEC-0011**：生产备份与回滚最小可靠策略：PG 为主；**7 天日备 + 4 周周备 + 3–6 个月月备 + 异地备份 + 恢复演练**；SQLite 封存归档（只读对照）。见 `memory/decisions/DEC-0011-生产备份与回滚最小可靠策略.md` 与 `docs/备份-日常方案.md`。

## 会话作战卡（复制粘贴填写）

- **任务代号**：REFERRALS-LAUNCH / 其他
- **目标一句话**：
- **上线口径**（必须明确）：推荐=（返佣/提现）还是奖励=（次数/额度）？
- **上线范围**：`web/` 主站 / `p/a` 行情官 / 二者都要
- **成功标准**（可验证的 3 条）：
  - [ ] 
  - [ ] 
  - [ ] 

### 现状（贴证据）
- **前端入口**：
- **后端路由**：
- **数据表/配置**：
- **鉴权方式**：X-API-Key / Bearer Token / 其他

### 缺口清单
- **P0（阻塞上线）**：
  - [ ] 
  - [ ] 
- **P1（上线后补）**：
  - [ ] 
  - [ ] 

### 并行分工（默认两条）
- **链路 A：产品/前端/用户路径** → 负责人：
- **链路 B：后端/数据/部署** → 负责人：

### Runbook（上线/回滚）
- **上线**：
  1. 
  2. 
- **回滚**：
  1. 
  2.
