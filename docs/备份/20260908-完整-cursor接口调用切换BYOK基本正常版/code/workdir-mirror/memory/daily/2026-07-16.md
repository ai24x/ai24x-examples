## 日期

- 日期：2026-07-16（晚间续写）
- 当前阶段：A1 公网 BK/THS/搜索/华为布局问题已修复并上线
- HEAD 核对码：**`cf70831`**（双推 gitee + origin）

## 今日结论（最重要的 3 条）

1. **东财 BK 公网信号不全**：根因是回传 K 线时 `time` 为 `{year,month,day}`，`String(time)`→`[object Object]`，`/api/signals/compute` 失败后只剩本地「金/等」；已修序列化，BK 固定走 chart→compute。
2. **THS `ths:` 缓存**：Windows 上冒号写成 NTFS ADS，历史信号被冻住；缓存文件名需 `_safe_signal_cache_filename`，CACHE_VERSION=5，启动删 `data/signal_cache/ths`。
3. **搜索 syht/bdt/gyyc**：拼音可命中 THS；列表 **BK 在前、同花顺在后并标注**（勿把 THS 插到第一项）。

## 本晚已修复清单（已上线）

| 问题 | 修复要点 | 提交 |
|------|----------|------|
| `gyyc` 搜不到高压氧舱 | THS suggest 支持拼音（pypinyin） | `a30d2d7` |
| THS 历史信号缺失 | 禁 `:` 文件名 + 清 ADS + 窗口内锁定 | `a30d2d7` |
| `/api/me` 打到 18001→404 | `shell.js` 本地指 18011 | `a30d2d7` |
| 华为首页验证码按钮看不见 | 窄屏 OTP 上下排 + text-size-adjust | `a30d2d7` |
| syht/bdt 默认点到 THS 与本地 BK 不一致 | 注入同花顺改为 **排后**，标注东财/同花顺 | `dfc32b6` |
| 公网 BK 只有金/等 | `POST /api/signals/compute` + 正确 YMD | `eb25928`→`cf70831` |

## 关键口径（别忘）

- **本地端口不要改成 8001**：本地 `18001/18011`，生产 `a-api-8001`→8001
- BK ≠ THS：同名概念不同编制，信号不可横向对比
- 88 开头付费源（`ths_daily`）可有当日滞后；已收盘历史一般可对齐同花顺同代码
- 生产更新：`git pull origin master` → 清 `signal_cache`（含 `ths` 宿主）→ `pm2 restart a-api-8001`；静态强刷 SW **v22**

## 副脑03 最短更新（已验证通过后的口径）

```bat
cd /d C:\ai24x01
git pull origin master
git log --oneline -1
pm2 restart a-api-8001
```

## 下一步（只列 3 件）

1. 观察公网 BK（商业航天/半导体）信号是否持续稳定
2. 同花顺指标广场是否已真实发布（文档齐、转化待确认）
3. 合规话术落到首页/SEO（仍有「信号」残留）
