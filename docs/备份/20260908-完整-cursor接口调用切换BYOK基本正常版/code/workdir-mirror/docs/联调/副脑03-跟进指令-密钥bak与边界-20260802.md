# 副脑03 · 跟进指令（密钥 bak 清理 + 边界 · 2026-08-02）

> 国际站 `www` / `api` 已在 **副脑04**。本指令给 03：**安全卫生 + 边界确认**，不部署本包 guides。

## 必须做（P0）

1. 检查 `C:\ai24x01`（或你机上的同构仓）是否有 `api/.env.bak*`、`*.bak` 含密钥  
2. **删除或移出仓库目录**；确认未 `git add` / 未 push  
3. 拉最新 master 后 `.gitignore` 已含 `*.bak` / `.env.*`（科设已推）— 本地仍建议手清旧文件  

```powershell
Set-Location C:\ai24x01   # 按你实际路径
git pull origin master
Get-ChildItem -Recurse -Force -Include *.bak,*.env.bak* -ErrorAction SilentlyContinue |
  Where-Object { $_.FullName -match '\\api\\|\\.env' } |
  Select-Object FullName
# 有则删除或移到仓外备份盘；回报：已清理 / 无此类文件
```

## 明确不做

| 项 | 说明 |
|----|------|
| 本包 guides / PayPal 控制台文案 | 由 **04** 上 `www.ai24x.com` 验收 |
| PayPal Live 长期养在 03 | Live 与国际库目标在 **04** |
| 改 `a.ai24x.com` 行情官为跟本包 | 无关 |

## 可选（P1）

- 若 03 仍跑 Token API 过渡实例：确认已拉 `eee0cef`（双语 detail 不泄漏）；国际主流量以 04 为准  
- 国内微信/支付宝联调照旧，**不**为 LobeChat 改 03

## 回报主脑

1. 密钥 bak：已清理 / 本机无  
2. 未误 push 任何 `.env*`  
3. 确认国际验收找 **04**，不挡 03 国内任务
