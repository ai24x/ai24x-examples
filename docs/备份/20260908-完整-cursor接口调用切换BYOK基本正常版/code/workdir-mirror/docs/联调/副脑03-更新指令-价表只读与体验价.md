# 副脑03 · 价表只读 + 入门体验价口径

> 发令：2026-07-30  
> 内容：管理台「支付与订单 → 价表（只读）」；入门包默认仍为体验价 ¥1（可保留）。  
> 用户文案去掉「联调」字样。无需改 `.env` 价键，除非你要改价。  
> **禁止**整文件覆盖 `.env`；NSSM：`AI24X-core`。

## 执行

```powershell
Set-Location C:\ai24x01
git pull origin master
git log -1 --oneline
# 期望含：feat(admin): readonly token plans

Restart-Service AI24X-core
Start-Sleep -Seconds 3
```

## 验收

1. https://www.ai24x.com/token-admin.html → 支付与订单 → **价表（只读）** 能看到 4 档套餐与 env 键  
2. 入门包显示体验价 ¥1 / 1 万 token（若未设 `TOKEN_PRICE_TOKEN_PACK_10K_FEN`）  
3. 定价页/控制台入门包说明为「体验价」，无运维黑话  

## 说明（无需操作）

- PayPal Webhook：P1 兜底，主路径 Capture 已通则暂可不配  
- 微信/支付宝：继续国内 CNY；国际用 PayPal  
