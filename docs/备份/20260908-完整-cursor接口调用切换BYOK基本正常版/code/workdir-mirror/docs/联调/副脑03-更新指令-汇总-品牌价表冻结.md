# 副脑03 · 汇总更新（品牌 + 价表 + 冻结 + models 脱敏）

> 发令：2026-07-30  
> 本机已冒烟：`scripts_token_local_smoke.py` → ALL SMOKE OK  
> **禁止**整文件覆盖 `.env`；NSSM：`AI24X-core`。

## 本包内容

1. 品牌隐藏 + AI24X 系统提示 + chat `model`=档位  
2. 管理台：路由状态 / 支付通道 / **价表只读** / **用户冻结·解冻**  
3. `/v1/models` 档位文案脱敏（上游 id 仍在 `upstream.l*_model` 供运维 curl）  
4. 入门包保持体验价 ¥1（勿抬价）  
5. PayPal Webhook：**代码已有**；真商户挂 URL 可后置（Capture 主路径已通）

## 执行

```powershell
Set-Location C:\ai24x01
git pull origin master
git log -1 --oneline
# 期望含：freeze / plans / brand（本包最新 commit）

# 软迁移会在启动时给 auth_users 补 frozen_at / freeze_reason
Restart-Service AI24X-core
Start-Sleep -Seconds 4

curl.exe -sS http://127.0.0.1:8002/v1/models | findstr /i "upstream_mode flash note"
curl.exe -sS -H "X-SMS-Internal-Key: <主站内部密钥>" http://127.0.0.1:8002/v1/admin/token/plans | findstr /i "token_pack_10k promo"
```

## 验收

1. https://www.ai24x.com/console.html 硬刷新：下拉 auto/flash/pro/ultra；问身份宜答 AI24X  
2. https://www.ai24x.com/token-admin.html  
   - 价表只读可见入门包体验价  
   - 钱包页：冻结 / 解冻（测完请解冻）  
   - 路由状态 / 支付通道可刷新  
3. 冻结用户后登录应提示「账号暂不可用，请联系客服。」

## 回滚

```
git log -5 --oneline
# 必要时 checkout 上一 commit 后 Restart-Service AI24X-core
```

改价仍用行级 `TOKEN_PRICE_*_FEN`；国际支付继续 PayPal，国内微信支付宝不改国际收单。
