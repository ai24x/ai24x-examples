## 日常备份说明（先记录，后续落脚本）

目标：在不打扰日常开发/联调的前提下，实现**可恢复、可追溯、可异地**的备份。

> 结论：生产环境仅保留“最近 2–3 天”**不够**。推荐最小可靠策略为：**7 天热备 + 4 周周备 + 3–6 个月月备 + 至少 1 份异地**；并定期做恢复演练。

### 1) 备份范围（按优先级）

- **代码与文档（最高优先级）**：通过 Git 推送到远端（Gitee/GitHub）即为异地备份。
- **配置与密钥（禁止入库，但必须备份）**：
  - 各服务 `.env`（真实值）
  - Nginx 站点配置、证书与私钥（若有）
  - PM2 配置（如 `ecosystem*.js/cjs`）与自启动相关配置
- **数据（按实际服务）**：
  - **PostgreSQL（主流程）**：每日 `pg_dump`（全量），必要时启用 WAL 归档（PITR 时间点恢复）
  - **SQLite（历史/遗留）**：建议封存归档（只读对照），不再作为生产主流程；如仍存在，仅做“文件级归档备份”
- **可选运行态**：
  - 用户上传目录/静态资源目录（若存在）
  - 重要日志（仅保留最近若干天，用于回溯；不建议长期囤积）

### 2) 推荐策略（分层 + 保留 + 异地）

- **频率（最小可靠）**：
  - **每日全量**（建议凌晨 03:30）
  - **每周全量**（建议周日）
  - **每月全量**（建议每月 1 号）
  - **重要升级/数据库迁移前**：手动补一次“升级前快照”（强制）
- **保留（生产推荐）**：
  - **每日全量保留 7 天**（热备：随时回滚）
  - **每周全量保留 4 周**（覆盖慢性问题）
  - **每月全量保留 3–6 个月**（覆盖长期潜伏的数据污染/误删）
  - 具体可按磁盘与成本调整，但不建议低于上述最小值
- **异地（必做）**：
  - 至少 1 份备份落到**另一台机器/对象存储/NAS**（与部署机物理隔离），防单机盘坏/勒索

### 2.1 进阶：PITR（时间点恢复，强烈建议生产开启）

当生产出现误操作（误删/误更新）时，`pg_dump` 只能回到“某个时间点的快照”。PITR 通过 WAL 归档可以回到“误操作前 5 分钟”。

- 建议生产在稳定后开启：WAL 归档 + 定期 base backup + 异地归档
- 原则：**没演练过的 PITR = 没有 PITR**

### 3) Windows（副脑03）最小可执行流程（后续写脚本）

建议落地目录（示例）：

- 本地：`E:\\BACKUP\\ai24x01\\`
- 异地：`X:\\ai24x01\\`（NAS/云盘挂载盘符）
- 日志：`E:\\BACKUP\\ai24x01\\logs\\`

每日任务执行步骤（示例）：

1. **打包配置**：收集 `.env`、Nginx 配置、PM2 配置、关键运维脚本
2. **备份数据库**：
   - PostgreSQL：`pg_dump -Fc` 输出 `.dump`（推荐），再 zip
   - SQLite（如仍存在）：只做“文件级归档备份”，并明确标注 `sqlite-legacy`（不参与现行恢复口径）
3. **生成校验**：对 zip 生成 `sha256`（可选）
4. **同步到异地**：`robocopy` 或等价方式
5. **清理旧备份**：按保留策略删除过期目录

### 4) 备份是否靠谱的唯一标准：可恢复

每周抽检（可选自动化）：

- 从最近备份解压到临时目录
- 用备份 DB 启动一次 API（或至少执行一次只读查询）
- 通过才算合格（否则视为“备份失败”）

生产建议：每月至少做一次“全流程恢复演练”（新机器/新目录还原 Nginx+PM2+DB），并记录耗时与风险点。

### 5) 后续落地（明天再做）

- 在 `scripts/backup/` 增加：
  - `backup_all.ps1`（Windows 定时任务入口）
  - `backup_sqlite.ps1` / `backup_pg.ps1`（拆分便于复用）
  - `RESTORE.md`（恢复演练指令）
- 文档索引 `docs/README.md` 增加该文档入口

---

## 6) PostgreSQL（Windows 裸机）最小命令卡（可直接手动跑）

以下示例以 **AI 行情官** 的库为例（副脑03：`ai24x_a_cn`）。请按环境替换库名：

- 预演（副脑01）：`ai24x_a_pre`
- 国内生产（副脑03）：`ai24x_a_cn`
- 国际生产（副脑04）：`ai24x_a_intl`

### 6.1 备份（推荐 custom 格式，恢复更稳）

PowerShell：

```powershell
$ts = Get-Date -Format "yyyyMMdd-HHmmss"
$outDir = "E:\BACKUP\ai24x01\pg"
New-Item -ItemType Directory -Force -Path $outDir | Out-Null

# 方式 A：使用环境变量（推荐：从 .env / 系统环境变量读取）
pg_dump -Fc -Z 6 -f "$outDir\ai24x_a_cn-$ts.dump" "postgresql://ai24x_a:<PASSWORD>@127.0.0.1:5432/ai24x_a_cn"

# 方式 B：使用本机已配置的 PG 账号（如已在 pgpass / 登录态中）
# pg_dump -Fc -Z 6 -f "$outDir\ai24x_a_cn-$ts.dump" -h 127.0.0.1 -p 5432 -U ai24x_a ai24x_a_cn
```

### 6.2 恢复（兜底用，先停写/维护模式）

```powershell
# 先确保目标库存在（必要时先 drop/recreate）
pg_restore --clean --if-exists -d "postgresql://ai24x_a:<PASSWORD>@127.0.0.1:5432/ai24x_a_cn" "E:\BACKUP\ai24x01\pg\ai24x_a_cn-YYYYMMDD-HHMMSS.dump"
```

> 原则：**生产恢复前先停写**，并保留“恢复前快照”（再备份一次）。

