# 需求类型：②需副脑办(副脑04)
# 需求描述：Step2 版本号扫尾补拉——open 全站 38 页 locales.js 版本号 20260828k→20260828m（纯静态引用，无内容变更），与 Step2 内容同批；04 上残留的 36 个旧版本引用需随本批归零
# 验收标准：git grep 残留 0；open /health commit=ab4b67d；公网 console.html 含 locales.js?v=20260828m
# 期望时间：立即

> **目标提交：`ab4b67d`**

## 变更摘要

- `p/open/web/*.html`（38 页，不含 console.html 已在上批）：`config/locales.js?v=20260828k` → `20260828m`（纯版本号，1 行/页）
- 无 JS/locales/css 内容变更，open 服务无需重启（FastAPI mount 静态即时生效）；如需稳妥可 `Restart-Service AI24X-open-api -Force`

## 执行步骤

1. `git -C C:\ai24x01 pull`
2. HEAD 校验：`git -C C:\ai24x01 merge-base --is-ancestor ab4b67d HEAD` → 返回 0 即通过（无输出=成功）
3. 残留归零校验（必须无输出）：
   ```powershell
   git -C C:\ai24x01 grep -n "locales.js?v=20260828k" -- p/open/web
   git -C C:\ai24x01 grep -n "console.js?v=20260828j" -- p/open/web
   ```
4. `Restart-Service AI24X-open-api -Force`（可选，建议执行确保静态即时生效），等待 10s
5. 公网验收：
   - `https://open.ai24x.com/health` → commit 以 `ab4b67d` 开头
   - `https://open.ai24x.com/console.html` → 200，且含 `creditsOvCta` + `locales.js?v=20260828m` + `console.js?v=20260828l`，且不含 `id="stat-balance"`

## 群确认（04 必发）

```
✅ 04更新完成｜open统一账单Step2-版本号扫尾｜EXP=ab4b67d HEAD=<12位> health=<12位>｜locales 20260828m 残留 0
```
