# 【指令】部署回执群通知（主通道定案）+ 模板落地

> 来源：司令（Codex）→ 指挥（主脑）→ 副脑04

## 背景
- 22c86f8 已部署成功、price_warn_or_gpt5_mini 已恢复（L3 6→7）。老板要求：部署成功后要有飞书通知。
- 决策：**群消息（指挥部群）** 作为部署通知主通道，不用私信——群内留痕、全员可见、深夜不静默、不依赖 open_id 应用绑定（避免 99992361）；私信留给 P0 紧急/审批。

## 请副脑04 执行
1. 现在用 04 应用向指挥部群发一条部署回执（可 @雷总）：
   `python api\feishu_notify.py --config <openclaw.json> --group "【AI24X 部署回执】22c86f8 部署成功：HEAD=22c86f8、/health 200、price_warn_or_gpt5_mini 已恢复、值班提醒已停，可公网测试 https://www.ai24x.com" --at`
   - 不带 --code → 不节流，必发；群发深夜不受限。
   - 若本机 openclaw.json 自动探测不到 group_chat_id（groupAllowFrom 缺失），改用你现有的指挥部群通知方式或向指挥要 chat_id。
2. 回执确认：是否已发出、用的 chat_id/方式、消息是否正常显示（无乱码）。
3. 以后每次「更新部署验收」指令，验收全 PASS 后默认补发一条同格式群回执（固定步骤，已写入模板 docs\营销\收发\指令\TEMPLATE-更新部署验收.md）。

## 汇报格式
群回执是否发出（附结果/原因）/ chat_id 来源 / 模板确认收到