# 需求类型：②需副脑办(副脑04)
# 需求描述：统一支付中台 Phase 3a —— BYOK 订阅订单并入 core 支付中台（open 服务代理下单/查单/订单列表合并展示；Dodo webhook → core 履约 → 回调 open 激活订阅）；生产 core+open 两端配置 BILLING_SERVICE_KEY 服务密钥
# 验收标准：core/open health commit=4a8f68e 前缀；BILLING_SERVICE_KEY 两端已配置；admin billing 端点鉴权 403 正确；open fulfill 端点存在
# 期望时间：push 后立即 deploy04

> **目标提交：`4a8f68e`**

## 变更摘要

- core `api/`（5 文件）：`config.py` 新增 billing_service_key；`schemas.py` 新增 AdminBillingDodoOrderBody/QueryBody；`pay_products.py` PRODUCTS 加 `byok`（B 前缀 + 远程履约）；`token_pay_service.py` 支持 byok 订单/远程履约（`_fulfill_byok_remote` 幂等）；`main.py` 新增 `_require_billing_service_key` + `/v1/admin/billing/dodo/order|query` + `/v1/admin/billing/orders`；dodo/creem webhook 产品校验分流 markets/byok→product_plan；修复 settle 函数名拼写（`order_settle_amount_fen`）
- open `p/open/api/`（3 文件）：`config.py` 新增 billing_service_key；`schemas.py` 新增 AdminByokFulfillBody；`main.py` 新增 `_core_billing_proxy`（X-Billing-Service-Key 调 core）、`/v1/billing/dodo/order|query` 改服务代理、`/v1/billing/orders` 本地+core 合并倒序、新增 `POST /v1/admin/byok/fulfill`（X-Admin-Key 鉴权 + 幂等激活订阅）、dodo webhook 未知订单幂等忽略
- 无前端/版本号变更（本批纯后端，无需 bump）

## 密钥配置（第一步，必须先跑）

司令已把补丁脚本 scp 到 04：`C:\Users\Administrator\ops\billing_service_key_prod_20260828.ps1`

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File C:\Users\Administrator\ops\billing_service_key_prod_20260828.ps1
```

校验（不打印密钥明文，只看长度与备份）：

```powershell
$c = (Select-String -Path C:\ai24x01\api\.env -Pattern '^BILLING_SERVICE_KEY=(.+)').Matches[0].Groups[1].Value
$o = (Select-String -Path C:\ai24x01\p\open\api\.env -Pattern '^BILLING_SERVICE_KEY=(.+)').Matches[0].Groups[1].Value
if ($c.Length -ne 64 -or $o.Length -ne 64 -or $c -ne $o) { throw "BILLING_SERVICE_KEY 未配置或两端不一致" } else { Write-Host "BILLING_SERVICE_KEY OK len=64 same" }
Get-ChildItem C:\ai24x01\api\.env.bak-billing-key-* , C:\ai24x01\p\open\api\.env.bak-billing-key-* | Measure-Object | ForEach-Object { Write-Host "backup count=$($_.Count)" }
```

## 服务

- `Restart-Service AI24X-core -Force`（www core 8002，nssm）
- `Restart-Service AI24X-open-api -Force`（open 18080，nssm）
- markets 18012 不动

## 执行步骤

1. 备份+清理脏改动（仅本次涉及路径，防止 pull 冲突）：
   ```powershell
   $bk = "C:\Users\Administrator\ops\.bak-unified-billing-3a-20260828"
   New-Item -ItemType Directory -Force -Path $bk | Out-Null
   git -C C:\ai24x01 status --short -- api p/open/api | ForEach-Object {
     $rel = ($_ -replace '^\S+\s+','').Trim()
     if ($rel) {
       $src = Join-Path "C:\ai24x01" $rel
       if (Test-Path -LiteralPath $src) {
         $dst = Join-Path $bk ($rel -replace '[\\/]','__')
         Copy-Item -LiteralPath $src -Destination $dst -Force
         git -C C:\ai24x01 checkout -- $rel
       }
     }
   }
   ```
2. `git -C C:\ai24x01 pull`
3. HEAD 校验：`git -C C:\ai24x01 merge-base --is-ancestor 4a8f68e HEAD` → 返回 0 即通过（无输出=成功）
4. 重启两个服务，等待 15s
5. 公网验收（全部必须通过，逐条回执）：
   - `https://www.ai24x.com/health` → commit 以 `4a8f68e` 开头（core 8002）
   - `https://open.ai24x.com/health` → commit 以 `4a8f68e` 开头（open 18080）
   - core 服务密钥鉴权（不带密钥必须 403/422，不带密钥建不了单）：
     ```powershell
     $r1 = Invoke-WebRequest -Uri "https://www.ai24x.com/v1/admin/billing/dodo/order" -Method POST -ContentType "application/json" -Body '{"email":"nobody@example.com","plan":"byok_pro_month","product":"byok"}' -UseBasicParsing -SkipHttpErrorCheck
     if ($r1.StatusCode -lt 400) { throw "no-key should fail" }
     $r2 = Invoke-WebRequest -Uri "https://www.ai24x.com/v1/admin/billing/dodo/order" -Method POST -ContentType "application/json" -Headers @{"X-Billing-Service-Key"="wrong"} -Body '{"email":"nobody@example.com","plan":"byok_pro_month","product":"byok"}' -UseBasicParsing -SkipHttpErrorCheck
     if ($r2.StatusCode -lt 400) { throw "wrong-key should fail" }
     Write-Host "core billing key auth OK no=$($r1.StatusCode) wrong=$($r2.StatusCode)"
     ```
   - open fulfill 端点存在（无 X-Admin-Key 必须 403）：
     ```powershell
     $r3 = Invoke-WebRequest -Uri "https://open.ai24x.com/v1/admin/byok/fulfill" -Method POST -ContentType "application/json" -Body '{"email":"nobody@example.com","plan":"byok_pro_month"}' -UseBasicParsing -SkipHttpErrorCheck
     if ($r3.StatusCode -lt 400) { throw "no admin key should fail" }
     Write-Host "open fulfill auth OK no=$($r3.StatusCode)"
     ```
   - open 订单合并端点存在（匿名 401）：
     ```powershell
     $r4 = Invoke-WebRequest -Uri "https://open.ai24x.com/v1/billing/orders" -UseBasicParsing -SkipHttpErrorCheck
     if ($r4.StatusCode -ne 401) { throw "anon orders should 401" }
     Write-Host "open orders auth OK anon=$($r4.StatusCode)"
     ```
   - 生产两端 .env 校验（见「密钥配置」段）通过

## 群确认（04 必发）

```
✅ 04更新完成｜统一支付中台Phase3a-BYOK并入core｜EXP=4a8f68e HEAD=<12位> health=<12位>｜BILLING_SERVICE_KEY 两端 len=64 一致｜admin billing/fulfill/orders 鉴权验收通过
```
