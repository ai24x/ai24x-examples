# 【03 更新】盘中默认展示最近收盘定型归档 + 主线口径统一（15:10 扫描）

> 通道：司令直连 03（deploy03.ps1）｜ 目标提交 **7eb2022**（前序 601133f 同批）
> 03 主机：123.207.199.238 · 仓库 C:\ai24x01 · 服务 AI24X-a1-api（NSSM，8001）· 无 D 盘（备份落 C:\backup\ai24x_a\）
> a1 /health 无 commit 字段：验收用「进程新建时间 + 文件标记 + 接口行为」

## 背景
老板确认核心原则：**以收盘后定型数据为主；未收盘时一律展示「最近一个收盘定型归档」（昨日收盘结果），不做盘中默认扫描**（盘中扫描会千人千面、浪费资源）。
- 事故根因：15:03 自动扫描时上游 K 线当日 bar 未定型（实测 15:03-15:07 抓到盘中 bar），生产 08-17 成分评分被污染 → 主线排序与本地不一致（生产「通信光模块CPO」居首 vs 本地「AI服务器算力」居首）。
- 本批修复（601133f + 7eb2022）：
  1. 自动扫描延后 15:03 → 15:10（收盘数据定型后再扫）。
  2. 盘中/非交易日回退展示最近定型归档，且归档/stale 回退时以「最近复盘定型主线」为准重建 mainlines + 判定依据小字（_reattach_ths 统一口径）。
- 本批数据修复：清理生产 08-18 盘前旧算法半成品归档/历史/日缓存；同步本地 08-17 收盘最终版归档 + history 键（hs-23:54 / kc-22:29 / bj-22:29 / bj_all-22:30，新算法最终版）。

## 执行步骤（PowerShell，整段复制运行）
```powershell
$ErrorActionPreference = "Stop"
$REPO = "C:\ai24x01"
$SRV  = "AI24X-a1-api"
$EXP  = "7eb2022"
$BK   = "C:\backup\ai24x_a\20260818_pre_7eb2022"
$PKG  = "C:\Users\Administrator\_sync_20260817_final"
$ARCH = "C:\ai24x01\p\a1\api\server\data\bj_archive"

Set-Location $REPO

"--- 0) 备份本次改动文件 + 相关数据 ---"
$FILES = @("p\a1\api\server\app\bj_screener.py","p\a1\api\server\app\daily_report.py")
New-Item -ItemType Directory -Force -Path "$BK\bj_archive" | Out-Null
foreach ($x in $FILES) {
  if (Test-Path "$REPO\$x") {
    $chg = git -C $REPO status --porcelain -- $x
    if ($chg) { Copy-Item "$REPO\$x" "$BK\$(Split-Path $x -Leaf).bak" -Force; git -C $REPO checkout -- $x }
  }
}
Get-ChildItem "$ARCH\2026-08-18-*.json" -ErrorAction SilentlyContinue | Copy-Item -Destination "$BK\bj_archive" -Force
foreach ($f in @("2026-08-17-hs.json","2026-08-17-kc.json","2026-08-17-bj.json","2026-08-17-bj_all.json")) {
  if (Test-Path "$ARCH\$f") { Copy-Item "$ARCH\$f" "$BK\bj_archive" -Force }
}
Copy-Item "C:\ai24x01\p\a1\api\server\data\bj_scan_history.json" "$BK\bj_scan_history.json.bak" -Force
Copy-Item "C:\ai24x01\p\a1\api\server\data\bj_scan_daily.json" "$BK\bj_scan_daily.json.bak" -Force
"备份目录: $BK"

"--- 1) 拉码 + HEAD 校验（目标提交在历史中）---"
git pull
$HEAD = (git rev-parse --short=12 HEAD).Trim()
"HEAD=$HEAD"
git merge-base --is-ancestor $EXP HEAD
if ($LASTEXITCODE -ne 0) { Write-Host "!! HEAD 不含目标提交 $EXP" -ForegroundColor Red; exit 1 }

"--- 2) 文件标记检查 ---"
$bj = Get-Content "$REPO\p\a1\api\server\app\bj_screener.py" -Raw -Encoding UTF8
$dr = Get-Content "$REPO\p\a1\api\server\app\daily_report.py" -Raw -Encoding UTF8
"m_reattach=$($bj.Contains('主线统一口径：归档/stale 回退时'))  m_info=$($bj.Contains('_mainline_info_from_archive'))"
"m_1510=$($bj.Contains('_AUTO_SCAN_TS = 15 * 3600 + 10 * 60'))  m_closed=$($bj.Contains('交易日 15:10 后视为收盘'))"
"dr_1510=$($dr.Contains('auto_scan_time: str = "15:10"'))  dr_note=$($dr.Contains('15:03 时上游 K 线当日 bar 尚未定型'))"
if (-not ($bj.Contains('主线统一口径：归档/stale 回退时') -and $bj.Contains('_mainline_info_from_archive') -and $bj.Contains('_AUTO_SCAN_TS = 15 * 3600 + 10 * 60') -and $dr.Contains('auto_scan_time: str = "15:10"'))) {
  Write-Host "!! 文件标记缺失" -ForegroundColor Red; exit 1
}

"--- 3) 重启 AI24X-a1-api (8001) ---"
$before = (Get-Process -Name python -ErrorAction SilentlyContinue | Sort-Object StartTime -Descending | Select-Object -First 1).StartTime
Restart-Service $SRV -Force
$ok = $false
for ($i = 0; $i -lt 15; $i++) {
  Start-Sleep -Seconds 5
  try { $h = Invoke-RestMethod -Uri "http://127.0.0.1:8001/health" -TimeoutSec 10 -UseBasicParsing; $ok = $true; break } catch {}
}
if (-not $ok) { Write-Host "!! /health 未就绪" -ForegroundColor Red; exit 1 }
"health=$($h.status)"
$after = (Get-Process -Name python -ErrorAction SilentlyContinue | Sort-Object StartTime -Descending | Select-Object -First 1).StartTime
"proc_before=$before"
"proc_after =$after"
if ($after -le $before) { Write-Host "!! 进程新建时间未更新" -ForegroundColor Red; exit 1 }

"--- 4) 数据清理 + 08-17 最终归档/历史同步（脚本见 _apply_sync.py）---"
& C:\PYTHON314\PYTHON.EXE "$PKG\_apply_sync.py"
if ($LASTEXITCODE -ne 0) { Write-Host "!! 数据同步失败" -ForegroundColor Red; exit 1 }

"--- 5) 再次重启（重载清理后的内存缓存）---"
Restart-Service $SRV -Force
$ok = $false
for ($i = 0; $i -lt 15; $i++) {
  Start-Sleep -Seconds 5
  try { $h = Invoke-RestMethod -Uri "http://127.0.0.1:8001/health" -TimeoutSec 10 -UseBasicParsing; $ok = $true; break } catch {}
}
if (-not $ok) { Write-Host "!! /health 未就绪" -ForegroundColor Red; exit 1 }

"--- 6) 行为验收（盘中回退 08-17 定型归档 + 主线口径）---"
& C:\PYTHON314\PYTHON.EXE "$PKG\_verify_03.py"
if ($LASTEXITCODE -ne 0) { Write-Host "!! 行为验收失败" -ForegroundColor Red; exit 1 }

"--- 7) 公网页面可访问 ---"
$pub = (Invoke-WebRequest -Uri "https://a.ai24x.com/gd.html" -UseBasicParsing -TimeoutSec 20).StatusCode
"pub_status=$pub"

Write-Host "=== 部署成功 OK ===" -ForegroundColor Green
```

## 验收清单（回执时逐条列出）
- HEAD=7eb2022（git merge-base 校验通过）／文件标记（_reattach_ths 主线统一口径 + _AUTO_SCAN_TS 15:10 + daily_report auto_scan_time 15:10）
- 进程新建时间更新（两次重启）／health 200
- _apply_sync.py：removed_archives≥1（08-18 stale 移出）、hist_keys 无 2026-08-18、daily_keys 无 20260818 与 test-key-xyz、8 个 08-17 归档文件复制 OK、INDEX_REBUILT
- _verify_03.py：DML=AI服务器算力,通信光模块CPO,半导体；4 市场 LAT date=2026-08-17 且 gen=1786982051/1786976983/1786976995/1786977001；SCAN stale=True intraday=True、ml 首名 AI服务器算力（含 top5 判定数据）、picks 为 08-17 最终版
- 公网 https://a.ai24x.com/gd.html 200（浏览器端 Ctrl+F5 看主线：AI服务器算力居首 + 判定小字）
- ⚠️ 问题项：无则写「无」

## 回执格式（03 自动发指挥部群）
- ✅ 已完成项：HEAD／文件标记／两次重启+health／数据清理与同步计数／行为验收（DML+4市场回退 08-17+主线首名+判定数据）／公网 200
- ⚠️ 问题项：逐条（无则写「无」）
- 凭据纪律：不回执任何 token 明文；备份目录仅报路径。

## 回滚预案
- `git revert 7eb2022` + 重启 AI24X-a1-api；数据回滚：恢复 `C:\backup\ai24x_a\20260818_pre_7eb2022\bj_scan_history.json.bak` 与 `bj_scan_daily.json.bak`、恢复 `bj_archive\` 内被覆盖/删除文件 + 重启。
