桥接协议升级 v4，请执行以下更新并并入长期记忆（SOUL.md/TOOLS.md）：

1. 【防递归】你的 cron「CX桥接-双向转达」只驱动 收发\指令\ 根层的【老板-*】指令文件（老板飞书消息落盘）。凡是命名含 codex- 的派发文件（司令派给你执行的），由你（指挥）自己读取执行，不要驱动桥接，避免递归循环。
2. 【进行中状态】回复文件以 [CX-Bridge 进行中] 开头 = 非失败：worker 后台继续跑，你转达给老板作"处理中"状态即可，不重试、不视为异常；回复文件就绪后（内容不以 [CX-Bridge 开头）你照常转达并移入 _sent\。
3. 【v4 持久线程】cx_bridge.ps1 已升级 v4，默认写入持久值守线程 019fdcdd-e3e7-7122-90a0-8ac181c9b141：所有桥接任务与回复都会追加进该线程形成连续记录（老板可在 Codex 桌面侧边栏打开查看）。驱动命令不变，无需传 -ThreadId。
4. 【立即转达】收发\回复\2026-08-07-2330-老板-司令-总流程-out.txt 是 23:30 总流程指令的真实回复（worker 后台完成后写入），请现在转达老板（带 [司令]），并移入 _sent\。
收到请回执一行确认。