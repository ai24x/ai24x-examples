# 【04 更新】console 充值页 Markets Pro 置顶 + Token 下沉 + 三站页眉页脚统一 · 目标 9247ff8

> 通道：司令直连 04（deploy04.ps1 同步 600s）。验收全 PASS 后自动发指挥部群回执；有问题先回传司令，不发群。

## 背景
- 老板反馈：console 充值页（www 用户中心）应该「**Markets 套餐前置为主打，需要接口开发的再选 Token 充值**」，现在很乱；且 `www 首页 / markets 首页 / markets app` 像 3 个独立站，顶部底部要统一（含用户中心、开通 VIP 入口）。
- 本批改动（9247ff8，53 文件）：
  1. `web/console.html` + `web/js/console.js`：Billing 面板重构——**Markets Pro 卡片置顶**（推荐徽章 + 月付 $24.9 / 年付 $199 直付 PayPal 按钮 + 订阅状态行），API Token 充值下沉为「For API developers」开发者卡片；新增 `doMarketsCheckout` 直连 markets checkout
  2. `web/js/shell.js` + `web/config/locales.js`：全站页头新增 **Upgrade VIP 胶囊**（深链 `app.html#sub`）+ 中英文案
  3. `p/markets/web/index.html` + `p/markets/web/app.html`：**统一深色粘性页眉**（品牌标 + Markets/Pricing/Help/Console 导航 + Upgrade VIP + 登录态）+ **统一四栏页脚**；app 登录态含 Console/Upgrade/Sign out
  4. 全站 43 页共享资源版本统一 `v=20260818g`（shell/locales/console.js）
  5. QA 脚本 5 个（仅本地回归用，不部署）
- 本地已验证：`_qa_vip_guide` 38/38、`_qa_console_vip` 7/7、`_qa_www` 23/23、`_qa_oauth` 全绿、`_qa_cn_index` 全绿、`_qa_opt` 22/22、`_qa_symbol_ui` 全绿。

## 注意事项
- **www 静态由仓库 web/ 直接提供**：git pull + 重启 AI24X-core(8002) 即生效，不复制文件。
- **markets 静态需同步** `C:\sites\markets.ai24x.com`（index.html + app.html，先备份 .bak-20260817-1252）+ 重启 AI24X-markets-api(18012)。
- 本批文件较多（43 html + 3 共享资源 + markets 2 静态页）：脚本里对**本批文件**逐一检查 04 本地脏改动（有则备份到 ops 再 checkout），避免 pull 冲突；其它历史未提交文件一律不动。
- ⚠️ PowerShell 中 `$24.9` 的 `$` 会被当变量：断言一律用**单引号字面量** `'$24.9'`，回执文案不带 `$` 符号。
- 版本号断言用 `-SimpleMatch`（`?` 是正则量词，会误报 FAIL）。

## 副脑04 执行脚本（整段复制运行）
```powershell
# === AI24X 更新部署验收 · 目标 9247ff8 ===
$ErrorActionPreference = "Stop"
$REPO = "C:\ai24x01"
$SRV_CORE = "AI24X-core"
$SRV_MARKETS = "AI24X-markets-api"
$EXP  = "9247ff8"
$SITES = "C:\sites\markets.ai24x.com"
$BATCH = @(
  "web/404.html","web/about.html","web/api.html","web/blog/index.html","web/blog/kimi-api-paypal-guide.html",
  "web/config/locales.js","web/console.html","web/dashboard.html","web/docs.html","web/forgot.html",
  "web/guides/codex.html","web/guides/continue.html","web/guides/curl.html","web/guides/cursor.html","web/guides/dify.html",
  "web/guides/index.html","web/guides/litellm.html","web/guides/lobechat.html","web/guides/n8n.html","web/guides/nodejs.html",
  "web/guides/open-webui.html","web/guides/openai-sdk.html","web/guides/openclaw.html","web/guides/openrouter-migration.html","web/guides/openrouter-vs-ai24x.html",
  "web/guides/powershell.html","web/help.html","web/index.html","web/js/console.js","web/js/shell.js",
  "web/login.html","web/models/deepseek.html","web/models/index.html","web/models/kimi.html","web/models/minimax.html",
  "web/models/qwen.html","web/models/vip-picks.html","web/models/xiaomi-mimo.html","web/models/zhipu-glm.html",
  "web/partner.html","web/paypal.html","web/pricing.html","web/product.html","web/refer.html","web/register.html","web/status.html",
  "p/markets/web/index.html","p/markets/web/app.html"
)

Set-Location $REPO

"--- 1) 本批文件本地脏改动：备份后 checkout ---"
foreach ($f in $BATCH) {
    $dirty = git status --short -- $f
    if ($dirty) {
        $bn = ($f -replace "/", "_")
        Copy-Item "$REPO\$f" "C:\Users\Administrator\ops\dirty-$bn-20260817-1252" -Force
        git checkout -- $f
        if ($LASTEXITCODE -ne 0) { Write-Host "!! checkout 失败: $f" -ForegroundColor Red; exit 1 }
        "dirty backup + checkout: $f"
    } else {
        "clean: $f"
    }
}

"--- 2) 拉取并校验目标提交 ---"
git pull
$HEAD = (git rev-parse --short=12 HEAD).Trim()
"HEAD=$HEAD"
git merge-base --is-ancestor $EXP HEAD
if ($LASTEXITCODE -ne 0) { Write-Host "!! 目标 $EXP 不在 HEAD 历史中" -ForegroundColor Red; exit 1 }
"target_verified=ok"

"--- 3) markets 静态同步（先备份）---"
foreach ($f in @("index.html", "app.html")) {
    if (Test-Path "$SITES\$f") { Copy-Item "$SITES\$f" "$SITES\$f.bak-20260817-1252" -Force }
    Copy-Item "$REPO\p\markets\web\$f" "$SITES\$f" -Force
}

"--- 4) 重启 markets (18012) + core (8002) ---"
Restart-Service $SRV_MARKETS -Force
$mok = $false
for ($i = 0; $i -lt 12; $i++) {
    Start-Sleep -Seconds 5
    try { $mh = Invoke-RestMethod -Uri "http://127.0.0.1:18012/health" -TimeoutSec 10 -UseBasicParsing; $mok = $true; break } catch {}
}
if (-not $mok) { Write-Host "!! markets /health 未就绪" -ForegroundColor Red; exit 1 }
"markets-health=$($mh.status)"

Restart-Service $SRV_CORE -Force
$cok = $false
for ($i = 0; $i -lt 12; $i++) {
    Start-Sleep -Seconds 5
    try { $ch = Invoke-RestMethod -Uri "http://127.0.0.1:8002/health" -TimeoutSec 10 -UseBasicParsing; $cok = $true; break } catch {}
}
if (-not $cok) { Write-Host "!! core /health 未就绪" -ForegroundColor Red; exit 1 }
"core-health=$($ch.status) commit=$($ch.commit)"
git merge-base --is-ancestor $EXP $ch.commit
if ($LASTEXITCODE -ne 0) { Write-Host "!! core commit 未包含目标 $EXP" -ForegroundColor Red; exit 1 }

"--- 5) 本地验收（www 源码 + markets 同步件）---"
$cl = Get-Content "$REPO\web\console.html" -Raw -Encoding UTF8
$cj = Get-Content "$REPO\web\js\console.js" -Raw -Encoding UTF8
$sh = Get-Content "$REPO\web\js\shell.js" -Raw -Encoding UTF8
$lc = Get-Content "$REPO\web\config\locales.js" -Raw -Encoding UTF8
$ci = Get-Content "$REPO\web\index.html" -Raw -Encoding UTF8
"www_console_pro_card=$($cl.Contains('btn-mk-month') -and $cl.Contains('btn-mk-year'))"
"www_console_token_dev=$($cl.Contains('page.console.tokenDev.title'))"
"www_console_checkout_fn=$($cj.Contains('function doMarketsCheckout'))"
"www_shell_upgrade_pill=$($sh.Contains('header-upgrade'))"
"www_locales_vip_key=$($lc.Contains('nav.vipUpgrade'))"
"www_index_upgrade_href=$($ci.Contains('app.html#sub'))"

$mki = Get-Content "$SITES\index.html" -Raw -Encoding UTF8
$mka = Get-Content "$SITES\app.html" -Raw -Encoding UTF8
"mk_index_unified=$($mki.Contains('brand-mark') -and $mki.Contains('upgrade-pill') -and $mki.Contains('footer-grid'))"
"mk_app_unified=$($mka.Contains('mk-top') -and $mka.Contains('mk-console') -and $mka.Contains('mk-footer'))"
"mk_app_price=$($mka.Contains('$24.9') -and $mka.Contains('$199'))"

$oldVer = 0; $newVer = 0
Get-ChildItem "$REPO\web" -Recurse -Filter *.html | ForEach-Object {
    $s = Get-Content $_.FullName -Raw -Encoding UTF8
    if ($s.Contains('locales.js?v=20260818f')) { $oldVer++ }
    if ($s.Contains('locales.js?v=20260818g')) { $newVer++ }
}
"version_old_f=$oldVer"
"version_new_g=$newVer"

if ($cl.Contains('btn-mk-month') -and $cl.Contains('page.console.tokenDev.title') -and $cj.Contains('function doMarketsCheckout') -and $sh.Contains('header-upgrade') -and $lc.Contains('nav.vipUpgrade') -and $mki.Contains('upgrade-pill') -and $mka.Contains('mk-top') -and $mka.Contains('$24.9') -and $oldVer -eq 0 -and $newVer -ge 40) {
    "--- 本地全部 PASS ---"
} else {
    Write-Host "!! 本地验收未全通过" -ForegroundColor Red; exit 1
}

"--- 6) 公网验收 ---"
$pcon = (Invoke-WebRequest -Uri "https://www.ai24x.com/console.html?x=q9247ff8" -TimeoutSec 25 -UseBasicParsing).Content
$pidx = (Invoke-WebRequest -Uri "https://www.ai24x.com/index.html?x=q9247ff8" -TimeoutSec 25 -UseBasicParsing).Content
$pmk = (Invoke-WebRequest -Uri "https://markets.ai24x.com/app.html?x=q9247ff8" -TimeoutSec 25 -UseBasicParsing).Content
$pmki = (Invoke-WebRequest -Uri "https://markets.ai24x.com/?x=q9247ff8" -TimeoutSec 25 -UseBasicParsing).Content
"pub_console_pro=$($pcon.Contains('btn-mk-month') -and $pcon.Contains('page.console.tokenDev.title'))"
"pub_www_v_g=$($pidx.Contains('locales.js?v=20260818g'))"
"pub_mk_app_unified=$($pmk.Contains('mk-top') -and $pmk.Contains('mk-footer') -and $pmk.Contains('$24.9'))"
"pub_mk_index_unified=$($pmki.Contains('upgrade-pill') -and $pmki.Contains('footer-grid'))"

if ($pcon.Contains('btn-mk-month') -and $pidx.Contains('locales.js?v=20260818g') -and $pmk.Contains('mk-top') -and $pmk.Contains('$24.9') -and $pmki.Contains('upgrade-pill')) {
    "--- 公网全部 PASS ---"
    Write-Host "=== 部署成功 OK ===" -ForegroundColor Green
} else {
    Write-Host "!! 公网验收未全通过" -ForegroundColor Red; exit 1
}
```

## 完成后群回执（固定步骤，主通道必做）
- 用 04 应用发指挥部群：
  `python api\feishu_notify.py --config <openclaw.json> --group "【AI24X 部署回执】console 充值页 Markets Pro 置顶 + Token 下沉 + 三站页眉页脚统一 9247ff8 成功：HEAD=9247ff8、core 8002 commit 一致、markets 18012 OK、console Markets Pro 卡+Token 下沉、三站统一页眉页脚（Upgrade VIP 胶囊/导航/页脚）、版本 20260818g 残留 0、公网四页全过。问题项：无" --at`
- 把 ✅ 已完成项 / ⚠️ 问题项 清单一并发群里。

## 回执格式（统一）
- ✅ 已完成项：HEAD / checkout / pull / core+markets health / console Markets Pro 置顶+Token 下沉 / www Upgrade VIP 胶囊 / markets 三站统一 / 版本 20260818g / 公网四页 / 群回执
- ⚠️ 问题项：逐条列出（无则写「无」）

## 回滚预案
- `git revert 9247ff8` + 重启 core(8002) + 重新同步 `C:\sites\markets.ai24x.com` 的 index.html/app.html（从 .bak-20260817-1252 恢复或重新 Copy）+ 重启 markets(18012)
