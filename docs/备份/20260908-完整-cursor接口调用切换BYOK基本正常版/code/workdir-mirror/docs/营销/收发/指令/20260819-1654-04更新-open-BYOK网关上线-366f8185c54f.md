# 【04 更新部署验收】open.ai24x.com BYOK 网关上线（首次后端搭建）· 366f8185c54f

> 司令已提交并双推（gitee + origin 均为 `366f8185c54f`）；老板已放行（2026-08-19 回「04更新」）。
> 执行人：副脑04（本机 C:\ai24x01，服务 AI24X-open-api 新建，nginx 在 C:\nginx）。

## 一、任务背景
- open.ai24x.com 从「Token 聚合转售」改造为「BYOK 智能网关」：用户自带 Key，平台提供智能路由 / 故障转移 / 请求缓存 / 用量成本看板，收服务费。
- 本次是**首次把 open 后端（p/open/api）部署到生产**：生产现状 = 纯静态旧站（nginx root `C:/nginx/html/ai24x`，无后端进程、无反代、无 .env）。
- 后端自托管静态站（FastAPI mount `p/open/web`），nginx 整站反代到 18080 即可。
- 数据库：新建**独立库 `ai24x_open_cn`**，不触碰 `ai24x_core_cn` / `ai24x_a_cn`。
- 合规红线：平台只做技术转发、不囤 token、不赚差价；用户 key AES-256-GCM 加密、明文不落盘；页面明示「网关服务费」。

## 二、部署步骤（04 整段复制运行；每段失败先看日志再继续）

### A. 拉取代码 + 校验
```powershell
$ErrorActionPreference = "Stop"
$REPO = "C:\ai24x01"
$EXP  = "366f8185c54f"
Set-Location $REPO
$dirty = git status --porcelain -- p/open
if ($dirty) { Write-Host "p/open 有本地改动，先备份再 stash：" -ForegroundColor Yellow; $dirty; git stash push -u -m "open-local-before-366f8185" }
git pull
$HEAD = (git rev-parse --short=12 HEAD).Trim()
"HEAD=$HEAD"
git merge-base --is-ancestor $EXP HEAD
if ($LASTEXITCODE -ne 0) { Write-Host "!! 目标 commit 不在历史" -ForegroundColor Red; exit 1 }
git diff --quiet $EXP HEAD -- p/open
if ($LASTEXITCODE -ne 0) { Write-Host "!! p/open 与目标不一致" -ForegroundColor Red; exit 1 }
Write-Host "A 代码校验通过 ✅" -ForegroundColor Green
```

### B. 建库 ai24x_open_cn（全新空库，不影响现有库）
```powershell
$envFile = "C:\ai24x01\api\.env"
$coreUrl = ""
Get-Content $envFile | ForEach-Object { if ($_ -match "^DATABASE_URL=(.+)$") { $coreUrl = $Matches[1].Trim() } }
$psql = "C:\AI24X\postgresql\pgsql\bin\psql.exe"
$adminUrl = ($coreUrl -replace "/ai24x_core_cn$","/postgres")
& $psql $adminUrl -w -t -A -c "CREATE DATABASE ai24x_open_cn" 2>&1
& $psql $adminUrl -w -t -A -c "GRANT ALL PRIVILEGES ON DATABASE ai24x_open_cn TO ai24x_a" 2>&1
```
> 若 `ai24x_a` 无 CREATEDB 权限：用本机 postgres 超级用户执行上面的建库 + 授权（凭据本机自备，勿回传日志）。

### C. 创建 open 生产 .env（密钥不回显、不入 git）
```powershell
$openEnv = "C:\ai24x01\p\open\api\.env"
$core = @{}
Get-Content $envFile | ForEach-Object { if ($_ -match "^([A-Za-z_][A-Za-z0-9_]*)=(.*)$") { $core[$Matches[1]] = $Matches[2] } }
$copyKeys = @(
  "DEEPSEEK_API_KEY","DEEPSEEK_BASE_URL","DEEPSEEK_MODEL",
  "MIMO_API_KEY","MIMO_API_KEY_FREE","MIMO_BASE_URL",
  "OPENROUTER_API_KEY","OPENROUTER_BASE_URL","OPENROUTER_SITE_URL","OPENROUTER_APP_NAME","OPENROUTER_API_KEY_FREE",
  "SILICONFLOW_API_KEY","SILICONFLOW_API_KEY_FREE","SILICONFLOW_COM_API_KEY","SILICONFLOW_MODEL",
  "REQUESTY_API_KEY","TOKENLAB_API_KEY",
  "TOKEN_LLM_UPSTREAM","TOKEN_LLM_TIMEOUT_S","TOKEN_LLM_L1_UPSTREAM","TOKEN_LLM_L2_UPSTREAM",
  "SMTP_HOST","SMTP_PORT","SMTP_USER","SMTP_PASSWORD","SMTP_FROM","SMTP_USE_TLS","SMTP_USE_SSL",
  "SMTP_BACKUP_HOST","SMTP_BACKUP_PORT","SMTP_BACKUP_USER","SMTP_BACKUP_PASSWORD","SMTP_BACKUP_FROM","SMTP_BACKUP_USE_TLS","SMTP_BACKUP_USE_SSL",
  "PAYPAL_MODE","PAYPAL_CLIENT_ID","PAYPAL_CLIENT_SECRET","PAYPAL_WEBHOOK_ID",
  "TOKEN_PAY_ENABLED","TOKEN_PAY_MOCK_ENABLED",
  "WECHAT_MCH_ID","WECHAT_APP_ID","WECHAT_MCH_SERIAL_NO","WECHAT_MCH_PRIVATE_KEY_PATH","WECHAT_API_V3_KEY","WECHAT_NOTIFY_SKIP_VERIFY","WECHAT_PAY_HOST",
  "ALIPAY_APP_ID","ALIPAY_GATEWAY","ALIPAY_MERCHANT_PRIVATE_KEY_PEM","ALIPAY_PUBLIC_KEY",
  "CRYPTO_ENABLED","CRYPTO_TRC20_ADDRESS","CRYPTO_MIN_CONFIRM",
  "SMS_106_ENABLED","SMS_106_ACCOUNT","SMS_106_PASSWORD","SMS_106_ENDPOINT","SMS_106_SIGN_NAME","SMS_106_TEMPLATE","SMS_INTERNAL_KEY"
)
$secret = (& "C:\ai24x01\api\venv\Scripts\python.exe" -c "import secrets; print(secrets.token_urlsafe(48))").Trim()
$lines = @(
  "# open.ai24x.com BYOK gateway production config (2026-08-19; DO NOT COMMIT)",
  "APP_ENV=prod",
  "DATABASE_URL=" + ($core["DATABASE_URL"] -replace "/ai24x_core_cn$","/ai24x_open_cn"),
  "SECRET_KEY=" + $secret,
  "AUTH_JWT_EXPIRE_DAYS=7",
  "STRICT_AUTH=true",
  "DISABLE_DOCS_IN_PROD=true",
  "SKIP_DB_INIT=false",
  "CORS_ORIGINS=https://open.ai24x.com,https://www.ai24x.com",
  "LOG_LEVEL=INFO",
  "BYOK_ENABLED=true",
  "BYOK_FALLBACK_TO_PLATFORM=false",
  "BYOK_CACHE_TTL_S=300",
  "BYOK_FREE_MONTHLY_REQUESTS=1000",
  "BYOK_ENFORCE_FREE_CAP=false",
  "BYOK_FEE_MODE=service_fee",
  "TOKEN_PAYPAL_RETURN_URL=https://open.ai24x.com/console.html?pay=paypal",
  "TOKEN_PAYPAL_CANCEL_URL=https://open.ai24x.com/console.html#billing",
  "TOKEN_WECHAT_NOTIFY_URL=https://open.ai24x.com/v1/billing/wechat/notify",
  "TOKEN_ALIPAY_NOTIFY_URL=https://open.ai24x.com/v1/billing/alipay/notify",
  "TOKEN_ALIPAY_RETURN_URL=https://open.ai24x.com/console.html#billing"
)
foreach ($k in $copyKeys) {
  if ($core.ContainsKey($k) -and $core[$k]) { $lines += "$k=" + $core[$k] }
}
[System.IO.File]::WriteAllLines($openEnv, $lines, (New-Object System.Text.UTF8Encoding($false)))
Write-Host "C .env 已生成：$openEnv（$($lines.Count) 键，值未回显）✅" -ForegroundColor Green
```

### D. venv + 依赖安装
```powershell
$py = "C:\Program Files\Python311\python.exe"
$openApi = "C:\ai24x01\p\open\api"
if (-not (Test-Path "$openApi\venv\Scripts\python.exe")) { & $py -m venv "$openApi\venv" }
& "$openApi\venv\Scripts\python.exe" -m pip install --disable-pip-version-check -q -r "$openApi\requirements.txt"
& "$openApi\venv\Scripts\python.exe" -c "import fastapi, sqlalchemy, pydantic_settings, httpx, cryptography, jose, passlib; print('D deps OK')"
```

### E. 注册服务 AI24X-open-api（nssm，端口 18080）
```powershell
$nssm = "C:\nssm\nssm.exe"
$srv = "AI24X-open-api"
if (-not (Get-Service -Name $srv -ErrorAction SilentlyContinue)) {
  & $nssm install $srv "$openApi\venv\Scripts\python.exe" "-m uvicorn main:app --host 127.0.0.1 --port 18080"
}
& $nssm set $srv AppDirectory $openApi
& $nssm set $srv AppEnvironmentExtra "PYTHONUNBUFFERED=1"
& $nssm set $srv AppStdout "C:\ai24x01\logs\open-api.out.log"
& $nssm set $srv AppStderr "C:\ai24x01\logs\open-api.err.log"
& $nssm set $srv Start SERVICE_AUTO_START
& $nssm start $srv
```

### F. 本机 /health 校验（含 commit 探针）
```powershell
$ok = $false
for ($i = 0; $i -lt 24; $i++) {
  Start-Sleep -Seconds 5
  try { $h = Invoke-RestMethod -Uri "http://127.0.0.1:18080/health" -TimeoutSec 10 -UseBasicParsing; $ok = $true; break } catch {}
}
if (-not $ok) { Write-Host "!! open /health 未就绪，看 C:\ai24x01\logs\open-api.err.log" -ForegroundColor Red; exit 1 }
"open health status=$($h.status) commit=$($h.commit)"
if ($h.status -ne "healthy" -or "$($h.commit)" -notlike "$EXP*") { Write-Host "!! open 服务未生效" -ForegroundColor Red; exit 1 }
Write-Host "F open 服务健康 ✅" -ForegroundColor Green
```

### G. nginx 整站反代（新配置已备好，直接替换 + 校验）
> 新配置文件已传至 `C:\ai24x01\scripts\nginx.conf.open-byok.new`（GBK 编码，仅改 open.ai24x.com 443 块：root/index → 注释，location / → proxy 18080，保留 acme/HSTS/日志）。
```powershell
$conf = "C:\nginx\conf\nginx.conf"
$bak = "$conf.bak-20260819-1654"
Copy-Item $conf $bak -Force
Copy-Item "C:\ai24x01\scripts\nginx.conf.open-byok.new" $conf -Force
& "C:\nginx\nginx.exe" -t
if ($LASTEXITCODE -ne 0) {
  Write-Host "!! nginx -t 失败，回滚备份" -ForegroundColor Red
  Copy-Item $bak $conf -Force
  exit 1
}
& "C:\nginx\nginx.exe" -s reload
Start-Sleep -Seconds 3
Write-Host "G nginx 已切换（备份：$bak）✅" -ForegroundColor Green
```

## 三、验收清单（全部 PASS 才发群回执）
1. 公网服务：`Invoke-WebRequest -Uri "https://open.ai24x.com/health" -UseBasicParsing` → `status=healthy` 且 `commit` 以 `366f8185c54f` 开头。
2. 公网定价页：`https://open.ai24x.com/pricing.html` →
   - BYOK 区上方 **3 张卡并排**（免费试用 $0 / BYOK Pro 月付 / BYOK Pro 年付），无「平台托管」字样的套餐卡；
   - 平台托管只出现在下方完整对照表（`#tokenTable`）；
   - `locales.js?v=20260818l` 命中，旧版本号残留 0（`[regex]::Matches($Content,'locales\.js\?v=(?!20260818l)[0-9a-z]+')` 计数=0）。
3. 数据库：`SELECT count(*) FROM byok_subscriptions`（连 `ai24x_open_cn`）可查（0 行正常，表已由 init_db 自动创建）。
4. 功能冒烟（用 04 本机浏览器或 curl）：
   - 注册一个新用户（可用临时邮箱）→ 控制台 → BYOK Key 管理添加一把自有 key（OpenAI/DeepSeek 等，`/v1/byok/keys` 返回 key_prefix 无明文）；
   - 调 `/v1/chat/completions`（OpenAI SDK 兼容）走该 key 返回结果；
   - 控制台 Billing：选 BYOK Pro → 弹出支付方式（PayPal / 微信 / 支付宝 / USDT / 模拟）→ 支付流程可发起（PayPal 按 core 的 PAYPAL_MODE 配置）。
5. 主站无回归：`https://www.ai24x.com` 正常（nginx 只改 open 块，其他 server 块未动）。

## 四、回滚预案（任一失败即回滚并回传司令）
```powershell
Stop-Service AI24X-open-api
Copy-Item "C:\nginx\conf\nginx.conf.bak-20260819-1654" "C:\nginx\conf\nginx.conf" -Force
& "C:\nginx\nginx.exe" -s reload
Set-Location C:\ai24x01
git reset --hard 70896cf  # 上一个生产 commit（如需代码回滚）
```
`ai24x_open_cn` 为全新空库，回滚可直接 DROP，不影响任何现有数据。

## 五、群回执（统一格式）
- ✅ 已完成项：HEAD / open /health(commit) / pricing 三卡 / 版本号 / byok_subscriptions 表 / 添加 key 冒烟 / nginx 切换。
- ⚠️ 问题项：逐条列出（无则写「无」）。
- 附公网证据：`https://open.ai24x.com/health` 的 commit + `pricing.html` 版本号。
