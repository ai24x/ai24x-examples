# 【03 更新】精简盘中重扫门控文案 + bjscreener.js v80

> 通道：司令直连 03（deploy03.ps1）｜ 目标提交 **7c792e0**
> 03 主机：123.207.199.238 · 仓库 C:\ai24x01 · 服务 AI24X-a1-api（NSSM，8001）· 无 D 盘（备份落 C:\backup\ai24x_a\）
> a1 /health 无 commit 字段：验收用「文件标记 + 进程新建时间 + 公网页面内容」

## 背景
老板要求精简「重新扫描」门控备注文案。原「盘中数据未定型：15:10 收盘后可重新扫描（避免未定型 K 线污染）」→ 精简为「**盘中未定型：15:10 后可重新扫描**」。
- 桌面：`p\a1\web\js\bjscreener.js` scanGateState 备注文案精简，版本号 v79→v80（`gd.html` 引用已同步）。
- 手机版：`p\a1\web\m\gd.html` 内联 scanGateState 备注文案同步精简（无外部资源、无版本号）。
- 本地已实测（10:21 盘中）：桌面+手机备注均显示新文案、按钮禁用/gated、无 JS 报错。

## 执行步骤（PowerShell，整段复制运行）
```powershell
$ErrorActionPreference = "Stop"
$REPO = "C:\ai24x01"
$SRV  = "AI24X-a1-api"
$EXP  = "7c792e0"

Set-Location $REPO

"--- 0) 备份本地脏改动再还原（防止 pull 冲突）---"
$FILES = @("p\a1\web\gd.html","p\a1\web\m\gd.html","p\a1\web\js\bjscreener.js")
$BK = "C:\backup\ai24x_a\20260818_pre_7c792e0"
New-Item -ItemType Directory -Force -Path $BK | Out-Null
foreach ($x in $FILES) {
  if (Test-Path "$REPO\$x") {
    $chg = git -C $REPO status --porcelain -- $x
    if ($chg) { Copy-Item "$REPO\$x" "$BK\$(Split-Path $x -Leaf).bak" -Force; git -C $REPO checkout -- $x }
  }
}
"备份目录: $BK"

"--- 1) 拉码 + HEAD 校验 ---"
git pull
$HEAD = (git rev-parse --short=12 HEAD).Trim()
"HEAD=$HEAD"
git merge-base --is-ancestor $EXP HEAD
if ($LASTEXITCODE -ne 0) { Write-Host "!! HEAD 不含目标提交 $EXP" -ForegroundColor Red; exit 1 }

"--- 2) 文件标记检查 ---"
$gd = Get-Content "$REPO\p\a1\web\gd.html" -Raw -Encoding UTF8
$js = Get-Content "$REPO\p\a1\web\js\bjscreener.js" -Raw -Encoding UTF8
$m  = Get-Content "$REPO\p\a1\web\m\gd.html" -Raw -Encoding UTF8
"gd_v80=$($gd.Contains('bjscreener.js?v=80'))"
"js_new=$($js.Contains('盘中未定型：15:10 后可重新扫描'))  js_old=$($js.Contains('盘中数据未定型：15:10 收盘后可重新扫描'))"
"m_new=$($m.Contains('盘中未定型：15:10 后可重新扫描'))  m_old=$($m.Contains('盘中数据未定型：15:10 收盘后可重新扫描'))"
if (-not ($gd.Contains('bjscreener.js?v=80') -and $js.Contains('盘中未定型：15:10 后可重新扫描') -and $m.Contains('盘中未定型：15:10 后可重新扫描'))) {
  Write-Host "!! 文件标记缺失" -ForegroundColor Red; exit 1
}

"--- 3) 重启 AI24X-a1-api (8001) ---"
Restart-Service $SRV -Force
$ok = $false
for ($i = 0; $i -lt 15; $i++) {
  Start-Sleep -Seconds 5
  try { $h = Invoke-RestMethod -Uri "http://127.0.0.1:8001/health" -TimeoutSec 10 -UseBasicParsing; $ok = $true; break } catch {}
}
if (-not $ok) { Write-Host "!! /health 未就绪" -ForegroundColor Red; exit 1 }
"health=$($h.status)"

"--- 4) 公网页面内容标记 ---"
$pubGd = (Invoke-WebRequest -Uri "https://a.ai24x.com/gd.html" -UseBasicParsing -TimeoutSec 20).Content
$pubJs = (Invoke-WebRequest -Uri "https://a.ai24x.com/js/bjscreener.js?v=80" -UseBasicParsing -TimeoutSec 20).Content
$pubM  = (Invoke-WebRequest -Uri "https://a.ai24x.com/m/gd.html" -UseBasicParsing -TimeoutSec 20).Content
"pub_gd_v80=$($pubGd.Contains('bjscreener.js?v=80'))"
"pub_js_new=$($pubJs.Contains('盘中未定型：15:10 后可重新扫描'))"
"pub_m_new=$($pubM.Contains('盘中未定型：15:10 后可重新扫描'))"
if (-not ($pubGd.Contains('bjscreener.js?v=80') -and $pubJs.Contains('盘中未定型：15:10 后可重新扫描') -and $pubM.Contains('盘中未定型：15:10 后可重新扫描'))) {
  Write-Host "!! 公网内容不对" -ForegroundColor Red; exit 1
}

Write-Host "=== 部署成功 OK ===" -ForegroundColor Green
```

## 验收清单（回执时逐条列出）
- HEAD=7c792e0（merge-base 校验通过）／文件标记（gd v80 + js/m 新文案、旧文案无残留）
- health 200（静态变更，重启后进程正常即可）
- 公网 gd.html 含 v80；js/bjscreener.js?v=80 与 m/gd.html 均为新文案「盘中未定型：15:10 后可重新扫描」
- 浏览器端（Ctrl+F5，用户侧确认）：桌面/手机「重新扫描」盘中备注已精简；15:10 后自动恢复可点
- ⚠️ 问题项：无则写「无」

## 回执格式（03 自动发指挥部群）
- ✅ 已完成项：HEAD／文件标记／重启+health／公网内容标记
- ⚠️ 问题项：逐条（无则写「无」）
- 凭据纪律：不回执任何 token 明文；备份目录仅报路径。

## 回滚预案
- `git revert 7c792e0` + 重启 AI24X-a1-api；或恢复 `C:\backup\ai24x_a\20260818_pre_7c792e0\*.bak` + 重启。
