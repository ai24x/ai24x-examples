# 指令 · 指挥官（主脑）· HF 获客突击：重启 + 并行推进（D1 拆线）

> 来源：司令（Codex）｜ 时间：2026-09-02 01:31 ｜ 通道：Codex→主脑直连 ｜ 优先级：P0

## 背景

- 08-29 14:35 已派发「HF 获客突击-分工与D3计划」（D0 完成），D1 起卡在「雷总确认 HF 邮箱/org」，连续 4 天未动。
- 雷总 09-02 指示：系统已调整完毕（全脑 DeepSeek 谷时通道已恢复），**尽快推进，D3 必须看得见**。
- 本指令 = 重启 + 优化版，替代原 D1 串行等待逻辑。

## 关键调整（司令审阅后新增，红线不变）

1. **D1 拆两条并行线**：
   - 文案线（01/02）**立即开工，不等账号**；
   - 建号线（04）等雷总一句邮箱确认后即刻执行，但 Space 静态骨架**先做**（不需要账号）。
2. **UTM 定死**（全链路追踪已确认可用，无需开发）：
   - 落地 URL：`https://open.ai24x.com/?utm_source=huggingface&utm_medium=org&utm_campaign=hf-org-launch`
   - 各入口按需加 `utm_content`（README / Space 描述 / 按钮区分）
   - 追踪链路已验证：前端 `js/utm.js` 采集 → 注册携带 → 后端 `auth_users.utm_*` → 后台渠道统计
3. **Space 用 HF 静态 Space**（免费不睡眠、无需容器/GPU），Cost Comparator 静态诚实表正合适。
4. **D3 验收加硬指标**（主脑验收必查）：
   - Org `ai24x` 公开可见
   - Space 公开可访问（200），双 CTA 可点
   - 落地 URL 带 UTM，注册后后台能看到 `huggingface` 渠道记录
5. 节奏压缩：D0+D1 今天（09-02）并行启动；D2-D3 上线验收；D7 复盘。

## 分工（沿用原计划 + 补明确交付物）

- ♟️ 主脑：拆令、催办、验收；把 HF 挂 18:40 看板 P0；每日 06:25 目标派发带 HF 进度
- 🧠 01：英文 README（Org 首页）+ Space 文案 + 话术红线卡 → 落盘 `C:\Users\Administrator\ops\worker\seo-content\hf\`，**09-03 18:00 前交**
- 📋 02：排进周计划 + 与 SEO Phase2 错峰 + D7 复盘模板 → 落盘 `C:\Users\Administrator\ops\orchestrator-output\`，**09-02 24:00 前交**
- 🌏 04：① 先做 Space 静态骨架（Cost Comparator 静态表 + 双 CTA + UTM 链接，本地可预览）② 等雷总邮箱确认后建 Org `ai24x` + 部署 Space ③ 台账 `ops\账号矩阵\hf-*.md`；不代付费、不写明文密钥进 git
- 👤 雷总：拍板 HF 邮箱/org（司令已直接询问）

## 催办节奏（主脑接管）

- 09-02 06:25 日目标派发：HF 文案线/骨架线
- 09-02 18:40 看板：HF 挂 P0，状态 = 文案线进行中 / 建号线待账号
- 09-03 06:33 隔夜巡检 + 07:30 晨报：HF 进度必带
- 09-03 18:00 验收 01 文案 → 04 建号部署 → 18:40 看板 D2/D3 状态

## 红线（不变）

- ❌ 硬刚「替代 HF Inference」、虚构省钱%、股票词、假镜像权重
- ✅ Gateway + BYOK/托管 + 中国模生产接入（P1 挂专题）
- 04 不代付费；凭据不经群明文、不进 git/ops

## 回执

完成后飞书汇报本条结果：✅ 已拆令派发（01/02/04 各收到）+ HF 已挂看板 P0 / ⚠️ 问题项（无则写「无」）。
