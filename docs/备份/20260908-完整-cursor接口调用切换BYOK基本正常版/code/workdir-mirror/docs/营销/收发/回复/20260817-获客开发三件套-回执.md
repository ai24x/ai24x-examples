# 回执 · 获客开发三件套（P0 措辞铁律 + SEO 生成器 + 美股简报生成器）

日期：2026-08-17 · 行情官 Codex 会话 · 未提交 git（按任务约定，等司令验收后提交+双推+部署）

## ✅ 已完成项

### P0 · AI 点评措辞铁律
- 链路定位：`p/markets/api/server/app/ai_brief.py`，入口 `main.py` `/api/ai/brief`（L266）→ `generate_brief`。
- 铁律落点（双重保险，已改）：① prompt `_SYSTEM_PROMPT` 硬约束补 `avoid`（现有 buy/sell/hold/accumulate/recommend/…全禁用，仅允许纯描述性技术体检）；② 输出黑名单 `_FORBIDDEN_RE` 补 `avoid`（命中 >0 自动降级为确定性合规模板 `_fallback_brief`）。
- 抽检脚本：`p/markets/scripts/audit_briefs.py`（与生产同源正则），已跑出报告 `p/markets/ops/brief-compliance-20260817.md`：扫描缓存 4 条（AAPL×2/MSFT/NVDA），**违规 0**。注：当前缓存仅 4 条（非 50-100），脚本支持 `--limit 100`，随积累可重跑全量。

### P1 · 程序化 SEO 落地页生成器（首批 20 页）
- 生成器：`p/markets/scripts/seo_gen.py`（数据=本地 markets API 18012，失败回退 data/cache；SVG 图表纯 Python 渲染 K线+MA20/MA60；AI 摘要为确定性技术描述，页内过禁词断言）。
- 产物：`p/markets/web/seo/` 共 36 文件（20 页 + index.html + 15 张 SVG）。
  - 股票 10 页：nvda/aapl/tsla/msft/amzn/meta/googl/amd/pltr/nflx（asof 2026-08-14）。
  - 指数对比 5 页：qqq-vs-spy / voo-vs-spy / dia-vs-spy / iwm-vs-spy / qqq-vs-voo。
  - 教程 5 页：rsi / macd / moving-averages / support-resistance / volume-price。
  - 每页含唯一 title/description、SVG 图、技术快照、参考位表格、FAQ JSON-LD、CTA（app.html?symbol=）、互链。
- 样例 URL：`p/markets/web/seo/nvda.html`、`p/markets/web/seo/qqq-vs-spy.html`、`p/markets/web/seo/rsi-indicator-guide.html`。禁词扫描 0 命中。

### P1 · 每日美股简报生成器
- 生成器：`p/markets/scripts/brief_gen.py`（可 cron，交易日 ~05:00 UTC 后跑；单标的单日一次取数，API→缓存回退，遵守数据源纪律；无模型调用，纯描述性）。
- 样例产物：`p/markets/data/brief/20260817/brief.md + brief.html`（3 指数 DJI/INX/IXIC + 10 热门股，asof 2026-08-14，13 行，附免责）。禁词 0 命中。

## ⚠️ 问题项
- 抽检样本仅 4 条缓存（累计生成量不足 50-100），报告口径已注明，建议跑一周后重扫。
- SEO 图表为程序化 SVG（非浏览器截图），如需 PNG 截图可后续加 Playwright 出图流程。
- seo/ 尚未接入 web 根 sitemap.xml / robots.txt（属共享文件，未动），部署时可评估并入。
- 未提交 git、未部署（按任务约定交司令审）。

## 下一步建议
- 司令验收后提交（`p/markets/scripts/{seo_gen,brief_gen,audit_briefs}.py` + `api/server/app/ai_brief.py` + `web/seo/` + `ops/brief-compliance-*.md`），部署时同步 `C:\sites\markets.ai24x.com` 静态 + 重启 18012。
