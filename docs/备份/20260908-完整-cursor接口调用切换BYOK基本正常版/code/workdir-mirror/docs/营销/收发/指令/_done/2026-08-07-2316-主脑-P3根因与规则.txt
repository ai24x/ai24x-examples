P3 SIGKILL 根因已定位并修复（cx_bridge.ps1 已升级 v2），请主脑执行以下规则：

【根因】你（主脑）的 exec 工具约 120 秒超时会杀掉整个进程树——23:02 两次"SIGKILL"都是这个原因：任务本身是长链条（Codex→你→SSH四台副脑→汇总），Codex 同步等待必然超 120s 被杀。第三次 23:10 成功是因为活已被你干完，Codex 只需 15 秒总结。

【新规则】
1. cx_bridge 只用于 ≤100 秒的短任务（确认、查状态、生成文档、落盘）。已升级为"派发+确认"模式：需要你/副脑多步执行的任务，Codex 只派发给你并立即确认，不同步等待。
2. 长任务（SSH 派发多台副脑、多步汇总、预计 >100s）：不要用 cx_bridge 同步等——由你（主脑）直接执行并飞书汇报雷总，Codex 的桥接只做确认。
3. 已派发的任务不要重复驱动（23:02 你重试 3 次，前两次纯浪费）。若某任务桥接驱动 2 次都失败：停止重试，由你直接执行/拆解，并在飞书说明原因。
4. 你驱动 cx_bridge.ps1 的 exec 调用总时长上限 115s；回复文件以 [CX-Bridge 异常] 开头时转达雷总但不重试超 1 次。
5. 本规则并入你的长期记忆（TOOLS.md/AGENTS.md），后续不再犯。
收到请回执一行确认。