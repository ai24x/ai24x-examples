# 【04 更新部署验收】全站品牌定位 + 免责声明 + 支付中台统一 · be64a991dec2

> 司令已提交并双推（gitee + origin 均为 `be64a991dec2`）；老板已放行并回复「04更新」。
> 执行人：副脑04（本机 C:\ai24x01）。本批同时触及 **www/core(8002)**、**markets(18012)**、**open(18080)** 三个服务，按步骤全部执行；验收全 PASS 后自动发指挥部群回执（✅/⚠️ 清单），有任一问题先回传司令不发群。

## 一、背景
按《AI24X 全站统一品牌定位 + 支付合规文案优化》任务书（v1/v2）+ 支付中台改造，本批内容：
1. **品牌伞统一**：AI24X — AI-assisted tools for markets & developers；www index/product hero 加品牌伞、open 站标题改「AI24X Open — AI-assisted developer gateway」、markets pricing/app 清 signals 雷词（换 AI commentary / AI chart markers）、www help signals→crossovers。
2. **免责声明补齐**（合规护城河）：www 共享 footer + pricing note 中英文免责声明（仅供教育用途，不构成投资建议；行情延迟≥15 分钟）；markets index signal markers→trend markers。
3. **UI 亮化**：console「Upgrade to Pro」统一深链 `https://markets.ai24x.com/app.html#sub` + Overview Markets 卡「Open product」点亮；markets 首页 Sign in / Sign up / Google 按钮点亮；screener 扫描进度条 + 教育用途声明。
4. **支付中台**：core 新增 `/v1/billing/products` 统一产品目录（token + markets 两项目）；微信/支付宝/PayPal/Creem 下单带 `product` 参数；付款成功按产品履约（token 本地入账 / markets 签名回调子服务 `/api/subscribe/fulfill` 激活订阅）；markets SQLite 幂等迁移（out_trade_no/channel）+ 免费档 10；markets PWA（icons/manifest/sw.js）；privacy/seo 数据源合规文案（aggregated market data）。
5. **版本号**：locales.js `v=20260821b`（www 39 页 + open 39 页）、shell.js `v=20260821b`（www 39 页）、console.js `v=20260821c`（console.html 1 页），旧版本残留必须为 0。

## 二、副脑04 执行脚本（整段复制运行）
```powershell
# === AI24X 更新部署验收 · 目标 be64a991dec2（品牌/免责声明/支付中台）===
$ErrorActionPreference = "Stop"
$REPO = "C:\ai24x01"
$SRV_CORE = "AI24X-core"
$SRV_MK   = "AI24X-markets-api"
$SRV_OPEN = "AI24X-open-api"
$EXP  = "be64a991dec2"
$SITES = "C:\sites\markets.ai24x.com"
$TAG = "20260821-brand"
$nssm = "C:\nssm\nssm.exe"

Set-Location $REPO

"--- 0) 本批路径如有本地改动：备份后 checkout（只动本批路径，其它未提交文件不动）---"
$paths = @("web", "api", "p/markets", "p/open/web")
$dirty = git status --porcelain -- $paths
if ($dirty) {
    "以下路径有本地改动，先备份："
    $dirty
    $bakDir = "C:\Users\Administrator\ops\dirty-$TAG"
    New-Item -ItemType Directory -Force -Path $bakDir | Out-Null
    $dirty | Where-Object { $_ -notmatch "^\?\?" } | ForEach-Object {
        $rel = ($_ -replace "^...", "").Trim()
        if ($rel) {
            $src = Join-Path $REPO $rel
            if (Test-Path $src) { Copy-Item $src (Join-Path $bakDir ($rel -replace "[\\/]", "_")) -Force }
        }
    }
    git checkout -- $paths
    if ($LASTEXITCODE -ne 0) { Write-Host "!! checkout 失败" -ForegroundColor Red; exit 1 }
    "dirty 已备份到 $bakDir 并 checkout"
} else { "clean: 本批路径无本地改动" }

"--- 1) 拉取并校验目标提交 ---"
git pull
$HEAD = (git rev-parse --short=12 HEAD).Trim()
"HEAD=$HEAD"
git merge-base --is-ancestor $EXP HEAD
if ($LASTEXITCODE -ne 0) { Write-Host "!! 目标 $EXP 不在 HEAD 历史中" -ForegroundColor Red; exit 1 }
git diff --quiet $EXP HEAD -- api web p/markets p/open/web
if ($LASTEXITCODE -ne 0) { Write-Host "!! 关键路径在 $EXP 之后仍有改动，停止" -ForegroundColor Red; exit 1 }
"target_verified=ok"

"--- 2) 配置 MARKETS_FULFILL_SECRET（core 与 markets 同一密钥）---"
$coreEnv = "C:\ai24x01\api\.env"
$content = Get-Content $coreEnv -Raw
if ($content -notmatch "MARKETS_FULFILL_SECRET") {
    $secret = (& "C:\ai24x01\api\venv\Scripts\python.exe" -c "import secrets; print(secrets.token_hex(16))").Trim()
    Add-Content -Path $coreEnv -Value "MARKETS_FULFILL_SECRET=$secret" -Encoding UTF8
    "core .env 已追加 MARKETS_FULFILL_SECRET（值不回显）"
} else {
    $secret = [regex]::Match($content, "MARKETS_FULFILL_SECRET=(\S+)").Groups[1].Value
    "core .env 已有 MARKETS_FULFILL_SECRET（复用）"
}
if (-not $secret) { Write-Host "!! 无法取得 MARKETS_FULFILL_SECRET" -ForegroundColor Red; exit 1 }

$mkEnvNow = (& $nssm get $SRV_MK AppEnvironmentExtra 2>$null) -join "`n"
if ([string]::IsNullOrWhiteSpace($mkEnvNow)) {
    & $nssm set $SRV_MK AppEnvironmentExtra "MARKETS_FULFILL_SECRET=$secret"
    "nssm $SRV_MK AppEnvironmentExtra 已设置（此前为空）"
} else {
    "nssm $SRV_MK AppEnvironmentExtra 现有非空（不覆盖，打印现状）："
    $mkEnvNow
    [Environment]::SetEnvironmentVariable("MARKETS_FULFILL_SECRET", $secret, "Machine")
    "已改设机器级环境变量 MARKETS_FULFILL_SECRET（重启 markets 生效）"
}

"--- 3) 重启 www/core (8002) ---"
Restart-Service $SRV_CORE -Force
$okC = $false
for ($i = 0; $i -lt 15; $i++) {
    Start-Sleep -Seconds 5
    try { $hC = Invoke-RestMethod -Uri "http://127.0.0.1:8002/health" -TimeoutSec 10 -UseBasicParsing; $okC = $true; break } catch {}
}
if (-not $okC) { Write-Host "!! core /health 未就绪" -ForegroundColor Red; exit 1 }
"core-health=$($hC.status) commit=$($hC.commit)"
if ($hC.status -ne "healthy" -or "$($hC.commit)" -notlike "$EXP*") { Write-Host "!! core 未生效" -ForegroundColor Red; exit 1 }

"--- 4) markets 静态同步到 nginx 服务目录（先备份）---"
foreach ($f in @("app.html", "index.html", "pricing.html", "screener.html", "privacy.html")) {
    if (Test-Path "$SITES\$f") { Copy-Item "$SITES\$f" "$SITES\$f.bak-$TAG" -Force }
    Copy-Item "$REPO\p\markets\web\$f" "$SITES\$f" -Force
}
foreach ($f in @("aapl","amd","amzn","googl","meta","msft","nflx","nvda","pltr","tsla")) {
    Copy-Item "$REPO\p\markets\web\seo\$f.html" "$SITES\seo\$f.html" -Force
}
foreach ($f in @("manifest.webmanifest", "sw.js")) {
    Copy-Item "$REPO\p\markets\web\$f" "$SITES\$f" -Force
}
New-Item -ItemType Directory -Force -Path "$SITES\icons" | Out-Null
foreach ($f in @("apple-touch-icon.png", "icon-192.png", "icon-512.png", "icon.svg")) {
    Copy-Item "$REPO\p\markets\web\icons\$f" "$SITES\icons\$f" -Force
}
$sa = Get-Content "$SITES\app.html" -Raw -Encoding UTF8
$si = Get-Content "$SITES\index.html" -Raw -Encoding UTF8
$ss = Get-Content "$SITES\screener.html" -Raw -Encoding UTF8
"sites_app_payhub=$($sa.Contains('payhub')) sites_app_free10=$($sa.Contains('10 free')) sites_index_signin=$($si.Contains('Sign in')) sites_screener_progress=$($ss.Contains('sr-progress')) sites_manifest=$((Test-Path "$SITES\manifest.webmanifest"))"
if (-not ($sa.Contains('payhub') -and $ss.Contains('sr-progress') -and (Test-Path "$SITES\manifest.webmanifest") -and (Test-Path "$SITES\sw.js"))) { Write-Host "!! sites 静态文件缺失" -ForegroundColor Red; exit 1 }

"--- 5) 重启 markets (18012) ---"
Restart-Service $SRV_MK -Force
$okM = $false
for ($i = 0; $i -lt 15; $i++) {
    Start-Sleep -Seconds 5
    try { $hM = Invoke-RestMethod -Uri "http://127.0.0.1:18012/health" -TimeoutSec 10 -UseBasicParsing; $okM = $true; break } catch {}
}
if (-not $okM) { Write-Host "!! markets /health 未就绪" -ForegroundColor Red; exit 1 }
"markets-health=$($hM.status)"
if ($hM.status -ne "ok") { Write-Host "!! markets 状态异常" -ForegroundColor Red; exit 1 }

"--- 6) 重启 open (18080，自托管静态含 p/open/web) ---"
Restart-Service $SRV_OPEN -Force
$okO = $false
for ($i = 0; $i -lt 15; $i++) {
    Start-Sleep -Seconds 5
    try { $hO = Invoke-RestMethod -Uri "http://127.0.0.1:18080/health" -TimeoutSec 10 -UseBasicParsing; $okO = $true; break } catch {}
}
if (-not $okO) { Write-Host "!! open /health 未就绪" -ForegroundColor Red; exit 1 }
"open-health=$($hO.status) commit=$($hO.commit)"
if ($hO.status -ne "healthy" -or "$($hO.commit)" -notlike "$EXP*") { Write-Host "!! open 未生效" -ForegroundColor Red; exit 1 }

"--- 7) 本地验收 ---"
$prod = Invoke-RestMethod -Uri "http://127.0.0.1:8002/v1/billing/products" -TimeoutSec 20 -UseBasicParsing
"products_ok=$($prod.ok) markets=$($prod.products[0].product) token=$($prod.products[1].product) markets_plans=$($prod.products[0].plans.Count) token_plans=$($prod.products[1].plans.Count)"
if (-not $prod.ok -or $prod.products[0].product -ne "markets" -or $prod.products[1].product -ne "token" -or $prod.products[0].plans.Count -lt 2 -or $prod.products[1].plans.Count -lt 2) { Write-Host "!! /v1/billing/products 验收失败" -ForegroundColor Red; exit 1 }

# 履约端点：错密钥必须 403，好密钥必须走到业务校验（400 bad_payload 即证明密钥通过）
try {
    Invoke-RestMethod -Uri "http://127.0.0.1:18012/api/subscribe/fulfill" -Method Post -Headers @{ "X-Markets-Secret" = "wrong-secret" } -ContentType "application/json" -Body '{}' -TimeoutSec 10 -UseBasicParsing | Out-Null
    Write-Host "!! 错误密钥竟未拒绝" -ForegroundColor Red; exit 1
} catch { "fulfill_bad_secret_status=$($_.Exception.Response.StatusCode.value__)" }
$ful = $null
try {
    $ful = Invoke-RestMethod -Uri "http://127.0.0.1:18012/api/subscribe/fulfill" -Method Post -Headers @{ "X-Markets-Secret" = $secret } -ContentType "application/json" -Body '{"out_trade_no":"qa-fulfill-20260821","user_id":"auth_0","plan":"pro"}' -TimeoutSec 10 -UseBasicParsing
} catch {
    $resp = $_.Exception.Response
    if ($null -ne $resp) { "fulfill_good_secret_status=$($resp.StatusCode.value__)" } else { "fulfill_good_secret_err=$($_.Exception.Message)" }
}
if ($null -ne $ful) { "fulfill_good_secret_body=$($ful | ConvertTo-Json -Compress -Depth 4)" }

# 版本号残留检查（www 39 / open 39 / shell 39 / console 1；旧版残留 0）
$webCount = (rg -l "locales\.js\?v=20260821b" "$REPO\web" -g "*.html" 2>$null | Measure-Object).Count
$openCount = (rg -l "locales\.js\?v=20260821b" "$REPO\p\open\web" -g "*.html" 2>$null | Measure-Object).Count
$shellCount = (rg -l "shell\.js\?v=20260821b" "$REPO\web" -g "*.html" 2>$null | Measure-Object).Count
$consoleCount = (rg -l "console\.js\?v=20260821c" "$REPO\web" -g "*.html" 2>$null | Measure-Object).Count
$oldResidue = 0
foreach ($root in @("$REPO\web", "$REPO\p\open\web")) {
    Get-ChildItem $root -Recurse -Filter "*.html" | ForEach-Object {
        $c = Get-Content $_.FullName -Raw
        $oldResidue += ([regex]::Matches($c, 'locales\.js\?v=(?!20260821b)[0-9a-z]+')).Count
    }
}
"versions: web=$webCount open=$openCount shell=$shellCount console=$consoleCount old_residue=$oldResidue"
if ($webCount -ne 39 -or $openCount -ne 39 -or $shellCount -ne 39 -or $consoleCount -ne 1 -or $oldResidue -ne 0) { Write-Host "!! 版本号校验失败" -ForegroundColor Red; exit 1 }

# 免责声明 + 品牌文案
$loc = Get-Content "$REPO\web\config\locales.js" -Raw -Encoding UTF8
$locOk = ($loc.Contains('"footer.disclaimer"') -and $loc.Contains("仅供教育用途，不构成投资建议") -and $loc.Contains("For educational purposes only"))
"locales_disclaimer=$locOk"
$openIdx = Get-Content "$REPO\p\open\web\index.html" -Raw -Encoding UTF8
"open_title_ok=$($openIdx.Contains('AI24X Open — AI-assisted developer gateway'))"
$con = Get-Content "$REPO\web\console.html" -Raw -Encoding UTF8
"console_deeplink=$($con.Contains('https://markets.ai24x.com/app.html#sub')) console_open_product=$($con.Contains('open-product'))"
if (-not $locOk -or -not $openIdx.Contains('AI24X Open — AI-assisted developer gateway') -or -not $con.Contains('https://markets.ai24x.com/app.html#sub') -or -not $con.Contains('open-product')) { Write-Host "!! 文案验收失败" -ForegroundColor Red; exit 1 }

Write-Host "=== 部署成功 ✅ ===" -ForegroundColor Green
```

## 三、公网验收清单（04 服务器侧执行，本机→公网常超时）
1. `https://api.ai24x.com/health` → commit 以 `be64a991dec2` 开头（core 生效）。
2. `https://open.ai24x.com/health` → commit 以 `be64a991dec2` 开头（open 生效）。
3. `https://www.ai24x.com/` → 页脚可见中英文免责声明（en 语言下英文、zh 语言下中文）；首页 hero 品牌伞「AI24X — AI-assisted tools for markets & developers」。
4. `https://www.ai24x.com/console.html#billing` → Overview Markets 卡「Open product」可点、亮化；Upgrade to Pro 直达 `https://markets.ai24x.com/app.html#sub`。
5. `https://markets.ai24x.com/screener.html` → 扫描时有进度条；`https://markets.ai24x.com/app.html` → 免费档 10、payhub 弹窗、PWA 可安装（manifest/sw）。
6. `https://markets.ai24x.com/pricing.html` + `https://www.ai24x.com/pricing.html` → 免责声明可见；markets pricing 无 signals 雷词。
7. 版本号：www/open 各 39 页 `locales.js?v=20260821b`、www 39 页 `shell.js?v=20260821b`、console.html `console.js?v=20260821c`；旧版本号残留 0。

## 四、回滚预案
```powershell
# 任一服务验收失败先回传司令，按需执行：
git revert be64a991dec2   # 代码回退（04 确认后执行）
Restart-Service AI24X-core, AI24X-markets-api, AI24X-open-api -Force
# markets 静态：恢复 C:\sites\markets.ai24x.com\*.bak-20260821-brand 后重启 markets
```

## 五、群回执（统一格式，全 PASS 才发群）
- ✅ 已完成项：HEAD=be64a991dec2（merge-base + path diff 校验通过）、core/markets/open 三服务健康、/v1/billing/products（markets+token）、fulfill 密钥 403/通过、版本号（web 39/open 39/shell 39/console 1、旧版残留 0）、免责声明/品牌伞/Open product 亮化、screener 进度条、群回执已发。
- ⚠️ 问题项：逐条列出（无则写「无」）。
- 附公网证据：api/open health commit + 页面版本号。
