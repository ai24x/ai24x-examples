# 【04 更新】open.ai24x.com vip-picks 预估消耗列宽修复 · 目标 bf3a19e

> 通道：司令直连 04（deploy04.ps1 同步 600s）。验收全 PASS 后自动发指挥部群回执（✅/⚠️ 清单）；有问题先回传司令，不发群。

## 背景
- 雷总反馈本地 vip-picks 页「预估消耗」列被长文本撑开。已实测定位：名模点名表该列内容含峰时费率（如「入 ¥15.12 · 出 ¥30.24/百万（峰时 入 ¥15.12 · 出 ¥40.32/百万）」），CSS `white-space: nowrap` + `min-width: 190px` 导致列宽 379px、整表横向溢出。
- 本批改动（bf3a19e，只动 1 个页面 + 1 个 QA 脚本）：
  1. `p/open/web/models/vip-picks.html`：`.est-cell` 改 `white-space: normal` + `min-width: 150px`；`.col-est` 表头同步 `min-width: 150px`（表头仍 nowrap）
  2. `p/markets/scripts/_qa_est_col_20260817.js`：新增列宽回归 QA（两张表 est 列宽、无横向溢出）
- 本地已验证（18015 Playwright 中英文）：中文表 2 est 列 379→223px、英文 181px；两张表均无横向溢出、无 JS 报错。
- 共享资源文件（locales/api/shell/support-widget）未变，版本保持 `v=20260817z`，无需全站升版。

## 注意事项
- 只动路径：`p/open/web/models/vip-picks.html` + `p/markets/scripts/_qa_est_col_20260817.js`；04 工作树其它历史未提交文件一律不动（先备份再 checkout，勿强解冲突）。
- 纯静态变更，**不需要重启 core**；robocopy `/MIR` 镜像 `p\open\web` → `C:\nginx\html\ai24x` 即生效。
- 所有 Invoke-WebRequest/RestMethod 必须带 `-UseBasicParsing`；公网验收带 `?x=` 缓存穿透。

## 副脑04 执行脚本（整段复制运行）
```powershell
# === AI24X 更新部署验收 · vip-picks 预估消耗列宽修复 · 目标 bf3a19e ===
$ErrorActionPreference = "Stop"
$REPO = "C:\ai24x01"
$EXP  = "bf3a19e"
$OPEN_SITE = "C:\nginx\html\ai24x"
$OPEN_SRC  = "C:\ai24x01\p\open\web"
$OPEN_BAK  = "C:\nginx\html\ai24x.bak-20260817-2024-列宽修复"
$FILES = @(
  "p/open/web/models/vip-picks.html",
  "p/markets/scripts/_qa_est_col_20260817.js"
)

Set-Location $REPO

"--- 0) 拉取前工作树状态 ---"
git status --short | Select-Object -First 20

"--- 1) 本次路径如有本地改动：备份后 checkout ---"
foreach ($f in $FILES) {
    $dirty = git status --short -- $f
    if ($dirty) {
        $bn = ($f -replace "/", "_")
        Copy-Item "$REPO\$f" "C:\Users\Administrator\ops\dirty-$bn-20260817-2024" -Force
        git checkout -- $f
        if ($LASTEXITCODE -ne 0) { Write-Host "!! checkout 失败: $f" -ForegroundColor Red; exit 1 }
        "dirty backup + checkout: $f"
    } else {
        "clean: $f"
    }
}

"--- 2) 拉取并校验目标提交已包含、本次路径与目标一致 ---"
git pull
$HEAD = (git rev-parse --short=12 HEAD).Trim()
"HEAD=$HEAD"
git merge-base --is-ancestor $EXP HEAD
if ($LASTEXITCODE -ne 0) { Write-Host "!! 目标 $EXP 不在 HEAD 历史中" -ForegroundColor Red; exit 1 }
git diff --quiet $EXP HEAD -- p/open/web/models/vip-picks.html p/markets/scripts/_qa_est_col_20260817.js
if ($LASTEXITCODE -ne 0) { Write-Host "!! 本次路径在 $EXP 之后仍有改动，停止" -ForegroundColor Red; exit 1 }
"target_verified=ok"

"--- 3) 备份现有 open 站目录 + robocopy 镜像同步 ---"
if (-not (Test-Path $OPEN_BAK)) {
    Copy-Item -LiteralPath $OPEN_SITE -Destination $OPEN_BAK -Recurse -Force
}
"backup=$OPEN_BAK"
if (-not (Test-Path "$OPEN_SRC\index.html")) { Write-Host "!! p\open\web 不存在 index.html" -ForegroundColor Red; exit 1 }
robocopy $OPEN_SRC $OPEN_SITE /MIR /NFL /NDL /NJH /NJS /NP
if ($LASTEXITCODE -ge 8) { Write-Host "!! robocopy 失败" -ForegroundColor Red; exit 1 }
"sync=OK"

"--- 4) 本地验收（站点目录）---"
$vp = Get-Content "$OPEN_SITE\models\vip-picks.html" -Raw -Encoding UTF8
"est_wrap=$($vp.Contains('td.est-cell { white-space: normal; min-width: 150px; }'))"
"col_est_min=$($vp.Contains('th.col-est { white-space: nowrap; min-width: 150px; }'))"
"old_nowrap_est=$($vp.Contains('td.est-cell { white-space: nowrap; min-width: 190px; }'))"
"ver=$($vp.Contains('v=20260817z'))"
if (-not ($vp.Contains('td.est-cell { white-space: normal; min-width: 150px; }') -and $vp.Contains('th.col-est { white-space: nowrap; min-width: 150px; }') -and (-not $vp.Contains('td.est-cell { white-space: nowrap; min-width: 190px; }')) -and $vp.Contains('v=20260817z'))) {
    Write-Host "!! vip-picks 本地验收未全过" -ForegroundColor Red; exit 1
}

"--- 5) 公网 open 站验收 ---"
$pub = (Invoke-WebRequest -Uri "https://open.ai24x.com/models/vip-picks.html?x=vp2024" -TimeoutSec 20 -UseBasicParsing).Content
"pub_new_css=$($pub.Contains('td.est-cell { white-space: normal; min-width: 150px; }'))"
"pub_no_old_css=$((-not $pub.Contains('td.est-cell { white-space: nowrap; min-width: 190px; }')))"
"pub_ver=$($pub.Contains('v=20260817z'))"
if (-not ($pub.Contains('td.est-cell { white-space: normal; min-width: 150px; }') -and (-not $pub.Contains('td.est-cell { white-space: nowrap; min-width: 190px; }')) -and $pub.Contains('v=20260817z'))) {
    Write-Host "!! 公网验收未全过" -ForegroundColor Red; exit 1
}

"--- 全部 PASS ---"
Write-Host "=== 部署成功 OK ===" -ForegroundColor Green
```

## 完成后群回执（固定步骤，主通道必做）
- 用 04 应用发指挥部群：
  `python api\feishu_notify.py --config <openclaw.json> --group "【AI24X 部署回执】bf3a19e open 站 vip-picks 预估消耗列宽修复成功：est-cell 改 normal+min-width 150px、旧 190px nowrap 残留 0、版本保持 20260817z、公网页 200 验收通过；问题项：无" --at`
- 把 ✅ 已完成项 / ⚠️ 问题项 清单一并发群里。

## 回执格式（统一）
- ✅ 已完成项：HEAD / 路径备份+checkout / merge-base+diff 校验 / 旧站备份 / robocopy 同步 / 本地 CSS 验收 / 公网验收 / 群回执
- ⚠️ 问题项：逐条列出（无则写「无」）

## 回滚预案
- 恢复旧站：删除 `C:\nginx\html\ai24x` 内容，把 `C:\nginx\html\ai24x.bak-20260817-2024-列宽修复\*` 复制回去（或 robocopy /MIR 反向）；静态回滚无需重启。
- 代码：`git revert bf3a19e`（仅 vip-picks 页面受影响）。
