# 【更新部署指令】控制台邀请回馈 · 被邀请人列表（目标 3b36ff4）

## 变更说明
- feat(web): 控制台邀请回馈卡新增「被邀请人列表」：脱敏用户名 / 注册时间 / 是否激活，默认页与邀请 Tab 双入口。
- 新增 GET /v1/referrals/invitees：返回一级被邀请人 [{referee_id, username_masked, registered_at, activated, level, status, reward_tokens}]；激活口径 = 有充值(topup)或消费(consume)流水（排除注册赠送/邀请奖励）。
- 涉及：api/main.py、api/token_mvp_service.py、web/console.html、web/js/console.js、web/js/api.js、web/config/locales.js（双推已到 gitee+origin）。

## 部署步骤（04 生产仓库 C:\ai24x01，core health 端口 8002）
```powershell
# === AI24X 更新部署验收 · 目标 3b36ff4 ===
$ErrorActionPreference = "Stop"
$REPO = "C:\ai24x01"
$SRV  = "AI24X-core"
$EXP  = "3b36ff4"

Set-Location $REPO
git pull
$HEAD = (git rev-parse --short=12 HEAD).Trim()
"HEAD=$HEAD"
if ($HEAD -ne $EXP) { Write-Host "!! HEAD 不匹配，期望 $EXP 实际 $HEAD" -ForegroundColor Red; exit 1 }
Restart-Service $SRV -Force
$ok = $false
for ($i = 0; $i -lt 12; $i++) {
    Start-Sleep -Seconds 5
    try { $h = Invoke-RestMethod -Uri "http://127.0.0.1:8002/health" -TimeoutSec 10; $ok = $true; break } catch {}
}
if (-not $ok) { Write-Host "!! /health 未就绪" -ForegroundColor Red; exit 1 }
"health=$($h.status) commit=$($h.commit)"
if ($h.commit -ne $EXP) { Write-Host "!! 服务未生效，commit=$($h.commit)" -ForegroundColor Red; exit 1 }
Write-Host "=== 部署成功 ✅ ===" -ForegroundColor Green
```

## 验收清单（部署成功后逐项执行）
① 接口实测（任选已登录 token 或造测试账号）：
   GET /v1/referrals/invitees?limit=50&offset=0（Authorization: Bearer <token>）
   - 无邀请 → 200 且 total=0 / rows=[]
   - 有邀请 → rows 含 username_masked（脱敏：邮箱 a***e@domain / 手机 138****8000）、registered_at、activated(true/false)、level、status
   - 造数验证：被邀请人有 topup 或 consume 流水 → activated=true；仅注册领赠送 → activated=false
② 页面标记（静态抓取即可，表格为 JS 渲染无需行内容）：
   https://www.ai24x.com/console.html 应包含 overview-invitees-wrap / panel-invitees-wrap / invite-reward-table 字样
③ 公网 health：https://api.ai24x.com/health → commit = 3b36ff4

## 群回执（固定）
用 04 应用发指挥部群，格式：
- ✅ 已完成项：HEAD=3b36ff4 / health 200 commit 校验 / /v1/referrals/invitees 接口实测（total、脱敏、activated 口径）/ 公网 health+页面标记
- ⚠️ 问题项：逐条列出（无则写「无」）

## 回滚预案
git revert 3b36ff4 + 重启 AI24X-core
