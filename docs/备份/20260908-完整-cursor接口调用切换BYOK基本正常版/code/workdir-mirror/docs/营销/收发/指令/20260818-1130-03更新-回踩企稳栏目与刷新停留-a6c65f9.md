# 【03 更新】新增「回踩企稳」栏目 + 刷新停留当前菜单页

> 通道：司令直连 03（deploy03.ps1）｜ 目标提交 **a6c65f9**
> 03 主机：123.207.199.238 · 仓库 C:\ai24x01 · 服务 AI24X-a1-api（NSSM，8001）· 无 D 盘（备份落 C:\backup\ai24x_a\）
> a1 /health 无 commit 字段：验收用「文件标记 + 进程新建时间 + 服务器侧行为 QA」

## 背景
老板确认：主算法保留（回踩企稳已是最高分形态），另加「回踩企稳」独立栏目（类似 MACD首红）供纯回踩流派一键过滤；同时修复「刷新后跳回默认菜单页」。
实现：
1. 后端 bj_screener.py 新增 `pb_view(out)`：复用 hs（沪深主线）扫描与当日缓存，过滤主推/备选中 `ztPullback`（涨停级回踩）/`pullback2`（板后回踩企稳）/`firstBoardRight`（首板右侧上拐）形态的标的；板块排行与主线卡片保持 hs 口径一致。main.py：market=pb 路由（合法列表+scan_market 映射 hs+两处返回 pb_view；start/partial/progress 同步映射）。
2. 前端桌面（bjscreener.js v82→v83 + gd.html 新 tab「回踩企稳」）：marketLabel/marketScope/isAll 加 pb；空态提示「暂无回踩企稳形态标的」；tab 点击存 localStorage `bj_tab`、boot 时恢复（URL market 参数优先，归档模式不受影响）。
3. 手机版 m/gd.html：加「回踩企稳」tab + 同样刷新停留（URL 优先→localStorage 兜底）。
本地已实测：桌面+手机「回踩企稳」tab 正常（空态提示正确、板块排行主线口径一致）；切 tab→刷新后停留原 tab；pb_view 单测过滤正确；无 JS 报错。

## 执行步骤（PowerShell，整段复制运行）
```powershell
$ErrorActionPreference = "Stop"
$REPO = "C:\ai24x01"
$SRV  = "AI24X-a1-api"
$EXP  = "a6c65f9"

Set-Location $REPO

"--- 0) 备份本地脏改动再还原（防止 pull 冲突）---"
$FILES = @("p\a1\api\server\app\bj_screener.py","p\a1\api\server\app\main.py","p\a1\web\js\bjscreener.js","p\a1\web\gd.html","p\a1\web\m\gd.html")
$BK = "C:\backup\ai24x_a\20260818_pre_a6c65f9"
New-Item -ItemType Directory -Force -Path $BK | Out-Null
foreach ($x in $FILES) {
  if (Test-Path "$REPO\$x") {
    $chg = git -C $REPO status --porcelain -- $x
    if ($chg) { Copy-Item "$REPO\$x" "$BK\$(Split-Path $x -Leaf).bak" -Force; git -C $REPO checkout -- $x }
  }
}
"备份目录: $BK"

"--- 1) 拉码 + HEAD 校验 ---"
git pull
$HEAD = (git rev-parse --short=12 HEAD).Trim()
"HEAD=$HEAD"
git merge-base --is-ancestor $EXP HEAD
if ($LASTEXITCODE -ne 0) { Write-Host "!! HEAD 不含目标提交 $EXP" -ForegroundColor Red; exit 1 }

"--- 2) 文件标记检查 ---"
$py = Get-Content "$REPO\p\a1\api\server\app\bj_screener.py" -Raw -Encoding UTF8
$main = Get-Content "$REPO\p\a1\api\server\app\main.py" -Raw -Encoding UTF8
$js = Get-Content "$REPO\p\a1\web\js\bjscreener.js" -Raw -Encoding UTF8
$gd = Get-Content "$REPO\p\a1\web\gd.html" -Raw -Encoding UTF8
$m  = Get-Content "$REPO\p\a1\web\m\gd.html" -Raw -Encoding UTF8
"py_pb=$($py.Contains('def pb_view'))  py_pb2=$($py.Contains('ztPullback") or pp.get("pullback2') -or $py.Contains('ztPullback') -and $py.Contains('pb_view'))"
"main_pb=$($main.Contains('pb_mode = market == "pb"'))  main_map=$($main.Contains('("hs" if pb_mode else market)'))"
"js_pb=$($js.Contains('if (m === "pb") return "回踩企稳"'))  js_save=$($js.Contains('localStorage.setItem("bj_tab"'))  js_restore=$($js.Contains('localStorage.getItem("bj_tab"'))"
"gd_v83=$($gd.Contains('bjscreener.js?v=83'))  gd_tab=$($gd.Contains('data-market="pb"'))"
"m_tab=$($m.Contains('data-m="pb"'))  m_save=$($m.Contains('localStorage.setItem(''bj_tab'''))"
if (-not ($py.Contains('def pb_view') -and $main.Contains('pb_mode = market == "pb"') -and $js.Contains('localStorage.setItem("bj_tab"') -and $js.Contains('localStorage.getItem("bj_tab"') -and $gd.Contains('bjscreener.js?v=83') -and $gd.Contains('data-market="pb"') -and $m.Contains('data-m="pb"') -and $m.Contains('bj_tab'))) {
  Write-Host "!! 文件标记缺失" -ForegroundColor Red; exit 1
}

"--- 3) 重启 AI24X-a1-api (8001) ---"
$before = (Get-Process -Name python -ErrorAction SilentlyContinue | Sort-Object StartTime -Descending | Select-Object -First 1).StartTime
Restart-Service $SRV -Force
$ok = $false
for ($i = 0; $i -lt 15; $i++) {
  Start-Sleep -Seconds 5
  try { $h = Invoke-RestMethod -Uri "http://127.0.0.1:8001/health" -TimeoutSec 10 -UseBasicParsing; $ok = $true; break } catch {}
}
if (-not $ok) { Write-Host "!! /health 未就绪" -ForegroundColor Red; exit 1 }
"health=$($h.status)"
$after = (Get-Process -Name python -ErrorAction SilentlyContinue | Sort-Object StartTime -Descending | Select-Object -First 1).StartTime
"proc_before=$before"
"proc_after =$after"
if ($after -le $before) { Write-Host "!! 进程新建时间未更新（Restart-Service 可能未真正重启，需手动补重启）" -ForegroundColor Red; exit 1 }

"--- 4) 服务器侧行为 QA：pb_view 过滤 + pb 路由 ---"
$qa = @'
import json
import app.bj_screener as bj
out = {
    "ok": True, "cached": True, "date": "2026-08-17", "asof": "2026-08-17",
    "market_code": "hs", "total": 10, "scanned": 60, "fine": 3,
    "generated_ts": 0, "elapsed_s": 1.0, "market": {}, "regime": "stable", "style": {},
    "mainlines": [{"name": "AI服务器算力", "src": "daily"}],
    "board_rank": [{"name": "AI服务器算力", "tier": "king", "mainline": True}],
    "picks": [
        {"code": "600111", "name": "PB1", "patterns": {"pullback2": 1, "ztPullback": 0}},
        {"code": "600222", "name": "FBR1", "patterns": {"firstBoardRight": 1}},
        {"code": "600333", "name": "SURGE1", "patterns": {"surgeStart": 1}},
        {"code": "600444", "name": "NONE", "patterns": {}},
    ],
    "runners": [
        {"code": "600555", "name": "PB2", "patterns": {"pullback2": 1}},
        {"code": "600666", "name": "SURGE2", "patterns": {"surgeStart": 1}},
    ],
}
o = bj.pb_view(out)
assert [c["code"] for c in o["picks"]] == ["600111", "600222"], "picks filter wrong"
assert [c["code"] for c in o["runners"]] == ["600555"], "runners filter wrong"
assert o["market_code"] == "pb" and o["board_rank"][0]["mainline"] is True, "meta wrong"
print("PB_VIEW_OK picks=", [c["code"] for c in o["picks"]])
'@
$qaPath = "$REPO\p\a1\api\server\_qa_pb_view_20260818.py"
[System.IO.File]::WriteAllText($qaPath, $qa, (New-Object System.Text.UTF8Encoding($false)))
Push-Location "$REPO\p\a1\api\server"
& "C:\PYTHON314\PYTHON.EXE" $qaPath
$qaOk = ($LASTEXITCODE -eq 0)
Pop-Location
Remove-Item -LiteralPath $qaPath -Force -ErrorAction SilentlyContinue
if (-not $qaOk) { Write-Host "!! 行为 QA 失败" -ForegroundColor Red; exit 1 }

"--- 5) 公网页面内容标记 ---"
$pubGd = (Invoke-WebRequest -Uri "https://a.ai24x.com/gd.html" -UseBasicParsing -TimeoutSec 20).Content
$pubJs = (Invoke-WebRequest -Uri "https://a.ai24x.com/js/bjscreener.js?v=83" -UseBasicParsing -TimeoutSec 20).Content
$pubM  = (Invoke-WebRequest -Uri "https://a.ai24x.com/m/gd.html" -UseBasicParsing -TimeoutSec 20).Content
"pub_gd_v83=$($pubGd.Contains('bjscreener.js?v=83'))  pub_gd_tab=$($pubGd.Contains('data-market="pb"'))"
"pub_js_save=$($pubJs.Contains('localStorage.setItem("bj_tab"'))"
"pub_m_tab=$($pubM.Contains('data-m="pb"'))  pub_m_save=$($pubM.Contains('bj_tab'))"
if (-not ($pubGd.Contains('bjscreener.js?v=83') -and $pubGd.Contains('data-market="pb"') -and $pubJs.Contains('localStorage.setItem("bj_tab"') -and $pubM.Contains('data-m="pb"'))) {
  Write-Host "!! 公网内容不对" -ForegroundColor Red; exit 1
}

Write-Host "=== 部署成功 OK ===" -ForegroundColor Green
```

## 验收清单（回执时逐条列出）
- HEAD=a6c65f9（merge-base 校验通过）／文件标记（pb_view 定义 + main.py pb_mode/映射 + js bj_tab 存取 + gd v83/tab + m tab/bj_tab）
- 进程新建时间更新／health 200
- 行为 QA：PB_VIEW_OK（picks 只留 pullback2/firstBoardRight，runners 只留 pullback2）
- 公网 gd.html v83 + pb tab；js?v=83 含 bj_tab 存取；m/gd.html 含 pb tab
- 浏览器端（Ctrl+F5，用户侧确认）：桌面/手机新增「回踩企稳」tab（空态提示正常、板块排行与主线口径一致）；切换任意栏目后刷新停留在原栏目
- ⚠️ 问题项：无则写「无」

## 回执格式（03 自动发指挥部群）
- ✅ 已完成项：HEAD／文件标记／重启+health／行为 QA／公网内容标记
- ⚠️ 问题项：逐条（无则写「无」）
- 凭据纪律：不回执任何 token 明文；备份目录仅报路径。

## 回滚预案
- `git revert a6c65f9` + 重启 AI24X-a1-api；或恢复 `C:\backup\ai24x_a\20260818_pre_a6c65f9\*.bak` + 重启。
