# 【04 放行】Markets 中国指数 K线 部署回执放行（81da70a 已部署达标）

> 通道：司令直连 04（deploy04.ps1）。上一轮你回传的「验收脚本断言字面量 bug」已由司令确认：白名单实际是 SAMPLES 数组 `sym:` 字段（按钮 data-symbol 是运行时生成），指令脚本断言写错；**部署本体与功能均已验证达标**，指令文档断言已修正推送（f1ee766）。现放行。

## 请执行（整段复制运行）
```powershell
# === 04 放行：发群回执 ===
$ErrorActionPreference = "Stop"
Set-Location "C:\ai24x01"
git pull
$HEAD = (git rev-parse --short=12 HEAD).Trim()
"HEAD=$HEAD"
if ($HEAD -notlike "f1ee766*") { Write-Host "!! 指令校准提交未拉到，先 git pull" -ForegroundColor Red; exit 1 }

python api\feishu_notify.py --config "C:\Users\Administrator\.openclaw\openclaw.json" --group "【AI24X 部署回执】Markets 中国指数 K线 + 失败清图 81da70a 成功：HEAD=81da70a、markets health OK、上证指数中文搜索出 120 根完整历史K线（asof 2026-08-14 上一交易日、未开盘无当日bar）、恒指 HKD/上证 CNY 货币正确、app 白名单含 6 个中国指数、搜索失败清空旧图+暂无数据提示、公网验收通过。问题项：验收脚本白名单断言字面量已按实际实现校准（SAMPLES sym 字段），功能验证通过，无遗留问题。" --at
if ($LASTEXITCODE -ne 0) { Write-Host "!! 群回执发送失败" -ForegroundColor Red; exit 1 }
Write-Host "=== 群回执已发 OK ===" -ForegroundColor Green
```

## 回执格式（统一）
- ✅ 已完成项：git pull 到 f1ee766 / 群回执已发（81da70a 达标 + 断言校准说明）
- ⚠️ 问题项：无
