# 需求类型：④国际生产/修复（副脑04）
# 标题：修复 2 项验收卡点：Buy on Gateway 跳转 404 + Redis 未运行（雷总 08-30 确认）
# 期望：今日谷段/明日完成，回执

## 背景
18:30 验收发现 2 项 P1 卡点（均影响转化/稳定性），雷总 08-30 20:36 确认处理：

### 1. `/p/open/web/` 与 `/p/markets/web/` 返回 404（丢单风险）
- www 站「Buy on AI Gateway」跳转链路断裂：按钮指向的路径返回 404，用户点购买见错误页
- 修复：定位跳转配置（nginx/前端路由），把链接修正为正确的购买页 URL（open.ai24x.com 对应页 / markets 对应页）
- 验收：curl 原 404 路径与修正后路径，购买按钮可达正确页面（HTTP 200 + 页面可加载）

### 2. Redis 未运行（6379 端口关闭）
- BYOK 用量 daily 缓存、续费提醒等依赖 Redis 的功能降级
- 修复：启动 Redis 服务 + 配置开机自启（Windows 服务/NSSM 均可）
- 验收：netstat 6379 监听 + 一个 Redis 读写测试通过 + 重启后仍自启

## 纪律
- 生产修改走 deploy04 流程/备份可回滚；改动不动 api/.env、不改密钥
- 完成后回执：修改文件/命令 + 验收证据（curl 状态码、netstat 输出）

## 回执
✅ 完成项 + 证据；⚠️ 问题项（无则写无）
