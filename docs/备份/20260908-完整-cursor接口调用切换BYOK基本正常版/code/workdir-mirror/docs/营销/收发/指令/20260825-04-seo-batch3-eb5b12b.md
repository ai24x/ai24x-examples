# 【04 更新】markets SEO 第三批 /stocks/ 上线（10 只）+ sitemap 46 条

> 通道：司令直连 04（deploy04.ps1 同步 600s）｜ 目标提交 **eb5b12b**
> 回执：落盘 ops + 收发回复留文件即可，**不发群**（降噪规则 2026-08-22）；有异常才上报司令。

## 背景
- 生成器 seo_gen.py STOCKS 新增 10 只（JPM/V/LLY/JNJ/PFE/XOM/BA/CAT/NKE/DIS），本地已生成 30 页股票页（20 旧 + 10 新），QA batch3 32/32 + batch2 回归 22/22 全绿。
- sitemap.xml 重建为 46 条（6 主页面 + 30 股票 + 10 对比/教程），本地已验。
- 全部页面图片绝对路径 `/seo/assets/{sym}.svg`（沿用第二批修复）；新增 10 个 SVG 资产。
- 本批无需改 nginx（/stocks/ 路由已上线）。

## 执行步骤（PowerShell，整段复制运行）
```powershell
$ErrorActionPreference = "Stop"
$REPO = "C:\ai24x01"
$EXP  = "eb5b12b"
$SITE = "C:\sites\markets.ai24x.com"

Set-Location $REPO

"--- 0) 拉码 + HEAD 校验 ---"
git pull
$HEAD = (git rev-parse --short=12 HEAD).Trim()
"HEAD=$HEAD"
if ($HEAD -notlike "$EXP*") {
  $anc = git merge-base --is-ancestor eb5b12b HEAD
  if ($LASTEXITCODE -eq 0) { Write-Host "!! HEAD=$HEAD 为期望提交后代，放行" -ForegroundColor Yellow }
  else { Write-Host "!! HEAD 不符" -ForegroundColor Red; exit 1 }
}

"--- 1) 文件就位检查 ---"
$f = @("p\markets\web\seo\jpm.html","p\markets\web\seo\lly.html","p\markets\web\seo\dis.html","p\markets\web\seo\assets\jpm.svg","p\markets\web\sitemap.xml")
foreach ($x in $f) { if (-not (Test-Path "$REPO\$x")) { Write-Host "!! 缺 $x" -ForegroundColor Red; exit 1 } }
"files_ok=5/5"

"--- 2) 备份 + 同步 seo 目录 + sitemap ---"
$ts = Get-Date -Format "yyyyMMdd-HHmmss"
Copy-Item "$SITE\seo" "$SITE\seo.bak-seo3-$ts" -Recurse -Force
Copy-Item "$REPO\p\markets\web\seo\*" "$SITE\seo\" -Recurse -Force
Copy-Item "$REPO\p\markets\web\sitemap.xml" "$SITE\sitemap.xml" -Force
"seo_files=$((Get-ChildItem "$SITE\seo" -File).Count) sitemap=1 bak=seo.bak-seo3-$ts"

"--- 3) 静态禁词抽检 ---"
$bad = Get-ChildItem "$SITE\seo" -Filter *.html | Select-String -Pattern '\b(buy|sell|target price|tips|picks)\b' -AllMatches
"seo_forbidden_hits=$($bad.Count)"
if ($bad.Count -gt 0) { Write-Host "!! SEO 页含禁用词" -ForegroundColor Red; exit 1 }

"--- 4) 公网验收 ---"
function Probe($url, $needle) {
  try {
    $r = Invoke-WebRequest -Uri $url -UseBasicParsing -TimeoutSec 25
    $ok = if ($needle) { $r.Content.Contains($needle) } else { $true }
    "{0} -> {1} needle={2} len={3}" -f $url, $r.StatusCode, $ok, $r.Content.Length
  } catch { "{0} -> ERROR {1}" -f $url, $_.Exception.Message }
}
Probe "https://markets.ai24x.com/stocks/jpm" 'rel="canonical" href="https://markets.ai24x.com/stocks/jpm"'
Probe "https://markets.ai24x.com/stocks/lly" "Eli Lilly"
Probe "https://markets.ai24x.com/stocks/dis" "Walt Disney"
Probe "https://markets.ai24x.com/stocks/xom" "https://markets.ai24x.com/seo/assets/xom.svg"
Probe "https://markets.ai24x.com/seo/assets/jpm.svg" $null
Probe "https://markets.ai24x.com/sitemap.xml" "stocks/xom"
Probe "https://markets.ai24x.com/seo/index.html" "stocks/dis"

"--- 5) sitemap 重提（best-effort，旧 ping 端点可能 404/410，不算失败） ---"
curl.exe -s -o NUL -w "google_ping=%{http_code}`n" "https://www.google.com/ping?sitemap=https%3A%2F%2Fmarkets.ai24x.com%2Fsitemap.xml"
curl.exe -s -o NUL -w "bing_ping=%{http_code}`n" "https://www.bing.com/ping?sitemap=https%3A%2F%2Fmarkets.ai24x.com%2Fsitemap.xml"
Write-Host "（如沿用 Search Console API 方式，请在 GSC 重提 sitemap）" -ForegroundColor Yellow

Write-Host "=== 部署成功 OK ===" -ForegroundColor Green
```

## 完成后回执（落盘，不发群）
- 落盘 `C:\Users\Administrator\ops\seo-batch3-deploy-20260825.md`：✅ HEAD / 文件 5/5 / seo+sitemap 同步（备份名）/ 禁词 0 / 公网验收 7 项 / sitemap 重提结果；⚠️ 问题项（无则写「无」）。
- 通知司令：本机收发目录留回执文件。

## 回滚预案
- 静态：恢复 `C:\sites\markets.ai24x.com\seo.bak-seo3-<ts>` + sitemap 手工还原；无需重启服务、无需改 nginx。
