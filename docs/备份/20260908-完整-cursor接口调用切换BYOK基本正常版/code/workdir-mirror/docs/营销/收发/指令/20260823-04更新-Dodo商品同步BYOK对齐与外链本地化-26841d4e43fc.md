# 【04 更新】Dodo 商品自动同步 + BYOK/Token 正式价对齐 + 全站外链本地环境感知

> 通道：司令直连 04（deploy04.ps1）｜ 目标提交 **26841d4e43fc**
> 04 主机：43.160.246.30 · 仓库 C:\ai24x01 · 服务 AI24X-core（NSSM，8002）· 静态 web/ 由仓库直读
> 本次涉及 api/ 后端（Dodo 同步 + 套餐目录）+ www 静态（BYOK 区 / 外链本地化）+ p/open 价格数据文件；无 markets 后端变更、无 DB 迁移

## 背景
老板放行：① Dodo 支付商品自动同步（分页去重/归档复用），Token 正式价 $2/$20/$49/$15/$99 + BYOK $9.9/$99 对齐支付后台；② 全站二级域名外链改为「本地环境感知」（本地 18012/18080 可测、生产保持原样），shell.js/console.js 版本 20260823c。本地 QA：_qa_env_links 20/20 + console_byok/pricing_byok/console_plans/www/nav_vip/oauth 全绿。

## 执行步骤（PowerShell，整段复制运行）
```powershell
$ErrorActionPreference = "Stop"
$REPO = "C:\ai24x01"
$SRV  = "AI24X-core"
$EXP  = "26841d4e43fc"

Set-Location $REPO

"--- 0) 备份本批文件本地脏改动再还原（防 pull 冲突；api/model_router.py 等无关 04 本地热修勿动）---"
$FILES = @("web/console.html","web/js/console.js","web/js/shell.js","web/pricing.html","web/product.html","web/models/index.html","web/guides/index.html","web/token-admin.html","web/config/locales.js","api/config.py","api/main.py","api/pay_products.py","api/token_pay_service.py","api/token_plans.py",".gitignore")
$BK = "C:\backup\www_core_20260823_pre_26841d4"
New-Item -ItemType Directory -Force -Path $BK | Out-Null
foreach ($x in $FILES) {
  if (Test-Path "$REPO\$x") {
    $chg = git -C $REPO status --porcelain -- $x
    if ($chg) { Copy-Item "$REPO\$x" "$BK\$(Split-Path $x -Leaf).bak" -Force; git -C $REPO checkout -- $x }
  }
}
"备份目录: $BK"

"--- 1) 拉码 + HEAD 校验 ---"
git pull
$HEAD = (git rev-parse --short=12 HEAD).Trim()
"HEAD=$HEAD"
git merge-base --is-ancestor $EXP HEAD
if ($LASTEXITCODE -ne 0) { Write-Host "!! HEAD 不含目标提交 $EXP" -ForegroundColor Red; exit 1 }

"--- 2) 文件标记检查（仓库源码）---"
$c1 = Get-Content "$REPO\web\js\shell.js" -Raw -Encoding UTF8
$c2 = Get-Content "$REPO\web\js\console.js" -Raw -Encoding UTF8
$c3 = Get-Content "$REPO\web\console.html" -Raw -Encoding UTF8
$c4 = Get-Content "$REPO\web\pricing.html" -Raw -Encoding UTF8
$c5 = Get-Content "$REPO\api\main.py" -Raw -Encoding UTF8
$c6 = Get-Content "$REPO\p\open\api\data\token_plans_override.json" -Raw -Encoding UTF8
$m1 = $c1.Contains('AI24X_OPEN_URL') -and $c1.Contains('a[data-open]')
$m2 = $c2.Contains('function productUrl') -and $c2.Contains('openApiBase')
$m3 = $c3.Contains('shell.js?v=20260823c') -and $c3.Contains('console.js?v=20260823c')
$m4 = $c4.Contains('byokSection') -and $c4.Contains('locales.js?v=20260823b')
$m5 = (Test-Path "$REPO\api\pay_dodo_products.py") -and $c5.Contains('pay_dodo')
$m6 = $c6.Contains('"price_usd": 2.0') -and $c6.Contains('"price_usd": 99.0') -and $c6.Contains('"price_usd": 9.9')
"mark_shell=$m1 mark_console=$m2 mark_console_ver=$m3 mark_pricing=$m4 mark_dodo=$m5 mark_token_price=$m6"
if (-not ($m1 -and $m2 -and $m3 -and $m4 -and $m5 -and $m6)) { Write-Host "!! 文件标记缺失" -ForegroundColor Red; exit 1 }

"--- 3) 重启 AI24X-core (8002)：PID 断言 + health commit 校验 ---"
$beforePid = (Get-CimInstance Win32_Service -Filter "Name='$SRV'").ProcessId
"svc_pid_before=$beforePid"
Restart-Service $SRV -Force
$ok = $false
for ($i = 0; $i -lt 24; $i++) {
  Start-Sleep -Seconds 5
  try {
    $svc = Get-CimInstance Win32_Service -Filter "Name='$SRV'"
    $afterPid = $svc.ProcessId
    $h = Invoke-RestMethod -Uri "http://127.0.0.1:8002/health" -TimeoutSec 10 -UseBasicParsing
    if ($afterPid -gt 0 -and $afterPid -ne $beforePid -and $h.commit -and $h.commit -like "$EXP*") { $ok = $true; break }
  } catch {}
}
"svc_pid_after=$afterPid health_ok=$ok commit=$($h.commit)"
if (-not $ok) { Write-Host "!! 服务未按预期重启或 commit 不符" -ForegroundColor Red; exit 1 }

"--- 4) 公网标记（?x= 缓存穿透）+ open 接口抽查 ---"
$u1 = (Invoke-WebRequest -Uri "https://www.ai24x.com/console.html?x=20260823c" -UseBasicParsing -TimeoutSec 25).Content
$u2 = (Invoke-WebRequest -Uri "https://www.ai24x.com/pricing.html?x=20260823c" -UseBasicParsing -TimeoutSec 25).Content
$pm1 = $u1.Contains('shell.js?v=20260823c') -and $u1.Contains('console.js?v=20260823c')
$pm2 = $u2.Contains('byokSection')
"pub_console_ver=$pm1 pub_pricing_byok=$pm2"
if (-not ($pm1 -and $pm2)) { Write-Host "!! 公网内容不对（可能缓存，换 ?x= 再试）" -ForegroundColor Red; exit 1 }
try {
  $b = Invoke-RestMethod -Uri "https://open.ai24x.com/v1/billing/plans" -UseBasicParsing -TimeoutSec 20
  $byokN = @($b.byok_plans).Count
  "open_byok_plans=$byokN"
  if ($byokN -lt 2) { Write-Host "!! open BYOK 套餐异常" -ForegroundColor Red; exit 1 }
} catch { Write-Host "!! open 接口异常: $($_.Exception.Message)" -ForegroundColor Red; exit 1 }

Write-Host "=== 部署成功 OK ===" -ForegroundColor Green
```

## 验收清单（回执时逐条列出）
- HEAD=26841d4e43fc（merge-base 校验通过）／文件标记 6 条（shell openUrl/data-open、console productUrl/openApiBase、console 版本 20260823c、pricing byokSection、pay_dodo_products.py、open token 正式价 $2/$99/$9.9）
- AI24X-core PID 变化（before/after）／health 200 + commit=26841d4e43fc
- 公网标记 2 条（console.html / pricing.html，?x=20260823c 穿透）+ open.ai24x.com/v1/billing/plans byok_plans=2
- 浏览器端（老板 Ctrl+F5 后确认）：www pricing BYOK 区、console Plans BYOK 卡跳 open、全站外链本地/生产双环境正确
- ⚠️ 问题项：无则写「无」

## 回执格式（04 自动发指挥部群）
- ✅ 已完成项：HEAD／文件标记／重启+health+commit／公网标记+open 抽查
- ⚠️ 问题项：逐条（无则写「无」）
- 凭据纪律：不回执任何 token 明文；备份目录仅报路径。

## 回滚预案
- `git revert 26841d4e43fc` + `Restart-Service AI24X-core -Force`；或恢复 `C:\backup\www_core_20260823_pre_26841d4\*.bak` + 重启 AI24X-core。
