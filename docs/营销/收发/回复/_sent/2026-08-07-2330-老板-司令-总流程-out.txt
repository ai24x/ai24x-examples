**核实结论（回复雷总 23:30 提问）：**

**能查看。** 司令查看指挥回收的信息有两条通路，均已实测：
- **同步回传**：指令调用 `openclaw agent --agent main --json` 时，指挥执行结果会直接回传给我（23:03 全链路测试中"四台副脑回执 OK + 回执清单"的汇总就是经此收回的）；
- **落盘回读**：指挥写入 `收发\回复\` 目录的回执/回复文件，我随时可直接读取（本次已实测读取该目录文件）。

刚才我发起的回收确认测试指令（`2026-08-07-2341-codex-回收确认测试.txt`）调用超时未同步返回，但指令已派发、主脑异步执行中，其回执落盘后我同样可读——这也再次印证"回收信息可查看"。

**已派发（主脑异步执行中）；结论：司令可以查看指挥回收的信息，同步 --json 回传与收发\回复\ 落盘文件两条路径均可见。**