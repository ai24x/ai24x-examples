# 复盘 gd 算法优化 + 10元下栏目 — 完成回执（总司令）

> 2026-08-25 · 本地 18011 已验证；等老板本机验收 → 放行 → 双推 + 03 更新

## ✅ 已完成（算法 + 前端 + 回放）

- **P0-1 主线去滞后**：主线排序权重改为「增量动量」（近 1-3 日新增异动家数、量能拐头、涨停梯队晋级、首次上榜优先）；连续上榜 N 日标「高位过热」降级，防追高接盘。
- **P0-2 强势动量轴并入总分**：近 3 日斜率、涨停/大阳次数、量比、创新高速度、RSI 加速度、主力净流入拐头；「刚右侧 + 放量 + 涨停/大阳确认」压过「低位慢涨」。
- **P0-3 king 位改「强动量 + 主线共振」**，回踩低吸类降为备选。
- **P0-4 门槛校准**：用「近期强动量 + 位置中低(<60%)」取代「位置<30% + 没涨」，避免误杀强势启动票。
- **P1 栏目收敛**：hs/kc/bj/bj_all 统一「强势主线 + 强势标的」双轴口径；macd/pb 接入统一强势动量轴。
- **P2 新增「10元下」栏目**：价 2~10 元 + 优质刚右侧 + 强势动量；硬伤排除 ST/退市/面值警戒(<2 元)/一字板/流动性不足；gd.html 新 tab。
- **历史回放**：`GET /api/bj/replay?date=YYYY-MM-DD&market=...`（VIP、只读缓存零上游）；gd.html 历史列表每行「⚡回放」按钮；手机版 m/gd.html 同步。

## 🔧 最新补丁（老板口径「有数据能有 2 只就 2 只最好」）

- **10元下最多 2 只、宁缺毋滥**：只展示带完整 K 线图（chart）的标的——备选池不带图会渲染成空白卡片（价格 0.00 / 无 K 线），一律过滤；
- 不足 2 只时用当日 K 线缓存回放补齐（`replay_scan` 只读零上游），仍要求带图 + 质量；命中即停，不硬凑；
- 实测：08-24 归档下 low10 从「6 只里 5 只无图」→ 只剩 **1 只带图（甘肃能化 2.62 元，60 根 K 线）**，无空白卡片。

## 🔧 贵金属「大跌还排主线第一」根因修复（2026-08-25 15:30）

- **根因**：08-25 复盘已收盘定型，判定「今日无主线」（observes=种植业/煤炭/通信光模块CPO）；
  但 `_daily_mainlines()` 因当日主线为空，擅自回退到 **08-24 昨日主线（贵金属/煤炭）**，
  判定小字（avg_up 4.69 / pos60 100% / composite 81.3）全是 **08-24 数据**，且前端只认 `src="daily"`，
  把昨日主线当今日主线展示 → 贵金属今天 -5.25% 大跌仍霸占主线第一。
- **修复**（bj_screener.py + bjscreener.js + m/gd.html）：
  ① 当日已定型（归档或板块缓存存在）→ 一律返回当日判定，即使「无主线」也不回退昨日；
  ② 只有当日完全无数据（盘中/早间未扫描）才回退最近归档日，并透传 `mainline_date`/`mainline_from`；
  ③ 前端支持 archive/recalc/fallback 来源 + 回退时显示「数据截至 YYYY-MM-DD」；
  ④ 无主线文案区分「今日已收盘研判：未锁定主线」。
- **验证（force=1 重扫 08-25）**：mainlines=[]（今日无主线）、observes=种植业/煤炭/通信光模块CPO；
  板块排行按 5 日主力净流入排（黄金概念第一/贵金属第二），贵金属不再标主线；
  桌面页 + 手机版 375px 均无 JS 报错、无横向溢出。

## 🧪 本地验证

- 18011 已重启（新 PID，/health ok）；`python -m compileall` + `node --check bjscreener.js` 通过；
- `market=low10` 返回 1 只带图标的；`/api/bj/replay?date=2026-08-24&market=low10` 正常；
- 当前 14:25 盘中 → 正确展示 08-24 收盘定型归档（`stale=True / intraday=True`）；**15:01 后可点「重新扫描」生成 08-25 当日数据**。

## 🔧 批2｜无主线→all 全市场扫描退化修复（2026-08-25 16:40）

- **根因**：今日复盘定型「无主线」→ `_detect_style` 判 `fan`（电风扇）→ `_fine_pass` 风格门槛 +4 且只做回踩形态 →
  all（沪深京全市场）扫描 fine 从 9 掉到 1，直接拖垮 MACD首红 / 10元下 两个全市场栏目。
- **修复**（bj_screener.py `_fine_pass`）：`_all_view = market == "all"`，全市场视图忽略风格门槛 delta（`_style_delta=0`）
  与 fan 形态限定——全市场栏目是「强势筛选」，不跟随主线有无波动，避免刚启动强势标的被误杀。
- **验证（force=1 重扫 08-25）**：all → fine=7、low10=青山纸业/南风股份、macd_reds 由复盘样本兜底 1 只（钢研高纳）。

## ✅ 自动更新机制核查（老板问：除沪深主线外，其它栏目 15:01 后是否自动更新？）

- **是，全部自动更新，但口径是 15:10**：`bj_screener._AUTO_SCAN_TS = 15:10`（2026-08-18 因 15:03-15:07 抓到未定型盘中 bar 事故，
  自动扫描从 15:03 调后到 15:10）。交易日 15:10 后自动依次扫描 `hs → kc → bj → bj_all → all` 五个市场池。
- **三个「视图栏目」复用主扫描，零额外上游**：MACD首红 与 10元下 复用 `all` 全市场扫描；回踩企稳 复用 `hs` 扫描
  （main.py 路由 `scan_market = "all" if (macd/low10) else ("hs" if pb else market)`）。
- **手动「重新扫描」15:01 即可点**（`_market_closed()` = 15:01），前端门控已同步为 15:01（文案「盘中未定型：15:01 后可重新扫描」）。

## ✅ 算法统一性核查（老板问：7 个栏目算法是否统一？）

- **结构统一**：七栏目共用同一套 `analyze()` 形态判定 + 评分主轴（位置/启动证据/承接量价/资金主线/时机均线，封顶防重复加分），
  同一 `_fine_pass` 精筛，同一情绪 Gate / 涨停质量 / ATR 分位。差异只在**市场参数分层**：
  - `hs / kc / all`（沪深主线/科创主线/全市场）：同一参数 `mcap 15~200亿 / 成交≥8000万 / 位置≤35% / scoreMin 44`；
  - `bj`（北证主线）：DEFAULT_CFG `mcap 5~40亿 / 成交≥3000万 / 位置≤30% / scoreMin 46`（更严、小市值弹性）；
  - `bj_all`（北证全市场）：`mcap 5~60亿 / 位置≤40% / 5日≤28% / scoreMin 46 / 换手≤25%`（放宽涨幅与换手，容 30cm 波动）；
  - `macd / pb / low10`：分别复用 all / hs / all 扫描结果做视图过滤，无独立算法分支。
- **今日 08-25 收盘后各栏目真实快照（16:41 重扫后）**：
  - 主线：全部 `[]`（今日已收盘研判「无主线」，observes=种植业/煤炭/通信光模块CPO）；
  - 沪深主线：潞安环能（king/主线代表通道，fine=0 属今日强约束真实判定）；
  - 科创主线：无达标标的（fine=1，强达电路 final=54 未达主推线）→ 回退最近归档 08-23（数据截至 08-21，嘉元/思泰克）；
  - 北证主线：龙辰科技/族兴新材；北证全市场：族兴新材/创达新材；
  - 10元下：青山纸业/南风股份；MACD首红：1 只（钢研高纳，复盘样本兜底；全市场首红样本今日仅 3 只、其中 2 只位置 64-66% 超 50% 被滤）；
  - 回踩企稳：0 只（hs fine=0 无回踩形态标的，宁缺毋滥，符合「2 只就够、不硬凑」口径）。

## 🔧 批3｜stale 回退路径主线残留修复（2026-08-25 17:20，算法统一性收尾）

- **现象**：科创主线（kc）今日无达标标的 → 回退 08-23 归档时，把归档旧主线（煤炭/通信光模块CPO）带进 mainlines 卡片，
  与沪深/北证（今日已收盘判定「无主线」）不一致——用户反复反馈的「排行/主线打架」在 stale 回退路径的残留。
- **修复**（bj_screener.py）：`_apply_stale_fallback` 合并旧归档 mainlines 前先查 `_daily_mainlines()`——
  当日已定型（含「无主线」）→ 沿用今日判定不回退旧主线；`_reattach_ths` 当日已定型且无主线时清空残留 mainlines。
- **验证**：hs/kc/bj/bj_all 四栏目 mainlines 全部 `[]`、observes 全部一致（种植业/煤炭/通信光模块CPO）；
  kc 仍正确展示「上一交易日筛选（数据日期 2026-08-21）」+ 今日无主线提示；Playwright 桌面+375px 手机零 JS 报错、无横向溢出。

## 📋 待办

- 老板本机验收 `http://127.0.0.1:18001/gd.html` →「10元下」最多 2 只带图；
- 回复「放行」→ 显式 add（bj_screener.py / daily_report.py / main.py / gd.html / bjscreener.js / bjscreener.css / m/gd.html）→ 双推 gitee/origin → deploy03 更新（备份 C:\backup\ai24x_a\，手机版 375px 验收）。

---

# 追加：复盘页「公开简报模式」（2026-08-25 20:50，对齐 markets Daily Brief 体验）

## 🎯 背景与结论

- 老板问「国内版有没有必要加一个类似 markets `daily/` 的页面、对应自己接口算法」；
- 核查结论：国内版已有对应页 `web/daily/index.html`（复盘/大盘，后端 `daily_report.py → /api/report/*`，未登录即返回合规 `public_md` 公开版），**不重复造页**；
- 按专业建议落地 = 现有复盘页升级「工具台 + 公开简报」**双模式**（低成本，复用同一套接口，零新增上游）。

## ✅ 已实现（web/daily/index.html，纯内联改动，未动共享资源）

- **`?brief=1` 公开简报模式**：
  - 只读：隐藏「立即生成/强制重扫」按钮、进度条、日志；**绝不触发扫描**；
  - 日期 chips 页内切换（复用 `/api/report/history` + `/api/report/{date8}`），默认高亮今日/最新一期；
  - 标题改「每日大盘简报 · 信号 · 市场宽度」，文案描述性合规（板块体检/主线锁定标注 VIP 专属）；
  - 未登录 → `public_md` 公开版（大盘信号+市场宽度）；VIP 登录 → 完整报告；已登录用户简报页带「返回工具台」链接；
- **默认（无参数）工具台模式完全不变**：按钮、自动生成/轮询、历史「打开报告 ↗」新窗口照旧；
- 工具台页头新增「📋 公开简报」入口按钮；
- SEO/分享用途：`https://a.ai24x.com/daily/?brief=1` 免登录可见（SW 对 HTML network-first，发布即生效，无需升 sw 版本）。

## 🧪 本地验证（Playwright，18001）

- 匿名 `?brief=1`：标题正确、按钮隐藏、13 个日期 chips、默认高亮今日、点 08-24 chip 内容切换正常；
- VIP 工具台 `/daily/`：按钮/简报入口可见、brief-bar 隐藏、历史列表正常；
- 375px 手机：`docW=bodyW=375` 无横向溢出；桌面+手机零 JS 报错。

## 📋 待办（并入上批一起放行）

- 老板本机验收 `http://127.0.0.1:18001/daily/?brief=1`（公开简报）与 `/daily/`（工具台）；
- 放行后显式 add（本批 + 上批 7 文件）→ 双推 gitee/origin → 03 更新（备份 C:\backup\ai24x_a\）。

## ➕ 追加：gd.html「今日简报」按钮（2026-08-25 晚）

- 桌面版 `web/gd.html`：复盘卡片标题行改 flex，右侧新增「📋 今日简报」按钮 → `/daily/?brief=1`（免费公开简报页），对未登录/免费用户同样可见；无共享资源改动，无需升版本号；
- 手机版 `web/m/gd.html`：顶栏新增绿色「📋 今日简报」胶囊链接（同 `?brief=1`），375px 无横向溢出；
- 验证：桌面按钮可见且跳转正确、手机入口可见、两者零 JS 报错（`_qa_gd_brief_btn_20260825.js` 全 PASS）。

## 📊 国际版 screener 对比结论（待老板拍板，未动手）

- 国际版 `screener.html` 优点：① 技术分大号彩色 /100（≥75 绿/≥60 橙/≥45 蓝/红）；② 形态/风险 chips 分色清晰；③ 顶部模式 chips 单列表切换；④ 卡片 meta 行含 5/20/60 日位置、量比、RSI、52 周；
- 国内 gd.html 现状：7 栏目 tab（信息量≥国际版）、卡片已带迷你 K 线图、支撑压力、形态标签、风险标签、主线/重点观察徽章、策略与资金行——功能更全，但**视觉密度高、评分不突出**；
- 建议（轻量对齐、不重写）：① 卡片评分大号化 + 彩色分级（对齐国际版）；② 卡片信息分层收敛（评分+位置做主角，meta 行 chips 化：位置/量比/RSI/市值/换手）；③ 形态→accent 色、风险→红 chips 视觉分离；保留 7 栏目 tab、合规文案与手机版；前端 js/css 改动需统一升版本号。
- 若老板同意，按上述三条做一版（P2，改动集中在 bjscreener.js + bjscreener.css + gd.html），与「今日简报」按钮一并放行。

## 🔍 追加：科创主线「停留 21 日数据」排查与修复（2026-08-25 晚）

- **根因（非 bug，是回退链 + 文案误导）**：08-22/08-23 为周末，08-23 kc 归档用的是周五 08-21 收盘数据（asof=08-21）；08-24/08-25 kc 扫描均正常完成（08-24 fine=5、08-25 fine=1 强达电路 54 分），但 0 只达标 → 按「宁缺毋滥」不存新归档 → 页面回退展示最近归档（即 08-23 扫描 · 数据截至 08-21 收盘），前端却写成「上一交易日（08-21）」造成"卡在 21 日"错觉；
- **修复**：后端 `_apply_stale_fallback` 等 4 处统一补 `stale_scan_date`（归档扫描日）；前端（bjscreener.js + m/gd.html）「上一交易日」文案全部改为「最近归档（08-23 扫描 · 数据截至 08-21 收盘）」，并在提示里保留"今日无合格（最接近：强达电路 54 分）"透明度；bjscreener.js 版本 103→104；
- **验证**：桌面 kc tab 显示「最近归档筛选（数据日期 2026-08-21 · 2026-08-23 扫描 · 今日无合格标的）」、手机 375px `?market=kc` 同口径、零 JS 报错（`_qa_kc_stale_20260825.js`）；今日简报/每日简报 QA 回归全绿；
- 后端已重启（18011 新 PID 51508，自动拉起）。


## ➕ 追加：近一周涨停「软优先」A 方案（2026-08-25 晚，老板指令"尽可能筛出刚一个涨停的优秀潜力标的"）

### 🎯 结论
- 采纳 A 方案（软优先，不做硬门槛）：**近 5 个交易日内有 1-2 个涨停**的标的，在排序/王者位「同级事件形态内优先置前」，标签「近1周涨停」；不设硬性入选线，避免误杀弱势池里真正强势的票。
- 设计口径：**涨停后 1-5 日回踩窗口**才标记（当日涨停排除 → 防"追板推荐"、与回踩低吸口径一致）；一字板（不可交易强度）排除；5 日内 ≥3 板连板妖股排除（`_zt_cnt5<=2`）；涨停质量由 `limitQuality`（一字/T字/换手/烂板分级）兜底。

### 📊 数据支撑（本地回测 bj_scan_history + 归档，77 个历史主推样本）
- 近一周有涨停 n=10：T+5 均值 **-2.89%**、5日收正率 **30%**、止损率 30%
- 无涨停 n=58：T+5 均值 **-7.82%**、5日收正率 **0%**、止损率 39.7%
- 细分最佳形态：`ztPullback`（涨停级回踩）n=5：T+5 均值 **+1.76%**、5日收正率 60%、止损率 0%——印证"首板回踩企稳"是主升段最暴利窗口（样本小，仅方向性参考）。

### ✅ 已实现
- 后端 `bj_screener.py`：`analyze()` 新增 `patterns["ztWeek"]`；排序接入 `_ztw()`（`_sort_key` / `all_sorted` 电风扇分支 / `leader_final.sort` / 北证 `_king_rank` 0-9 档）——排在 `_evp` 之后、`strongMom` 之前，同级事件形态内"有涨停的强势票"优先；
- 前端 `bjscreener.js`：`coreTags`/`reasonOf`/`runnerTags` 加「近1周涨停」（firstWeek 时不重复）；`web/m/gd.html` 同步；`gd.html` 版本 `bjscreener.js?v=104→v=105`；
- QA 脚本：`_ztweek_unit_test.py`（4/4 通过：潞安环能 08-06 涨停 08-11 标 True/08-18 不标、陕西黑猫 08-21 True、超声电子 False）、`_scan_ztweek_check.js`（hs 实扫跑通，唯一合格票青山纸业 08-25 当日涨停按设计不标）、`_ztweek_winrate_compare.py`（胜率对比）；回归 `_qa_gd_brief_btn` / `_qa_daily_brief` / `_qa_kc_stale` 全绿（kc 断言已改按接口 asof 动态化）。

### 📋 待办
- 与上批一起老板本机验收 → 放行 → 显式 add → 双推 → 03 更新（备份 C:\backup\ai24x_a\）。

## ➕ 追加：主提醒 / 重点提醒栏目标题图标（2026-08-25 晚，老板指令"标题前加些合适图标"）

- 主提醒（AI 精选）标题加 ⭐：桌面「⭐ 今日精选 · 上涨途中+股性活跃（N 只）」、手机「⭐ 今日 AI 筛选」；与顶部流程条「⭐AI 筛选」口径一致；
- 重点提醒（板块排行）标题加 🔍：桌面「🔍 重点观察 · 板块排行」/「🔍 主线 · 重点关注排行」、手机「🔍 板块排行」；与榜内图例「🔍 重点观察」一致；
- 文件：`bjscreener.js`（两处标题串）、`m/gd.html`（静态标题 + 动态刷新点 line879 同步 ⭐）；`gd.html` 版本 `bjscreener.js?v=105→v=106`；
- 验证：桌面+手机 375px 图标渲染 5/5（`_qa_sec_icons_20260825.js`）、今日简报/每日简报/科创数据标签 3 组回归全绿、零 JS 报错、无横向溢出。

## ➕ 追加：daily 简报「往期报告」一直加载中修复（2026-08-25 晚，老板反馈）

- **根因**：`web/daily/index.html` 的 brief 模式（`?brief=1`）在 `setStatus()` 里 `briefBoot()` 后直接 return，`renderHistory()` 从未被调用 → 底部「往期报告（历史归档）」停留静态「加载中…」；接口 `/api/report/history` 本身正常（13 项）。
- **修复**：`briefBoot()` 补调 `renderHistory(s.history || [])`，与顶部日期 chips 同一份数据源；
- **验证**：匿名 + VIP 两种 `?brief=1` 均渲染 13 条「打开报告 ↗」（指向 `/daily/view.html?date=YYYYMMDD`），无「加载中」残留、零 JS 报错（`_qa_brief_history_20260825.js`）；`_qa_daily_brief` 回归全绿。