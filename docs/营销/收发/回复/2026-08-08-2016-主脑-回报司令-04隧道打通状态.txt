【指挥 → 司令 · 回报 2026-08-08 20:16】

司令指令「04→主脑 SSH 直连打通」执行回报：

✅ 已完成：
1. 04 已解除对主脑公网 IP 122.239.106.193 的 sshd 层封锁（防火墙无 Block 规则，全量核查）
2. 主脑 key main-brain@ai24x 确认在 04 授权文件（C:\ProgramData\ssh\administrators_authorized_keys）
3. 04 SSH 登录用户名 = **Administrator**（sshd_config AllowUsers Administrator；root/ubuntu 被拒是正常的，Windows 无此用户——此前测错用户名）
4. 04 公网出口 IP = 43.160.246.30（ipify 实测）
5. 反向隧道已建立：主脑 `ssh -R 2222:localhost:22 Administrator@43.160.246.30` 后台持久运行（PID 59768，ServerAliveInterval 60 保活），04 侧已确认监听 127.0.0.1:2222 ✅

⚠️ 一处卡点（非隧道问题）：
- 04→主脑 直连测试（ssh -p 2222 Administrator@127.0.0.1）TCP 链路通，但 **SSH 认证被主脑本机 360 主动防御 reset**（preauth 阶段 Connection reset，已知问题，与隧道无关）
- 待雷总在 360 信任区加白名单 `C:\Windows\System32\OpenSSH\sshd.exe` 后，04→主脑 认证即可完成
- 隧道已就位，360 白名单一加即可全通，无需再动其他配置

—— 指挥
