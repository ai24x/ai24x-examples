# 回执：20260911 告警列表 / 飞书摘要 / 供应链排版

## 结论
✅ 代码已上 04（health 含 `8f4fb253` / `8ed25f02`）。DeepSeek「假倒挂」已消；预警中心改列表；飞书早晚摘要任务已改通。

## 根因
1. **价格红线**：`ds-v4-flash/pro` 是目录遗留行（L1/L2 已切 MiMo），用 DS 官方成本 + 层倍率误报倒挂。
2. **飞书没了**：`AI24X-Alert-Feishu-PM*` 指向坏掉的启动方式（上次结果 FILE_NOT_FOUND）；已改为 `api\run_feishu_alert_poll.cmd --digest`，默认打 `8002`，同内容 6h 合并，早晚各一次。

## 仍可能看到的 warn
- 「主通道待确认」：真实待办，需在供应链点一键确认，不会静默自动切。

## 说明
- 深夜非 P0 摘要静默（刚试跑被静默属预期）；明早 08:00 会发摘要（有 P0/P1 时）。
