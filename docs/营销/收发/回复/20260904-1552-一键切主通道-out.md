# 一键切主通道

> 2026-09-04｜主打建议卡真正写回路由

## 行为
- **VIP 点名**：`POST /v1/admin/price/apply-hero` → 重排 `vip_rates.channels`（目标置顶，其余兜底）；可选同步账本成本
- **flash/pro/ultra 档位**：仅当目标为 OpenRouter 时，写 `system_flags.token_llm_upstream=openrouter`
- TokenLab/Requesty 对档位不可一键（按钮灰掉并提示走 VIP）

## 管理台
`token-admin` → 供应链监控 → 主打建议卡 / VIP 表「一键切 …」

## 本地
`apply_hero_pick` smoke PASS（vip-glm 写回后已还原）
