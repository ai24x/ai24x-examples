# 04 回执：单点阻塞 P0（sync def + workers）

- **时间**：2026-09-02 21:40+
- **目标提交**：`56d46c2876b2`（sync chat + thread limiter）；生产 HEAD/health=`ec6b15ea7de9`（含部署脚本修复提交）
- **代码**：`api/main.py` + `p/open/api/main.py`
  - `/v1/chat/run`、`/v1/chat/completions`、`/v1/responses` 改为 **sync `def`**（FastAPI 线程池）
  - startup 提升 anyio thread limiter 默认 64
- **NSSM**（直改，硬编码参数，因 nssm get 返回 UTF-16 空字节）：
  - `AI24X-core`：`--workers 4` @ 8002
  - `AI24X-open-api`：`--workers 4` @ 18080
- **验收**：`https://api.ai24x.com/health` → healthy；服务 Running
- **备注**：首次 deploy04_direct 因 PS `$Service:` 解析失败；二次因 NSSM UTF-16 写坏参数后已用 `_fix_nssm_workers_04.ps1` 恢复。指令脚本已改为硬编码 + 去 `\0`。

# ✅ 04更新完成｜单点阻塞P0 sync+workers｜EXP=56d46c2876b2 HEAD=ec6b15ea7de9 health=ec6b15ea7de9｜chat同步线程池+uvicorn×4
