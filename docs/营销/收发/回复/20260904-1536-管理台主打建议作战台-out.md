# 管理台「主打建议」上线说明

> 2026-09-04｜按建议：对内作战台 = 最省钱 × 最稳 × 毛利；对外仍主推档位

## 改动
- `api/price_monitor.py`（同步 `p/open/api/price_monitor.py`）
  - `GET /v1/admin/price/monitor`（及 refresh）响应新增 **`hero_picks`**
  - 合并 `upstream_health.snapshot()`：档位最稳通道 + 熔断列表
- `web/token-admin.html` · 供应链监控页
  - 新增 **主打建议** 卡片（flash/pro/ultra）+ VIP 关注表

## 字段要点
- `hero_picks.tiers[]`：`cheapest` / `most_stable` / `suggest.action|headline|notes`
- `action`：`keep|hero|switch_cheaper|prefer_stable|avoid_unstable|fix_margin`
- 通道名仅管理端；对外产品口径不变

## 本地验收
```text
python -c "from price_monitor import snapshot; ..."  → PASS
打开 token-admin → 供应链监控 → 应见主打建议三卡
```

## 部署
属管理台 + core API；走常规 04 更新（重启 AI24X-core；token-admin 静态随 www/core 静态目录）。
