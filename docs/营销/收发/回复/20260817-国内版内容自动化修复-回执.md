# 回执 · 国内版内容自动化修复（03 复检 4 问题）

日期：2026-08-17 · 行情官 Codex 会话（p\a1）· 未提交 git（按约定交司令验收后 03 部署）

## ✅ 已完成项

### 1. 复盘自动触发（P0）
- 落点：`p\a1\api\server\app\daily_report.py` 新增 `_auto_loop()` / `_ensure_auto_loop()`（对齐 bj_screener 模式）：
  - 交易日（周末+节假日跳过）15:03（`config.json auto_scan_time`）后自动 `_start_scan(force=False, is_vip=True)`，**服务端全量跑，不依赖外部 token**；
  - 防重复：`_AUTO["done_date"]` + `_today_archived_ok()`（今日归档已生成且 asof==今日则跳过）；
  - 带锁：复用 `_busy()`（30 分钟卡死自动复位）；失败冷却：`_auto_blocked()`（10 分钟）。
- 启动：模块导入即启 daemon 线程（已验证 `auto_started=True`）；`/api/report/status` 新增 `auto_loop` 字段便于巡检确认。

### 2. port-check 补监控
- 新增 `scripts\port-check.bat` + `scripts\port-check.ps1`：覆盖 **18001 / 18011 / 18013 / 8001 / 8002 / 18012**，任一未监听即红色告警并 exit 1。本地实测运行正常（当前全端口 down，符合 03 结论）。
- 注：本仓库原无 `port-check.bat`（03 报告提到的应是 03 机器上文件），本机侧以 `scripts\` 新增脚本为准。

### 3. 分享卡 token 动态化
- `make_share_card.js` / `make_share_card_bj.js`（`p\a1\调研报告\03-个股深度研究\2026-08-07-北证潜力股推荐\工具\`）：已确认读取顺序为 `AI24X_TOKEN` 环境变量 → 同目录 token.txt 兜底；**新增 JWT exp 过期自检**——过期即告警提示改用环境变量或更新 token.txt（避免静默生成无登录态分享卡）。两文件 `node --check` 通过。
- `.gitignore` 新增 `**/工具/token.txt` + `**/token.txt`（不再把新 token 提交入库）。

### 4. 补跑能力（8/14）
- `daily_report.py` 的 `run_daily` 新增 `d8` 参数 + 按日缓存目录（`_CACHE_D8`）+ CLI：`python -m app.daily_report --backfill 20260814 --force`（编译/导入已验证）。
- 已核对归档现状：`板块主攻研判\20260812` 实际 7 文件齐全（report+4 JSON+mainlines+ths）；**20260814 缺失**属实。

## ⚠️ 问题项
- **8/14 补跑本地无法执行**：本沙箱外网被禁（腾讯/东财 socket 拒绝），且本机 18001/18011/18013/8001/8002/18012 全未运行 → 拉不到 8/14 真实数据。请司令/03 在 03 机器执行：
  `cd p\a1\api\server && python -m app.daily_report --backfill 20260814 --force`（完成后应生成 `板块主攻研判\20260814\{report.md,report.html,index_signal.json,plate_data.json,sector_score.json,breadth.json,ths_sentiment.json,mainlines.json}`）。
- **token.txt 已入库需摘除**：6 份 `工具\token.txt` 已被 git 跟踪（含 8-13 过期 token）。司令提交时可执行（勿删文件本体）：
  `git rm --cached "p/a1/调研报告/01-事件驱动研究/2026-08-06-DeepSeek涨价受益标的/工具/token.txt" "p/a1/调研报告/01-事件驱动研究/2026-08-07-DeepSeek涨价低风险优选/工具/token.txt" "p/a1/调研报告/02-行业研究/2026-08-06-大盘底金信号板块主攻研判/工具/token.txt" "p/a1/调研报告/03-个股深度研究/2026-08-07-PCB潜力龙头调研/工具/token.txt" "p/a1/调研报告/03-个股深度研究/2026-08-07-北证潜力股推荐/工具/token.txt" "p/a1/调研报告/03-个股深度研究/2026-08-07-煤炭潜力龙头调研/工具/token.txt"`
- 附带发现：`p\a1\daily_report\config.json` 内也有明文 token（旧逻辑，`cfg()` 已 `pop` 不使用），建议 03 部署时顺手移除该字段。
- 未提交 git、未部署（按约定交司令验收）。

## 部署建议
- 03 更新：`p/a1/api/server/app/daily_report.py` + 分享卡 2 个 JS + `.gitignore` + `scripts/port-check.*`；重启 18011（`Stop-Process 旧 PID` → `Start-Process python -m uvicorn app.main:app --host 127.0.0.1 --port 18011`，workdir `p\a1\api\server`）后验证 `/api/report/status` 返回 `auto_loop: true`。
