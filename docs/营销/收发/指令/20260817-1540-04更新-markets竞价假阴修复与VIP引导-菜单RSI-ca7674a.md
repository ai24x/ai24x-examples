# 【04 更新】Markets 竞价K线假大阴修复 + 菜单去行情官 + RSI 移除 + VIP 订阅引导 · 目标 ca7674a

> 通道：司令直连 04（deploy04.ps1 同步 600s）。验收全 PASS 后自动发指挥部群回执；有问题先回传司令，不发群。

## 背景
- 老板反馈（2026-08-17 上午集合竞价）：搜索 上证指数/深证成指，K 线柱子和 MACD 量能柱子都是大阴。根因：竞价/未开盘时上游返回未成形当日 bar（OHLC=0 或撮合价跳动），被缓存并渲染成假大阴。
- 同时：顶部菜单栏要去掉「行情官」、保留「首页」；首页专业图表栏目去掉 RSI；用户进来后开通行情官 VIP 订阅没引导好，账户充值栏目较乱（Token 充值与 Markets Pro 两套混在一起），app 页升级衔接乱。
- 本轮改动（ca7674acfaaa）：
  - **providers_us.py 竞价修复**：东财 `_drop_auction_today_bar`（交易日 09:30 前剔除当日残缺 bar，09:30 后仅剔除非法 OHLC）；腾讯路径 `_drop_bad_intraday_bar` 剔除当日占位 bar；cn 代码缓存 TTL 分级（无当日 300s / 当日有效 60s / 当日异常 30s），缓存内坏 bar 很快过期重拉；**两源故障/熔断时静默探测候选代码**（WETOUR→WETO 建议不再因腾讯熔断丢失），候选命中仍提示 symbol not found + did you mean。
  - **markets 首页 index.html**：hero 加「Upgrade to Pro →」（/app.html#sub）；How it works 加「Go Pro in 3 steps」引导卡（$24.9/month、$199/year、PayPal）；全页 RSI 文案移除。
  - **markets app.html**：登录免费用户顶部出现可关闭的升级引导条（3 次 AI 点评/天、自选 3 只 → Pro 无限点评/50 只），点 Upgrade 平滑滚动到 #sub；订阅面板加「① 选套餐 → ② PayPal 支付 → ③ 立即生效」步骤说明。
  - **console（www）**：Overview 的 Markets 卡片显示 Pro 订阅状态（免费/Pro 到期）+ 升级/管理入口；Billing 面板顶部加「两套体系」横幅（Token 充值与 Markets Pro 独立订阅区分）；markets 后端 CORS 放行 https://www.ai24x.com 与本地 8000，console 可直连查询订阅状态。
  - **shell.js**：顶部菜单 markets 链接改回 `index.html` + `nav.home`（首页保留，去掉「行情官」）。
  - **版本号**：shell.js/locales.js/console.js 内容变更 → 全站 43 页统一升 `v=20260818f`（旧版残留 0）。
  - QA：新增 `_qa_vip_guide_20260817.js`（24 项）、`_qa_console_vip_20260817.js`（6 项）、`_qa_auction_fix.py`（15 项单元测试）；`_qa_cn_index` 断言改环境自适应（未开盘无当日 bar / 盘中当日 bar OHLC 合法）；`_qa_opt` 22/22、`_qa_symbol_ui` 7/7 全过（WETOUR→WETO 建议恢复正常）。

## 注意事项
- 本批动 **markets 后端 py + markets 静态 + www web/**：markets 静态同步 `C:\sites\markets.ai24x.com`（app.html + index.html，先备份 .bak）；重启 **AI24X-markets-api(18012)**；www 由仓库 web/ 提供，git pull 后重启 **AI24X-core(8002)**。
- HEAD 校验用「目标前缀 + merge-base + 关键路径 diff --quiet 与目标一致」（指令文档自身也是新提交）。
- 所有 Invoke-WebRequest/-RestMethod 带 `-UseBasicParsing`；PowerShell 字符串断言用单引号。
- 版本号断言：`locales\.js\?v=20260818f`、`shell\.js\?v=20260818f`、`console\.js\?v=20260818f`（`?` 用 `\.\?` 或 -SimpleMatch，勿裸用正则）。

## 副脑04 执行脚本（整段复制运行）
```powershell
# === AI24X 更新部署验收 · 目标 ca7674a ===
$ErrorActionPreference = "Stop"
$REPO = "C:\ai24x01"
$SRV_MK  = "AI24X-markets-api"
$SRV_WWW = "AI24X-core"
$EXP  = "ca7674acfaaa"
$SITES = "C:\sites\markets.ai24x.com"
$TAG = "20260817-ca7674a"

Set-Location $REPO

"--- 0) 本批路径如有本地改动：备份后 checkout ---"
$keyFiles = @(
  "p/markets/api/server/app/main.py",
  "p/markets/api/server/app/providers_us.py",
  "p/markets/web/app.html",
  "p/markets/web/index.html",
  "web/js/shell.js",
  "web/js/console.js",
  "web/config/locales.js",
  "web/console.html"
)
foreach ($f in $keyFiles) {
    $dirty = git status --short -- $f
    if ($dirty) {
        $bn = ($f -replace "/", "_")
        Copy-Item "$REPO\$f" "C:\Users\Administrator\ops\dirty-$bn-$TAG" -Recurse -Force
        git checkout -- $f
        if ($LASTEXITCODE -ne 0) { Write-Host "!! checkout 失败: $f" -ForegroundColor Red; exit 1 }
        "dirty backup + checkout: $f"
    } else {
        "clean: $f"
    }
}
# 43 页 html 版本号（web/*.html）：有本地改动同样备份 + checkout
git status --porcelain -- web/*.html | ForEach-Object {
    $p = ($_.Substring(3)).Trim()
    if ($p) {
        $bn = ($p -replace "/", "_")
        Copy-Item "$REPO\$p" "C:\Users\Administrator\ops\dirty-$bn-$TAG" -Force
        git checkout -- $p
        "dirty backup + checkout: $p"
    }
}

"--- 1) 拉取并校验目标提交 ---"
git pull
$HEAD = (git rev-parse --short=12 HEAD).Trim()
"HEAD=$HEAD"
git merge-base --is-ancestor $EXP HEAD
if ($LASTEXITCODE -ne 0) { Write-Host "!! 目标 $EXP 不在 HEAD 历史中" -ForegroundColor Red; exit 1 }
git diff --quiet $EXP HEAD -- p/markets/api/server/app/main.py p/markets/api/server/app/providers_us.py p/markets/web/app.html p/markets/web/index.html web/js/shell.js web/js/console.js web/config/locales.js web/console.html
if ($LASTEXITCODE -ne 0) { Write-Host "!! 关键路径在 $EXP 之后仍有改动，停止" -ForegroundColor Red; exit 1 }
"target_verified=ok"

"--- 2) 同步 markets 静态到 nginx 服务目录（先备份）---"
foreach ($f in @("app.html", "index.html")) {
    if (Test-Path "$SITES\$f") { Copy-Item "$SITES\$f" "$SITES\$f.bak-$TAG" -Force }
    Copy-Item "$REPO\p\markets\web\$f" "$SITES\$f" -Force
}
$sa = Get-Content "$SITES\app.html" -Raw -Encoding UTF8
$si = Get-Content "$SITES\index.html" -Raw -Encoding UTF8
"sites_app_free_banner=$($sa.Contains('free-banner'))"
"sites_app_sub_steps=$($sa.Contains('sub.steps'))"
"sites_index_go_pro=$($si.Contains('Go Pro in 3 steps'))"
"sites_index_no_rsi=$(-not $si.Contains('RSI'))"
if (-not ($sa.Contains('free-banner') -and $si.Contains('Go Pro in 3 steps') -and -not $si.Contains('RSI'))) { Write-Host "!! sites 静态文件内容缺失" -ForegroundColor Red; exit 1 }

"--- 3) 重启 markets (18012) + core (8002) ---"
Restart-Service $SRV_MK -Force
Restart-Service $SRV_WWW -Force
$okM = $false
for ($i = 0; $i -lt 15; $i++) {
    Start-Sleep -Seconds 5
    try { $hm = Invoke-RestMethod -Uri "http://127.0.0.1:18012/health" -TimeoutSec 10 -UseBasicParsing; $okM = $true; break } catch {}
}
if (-not $okM) { Write-Host "!! markets /health 未就绪" -ForegroundColor Red; exit 1 }
"markets-health=$($hm.status)"
$okW = $false
for ($i = 0; $i -lt 12; $i++) {
    Start-Sleep -Seconds 5
    try { $hw = Invoke-RestMethod -Uri "http://127.0.0.1:8002/health" -TimeoutSec 10 -UseBasicParsing; $okW = $true; break } catch {}
}
if (-not $okW) { Write-Host "!! core /health 未就绪" -ForegroundColor Red; exit 1 }
"www-health=$($hw.status) commit=$($hw.commit)"
if ($hw.commit -notlike "$EXP*") { Write-Host "!! core 服务未生效 commit=$($hw.commit)" -ForegroundColor Red; exit 1 }

"--- 4) 本地验收：竞价修复 / WETOUR 建议 / CORS / 版本号 ---"
$kl = Invoke-RestMethod -Uri "http://127.0.0.1:18012/api/signals?symbol=sh000001&period=day&count=120" -TimeoutSec 40 -UseBasicParsing
$last = $kl.data.candles[$kl.data.candles.Count - 1]
"local_sse_bars=$($kl.data.candles.Count) asof=$($kl.data.meta.asof) last=$($last[0]) O=$($last[1]) C=$($last[2]) H=$($last[3]) L=$($last[4])"
$lastOk = ($last.Count -ge 5 -and [double]$last[1] -gt 0 -and [double]$last[2] -gt 0 -and [double]$last[3] -gt 0 -and [double]$last[4] -gt 0 -and [double]$last[3] -ge [double]$last[4])
if ($kl.code -ne 0 -or $kl.data.candles.Count -lt 100 -or -not $lastOk) { Write-Host "!! 上证 K线/当日bar 验收失败" -ForegroundColor Red; exit 1 }

$kz = Invoke-RestMethod -Uri "http://127.0.0.1:18012/api/signals?symbol=sz399001&period=day&count=120" -TimeoutSec 40 -UseBasicParsing
"local_sz_bars=$($kz.data.candles.Count) asof=$($kz.data.meta.asof)"
if ($kz.code -ne 0 -or $kz.data.candles.Count -lt 100) { Write-Host "!! 深成指 K线验收失败" -ForegroundColor Red; exit 1 }

$wt = Invoke-RestMethod -Uri "http://127.0.0.1:18012/api/signals?symbol=WETOUR&period=day&count=120" -TimeoutSec 40 -UseBasicParsing
"local_wetour_msg=$($wt.msg) suggested=$($wt.suggested)"
if ($wt.code -ne -1 -or $wt.msg -notlike 'symbol not found*' -or $wt.suggested -ne 'WETO') { Write-Host "!! WETOUR→WETO 建议验收失败" -ForegroundColor Red; exit 1 }

$cors = Invoke-WebRequest -Uri "http://127.0.0.1:18012/api/subscribe/status" -Headers @{ Origin = "https://www.ai24x.com" } -UseBasicParsing -ErrorAction SilentlyContinue
"local_cors_acao=$($cors.Headers['Access-Control-Allow-Origin'])"
if ($cors.Headers['Access-Control-Allow-Origin'] -ne 'https://www.ai24x.com') { Write-Host "!! CORS 验收失败" -ForegroundColor Red; exit 1 }

$vc = Get-Content "$REPO\web\console.html" -Raw -Encoding UTF8
$vx = Get-Content "$REPO\web\index.html" -Raw -Encoding UTF8
$vs = Get-Content "$REPO\web\js\shell.js" -Raw -Encoding UTF8
"version_console_f=$($vc.Contains('console.js?v=20260818f'))"
"version_shell_f=$($vx.Contains('shell.js?v=20260818f') -and $vx.Contains('locales.js?v=20260818f'))"
"version_nav_home=$($vs.Contains('nav(""index.html"", ""nav.home"", ""home"")') -and -not $vs.Contains('"nav.markets"'))"
if (-not ($vc.Contains('console.js?v=20260818f') -and $vx.Contains('shell.js?v=20260818f') -and $vx.Contains('locales.js?v=20260818f') -and $vs.Contains('nav(""index.html"", ""nav.home"", ""home"")'))) { Write-Host "!! 版本号/导航验收失败" -ForegroundColor Red; exit 1 }

"--- 5) 公网验收 ---"
$pubK = Invoke-RestMethod -Uri "https://markets.ai24x.com/api/signals?symbol=sh000001&period=day&count=120" -TimeoutSec 40 -UseBasicParsing
$pubLast = $pubK.data.candles[$pubK.data.candles.Count - 1]
$pubLastOk = ($pubLast.Count -ge 5 -and [double]$pubLast[1] -gt 0 -and [double]$pubLast[2] -gt 0 -and [double]$pubLast[3] -ge [double]$pubLast[4])
"pub_sse_bars=$($pubK.data.candles.Count) asof=$($pubK.data.meta.asof) lastOk=$pubLastOk"
$pubW = Invoke-RestMethod -Uri "https://markets.ai24x.com/api/signals?symbol=WETOUR&period=day&count=120" -TimeoutSec 40 -UseBasicParsing
"pub_wetour_suggest=$($pubW.suggested)"
$pubA = Invoke-WebRequest -Uri "https://markets.ai24x.com/app.html?x=wca7674a" -TimeoutSec 25 -UseBasicParsing
$pubI = Invoke-WebRequest -Uri "https://markets.ai24x.com/?x=wca7674a" -TimeoutSec 25 -UseBasicParsing
"pub_app_free_banner=$($pubA.Content.Contains('free-banner')) pub_index_go_pro=$($pubI.Content.Contains('Go Pro in 3 steps')) pub_index_no_rsi=$(-not $pubI.Content.Contains('RSI'))"
$pubC = Invoke-WebRequest -Uri "https://www.ai24x.com/console.html?x=wca7674a" -TimeoutSec 25 -UseBasicParsing
$pubH = Invoke-WebRequest -Uri "https://www.ai24x.com/index.html?x=wca7674a" -TimeoutSec 25 -UseBasicParsing
"pub_console_vf=$($pubC.Content.Contains('console.js?v=20260818f') -and $pubC.Content.Contains('markets-sub-line')) pub_index_vf=$($pubH.Content.Contains('shell.js?v=20260818f') -and $pubH.Content.Contains('locales.js?v=20260818f'))"

if ($kl.code -eq 0 -and $lastOk -and $kz.code -eq 0 -and $wt.suggested -eq 'WETO' -and $cors.Headers['Access-Control-Allow-Origin'] -eq 'https://www.ai24x.com' -and $pubK.code -eq 0 -and $pubLastOk -and $pubW.suggested -eq 'WETO' -and $pubA.Content.Contains('free-banner') -and $pubI.Content.Contains('Go Pro in 3 steps') -and -not $pubI.Content.Contains('RSI') -and $pubC.Content.Contains('console.js?v=20260818f') -and $pubH.Content.Contains('shell.js?v=20260818f')) {
    "--- 全部 PASS ---"
    Write-Host "=== 部署成功 OK ===" -ForegroundColor Green
} else {
    Write-Host "!! 验收未全通过" -ForegroundColor Red; exit 1
}
```

## 完成后群回执（固定步骤，主通道必做）
- 用 04 应用发指挥部群：
  `python api\feishu_notify.py --config <openclaw.json> --group "【AI24X 部署回执】Markets 竞价假阴修复 + 菜单/RSI/VIP引导 ca7674a 成功：HEAD=ca7674a、markets/core health OK、上证/深成当日K线 OHLC 合法无假大阴、WETOUR→WETO 建议恢复、CORS www 放行、markets 首页去RSI+Go Pro引导、app 免费用户升级引导条、console 两套体系区分+订阅状态、全站版本号 20260818f（43页无残留）、公网验收通过。问题项：无" --at`

## 回执格式（统一）
- ✅ 已完成项：HEAD / checkout / pull / sites 静态同步 / markets+core 重启 / 上证/深成 K线当日bar / WETOUR 建议 / CORS / 菜单首页 / RSI 移除 / app+首页+console VIP 引导 / 版本号 20260818f / 公网验收 / 群回执
- ⚠️ 问题项：逐条列出（无则写「无」）

## 回滚预案
- `git revert ca7674a` + 重启 markets(18012) + core(8002)；markets 静态恢复 `C:\sites\markets.ai24x.com\app.html.bak-20260817-ca7674a` / `index.html.bak-20260817-ca7674a`。
