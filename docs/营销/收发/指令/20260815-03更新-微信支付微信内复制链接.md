# 【03 更新指令】AI行情官 a1 · 目标提交 3fe250b9145e（2026-08-15）

> 派发：司令直连 03（scripts\deploy03.ps1）
> 03 主机：运维CN 123.207.199.238 · 仓库 C:\ai24x01 · 服务 AI24X-a1-api（NSSM，端口 8001，域名 a.ai24x.com）· 库 ai24x_a_cn（PG 127.0.0.1:5432）
> 03 无 D 盘；a1 /health 无 commit 字段：验收用 进程新建时间 + 行为验收
> 本批无数据库结构变更，跳过 pg_dump；如需回滚用 git revert 3fe250b9145e + 重启服务

## 目标提交
- 期望 HEAD（短12位）：3fe250b9145e

## 本批改动（微信支付 · 微信内改用 Native 二维码+复制链接）
1. startWechatNative：微信内置浏览器默认改走 /api/billing/wechat/native（二维码 + 复制付款链接），不再直跳 H5 收银台，规避「H5 支付 NO_AUTH（商户权限预开通中）」
2. 新增后台开关 wechat_h5_enabled（默认关闭）：为 1 时才直跳 H5，H5 失败自动降级 Native；/api/public/billing/plans 下发该字段
3. /api/billing/wechat/h5 未开通时返回 503 友好提示
4. 微信内弹窗文案：复制链接 → 粘贴到任意聊天/文件传输助手 → 点击打开即可拉起支付；本地持久化订单号，刷新/回页可恢复检测

## 执行步骤（PowerShell，分步）
~~~powershell
$ErrorActionPreference = "Stop"
$REPO = "C:\ai24x01"
$SRV  = "AI24X-a1-api"
$EXP  = "3fe250b9145e"

# 1/4 拉码 + HEAD 校验（先两次 ls-remote 防远端变动）
Set-Location $REPO
$r1 = git ls-remote origin master
$r2 = git ls-remote origin master
if ($r1 -ne $r2) { Write-Host "!! 远端引用在变动，稍后重试" -ForegroundColor Red; exit 1 }
git pull
$HEAD = (git rev-parse --short=12 HEAD).Trim()
"HEAD=$HEAD"
if ($HEAD -ne $EXP) { Write-Host "!! HEAD 不匹配，期望 $EXP 实际 $HEAD" -ForegroundColor Red; exit 1 }

# 2/4 重启服务
nssm restart $SRV
Start-Sleep -Seconds 5

# 3/4 验收（进程新建时间）
$ok = $false
for ($i = 0; $i -lt 12; $i++) {
    Start-Sleep -Seconds 5
    try { $h = Invoke-RestMethod -UseBasicParsing -Uri "http://127.0.0.1:8001/health" -TimeoutSec 10; $ok = $true; break } catch {}
}
if (-not $ok) { Write-Host "!! /health 未就绪" -ForegroundColor Red; exit 1 }
"health_ok=$($h.ok)"
$svc = Get-CimInstance Win32_Service -Filter "Name='AI24X-a1-api'"
$p = Get-Process -Id $svc.ProcessId
"svc_started=$($svc.State) process_start=$($p.StartTime)"

# 4/4 行为验收
$plans = Invoke-RestMethod -UseBasicParsing -Uri "http://127.0.0.1:8001/api/public/billing/plans" -TimeoutSec 15
"wechat_h5_enabled=$($plans.pay_methods.wechat_h5_enabled)"
try { $h5 = Invoke-WebRequest -UseBasicParsing -Uri "http://127.0.0.1:8001/api/billing/wechat/h5" -Method POST -Body '{"plan":"vip_month"}' -ContentType "application/json" -TimeoutSec 10; "h5_route=$($h5.StatusCode)" } catch { "h5_route=$($_.Exception.Response.StatusCode.value__) (401=路由存活，需登录)" }
$page = Invoke-WebRequest -UseBasicParsing -Uri "https://a.ai24x.com/account.html" -TimeoutSec 15
"page_has_flag=$($page.Content -match 'wechat_h5_enabled')"
"page_has_tip=$($page.Content -match '拉起微信支付')"
Write-Host "=== 部署成功 OK ===" -ForegroundColor Green
~~~

## 完成后回执
- 已完成项：HEAD / health / 进程新建时间 / wechat_h5_enabled=false / h5_route / 公网 page_has_flag / page_has_tip
- 问题项：逐条列出（无则写「无」）