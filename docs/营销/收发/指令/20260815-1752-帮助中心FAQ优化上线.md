# 【部署指令】帮助中心/接口页 FAQ 优化上线（commit e48e38f）+ 全站资源版本统一 20260816y

> 发件：司令 → 副脑04 ｜ 类型：标准更新部署（纯静态，无需重启 core）
> 背景：雷总要求对照 DeepSeek FAQ（API 相关）优化帮助中心与接口说明页，并补充「工单=系统私信」说明。本地已渲染验证（中英双语、无 JS 报错）。

## 一、本次改动（纯前端，无后端变更）
1. `web/help.html`：FAQ 9 条 → 16 条，新增 余额有效期/到账核对/退款/发票/Key 泄漏处置/按 Key 查用量/流式空行 keep-alive；「工单=与系统的私信」说明；FAQPage 结构化数据 4→7 条；meta 更新
2. `web/docs.html`：接口地址补全三端点（/v1/chat/run + /v1/chat/completions + /v1/responses）、鉴权说明；新增「接口行为与常见错误」5 卡（keep-alive/400/401/429/500）
3. `web/config/locales.js`：zh/en 新增对应键；**全站共享资源 ?v= 统一 20260816y（40 页）**——同时补齐上批峰谷仅提交 4 页、其余 36 页版本号未入库的半成品，消除缓存串味风险

## 二、执行步骤（整段复制 PowerShell，仅拉取+校验，不重启服务）

```powershell
# === AI24X 更新部署验收 · 目标 e48e38f ===
$ErrorActionPreference = "Stop"
$REPO = "C:\ai24x01"
$EXP  = "e48e38f"

$r1 = (git ls-remote origin master | Select-Object -First 1)
Start-Sleep -Seconds 1
$r2 = (git ls-remote origin master | Select-Object -First 1)
if ($r1 -ne $r2) { Write-Host "!! 远端引用变动，稍后重试" -ForegroundColor Red; exit 1 }
Set-Location $REPO
git status --porcelain
if ((git status --porcelain).Count -gt 0) { Write-Host "!! 仓库有本地改动，先 stash 保护再 pull" -ForegroundColor Yellow; git stash push -m "deploy-e48e38f-protect" }
git pull
$HEAD = (git rev-parse --short=12 HEAD).Trim()
"HEAD=$HEAD"
if ($HEAD -notlike "$EXP*") { Write-Host "!! HEAD 不匹配，期望前缀 $EXP 实际 $HEAD" -ForegroundColor Red; exit 1 }
if ((git stash list | Select-String "deploy-e48e38f-protect")) { Write-Host "恢复本地改动" -ForegroundColor Yellow; git stash pop }

# —— 静态页面内容验收（本地文件）——
$help = Get-Content "$REPO\web\help.html" -Raw
$docs = Get-Content "$REPO\web\docs.html" -Raw
$loc  = Get-Content "$REPO\web\config\locales.js" -Raw
"help_q16=$($help -match 'page.help.q16t') ticket_lead=$($help -match 'page.help.ticket.lead')"
"docs_behavior=$($docs -match 'page.docs.behavior') docs_b5=$($docs -match 'page.docs.b5t')"
"locales_key=$($loc -match 'page.help.q16t')"
if ($help -notmatch 'page.help.q16t' -or $help -notmatch 'page.help.ticket.lead' -or $docs -notmatch 'page.docs.behavior' -or $loc -notmatch 'page.help.q16t') {
  Write-Host "!! 页面内容未就位" -ForegroundColor Red; exit 1 }

# —— 公网版本号抽查（help/docs + 一个未提交过的新页）——
$h = (Invoke-WebRequest -Uri "https://www.ai24x.com/help.html" -UseBasicParsing -TimeoutSec 20).Content
$d = (Invoke-WebRequest -Uri "https://www.ai24x.com/docs.html" -UseBasicParsing -TimeoutSec 20).Content
$i = (Invoke-WebRequest -Uri "https://www.ai24x.com/index.html" -UseBasicParsing -TimeoutSec 20).Content
"pub_help_v=$($h -match 'locales\.js\?v=20260816y') pub_docs_v=$($d -match 'locales\.js\?v=20260816y') pub_index_v=$($i -match 'locales\.js\?v=20260816y')"
if ($h -notmatch 'locales\.js\?v=20260816y' -or $d -notmatch 'locales\.js\?v=20260816y' -or $i -notmatch 'locales\.js\?v=20260816y') {
  Write-Host "!! 公网版本号未生效（缓存？先 Ctrl+F5/无痕验证）" -ForegroundColor Red; exit 1 }

# —— core 健康兜底（未改动后端，仅确认无回归）——
$pub = Invoke-RestMethod -Uri "https://api.ai24x.com/health" -TimeoutSec 15 -UseBasicParsing
"public_health=$($pub.status) commit=$($pub.commit)"
if ($pub.status -ne "healthy") { Write-Host "!! 公网 health 异常" -ForegroundColor Red; exit 1 }
Write-Host "=== 部署成功 ===" -ForegroundColor Green
```

## 三、群回执（部署通过后立即发，固定步骤）
- 用 04 应用发指挥部群：`python api\feishu_notify.py --config <openclaw.json> --group "【AI24X 部署回执】e48e38f 帮助中心/接口页 FAQ 优化上线：HEAD 匹配；help.html 16 条 FAQ+工单私信说明、docs.html 三端点+常见错误 5 卡就位；全站共享资源 v=20260816y 统一（40 页，消除上一批版本号半成品串味）；core health 正常；纯静态未重启" --at`
- 格式：✅ 已完成项 / ⚠️ 问题项（无则写「无」）

## 四、回执时机规则
- 全部 PASS → 直接发群回执
- 任一问题项/失败 → **先回传司令，不发群**；司令处理后给下一步

## 五、回滚预案
- `git revert e48e38f` 即可回滚本包（纯静态，无需重启 core；浏览器缓存随版本号自然刷新）
