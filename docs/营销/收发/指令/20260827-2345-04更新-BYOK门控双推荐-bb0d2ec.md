# 【04 更新】BYOK 免费门控 + Value/Scale 双推荐（bb0d2ec）

> 通道：司令直连 04（deploy04.ps1）｜ 目标提交 **bb0d2ec**  
> 04 主机：43.160.246.30 · 仓库 `C:\ai24x01`  
> 回执：`docs/营销/收发/回复/20260827-2345-04更新-BYOK门控双推荐-bb0d2ec-out.md`；有异常才上报  
> **禁止**：改价、改 Dodo product_id、轮换密钥、`git add -A` / `reset --hard`  
> **env**：可用行级确认 `BYOK_ENFORCE_FREE_CAP`（默认代码已 True；若 `.env` 显式 `=0` 请改为 `1`，**勿整文件覆盖 .env**）

# 需求类型：②需副脑办(副脑04)
# 需求描述：部署 BYOK 免费月请求硬门控 + 托管双推荐高亮 + 文案对齐
# 验收标准：见下文
# 期望时间：立即

---

## 变更要点

### A. BYOK 门控（open-api，需重启）
- 免费档：每月默认 **1000** 次 BYOK 请求；超限返回 **402**，**不**静默回落平台路由
- Pro 订阅：不限次
- 默认 `BYOK_ENFORCE_FREE_CAP=True`（代码）；生产若 `.env` 写了 `0` 须改为 `1`
- 控制台展示剩余/超限提示与 Pro 不限次

### B. 托管推荐（www + open 静态）
- Value Pack → **Best start / 入门首选**
- Scale → **Best for named models / 名模首选**
- 排序 Value → Scale 靠前

### C. 文案
- BYOK 免费/Pro 对齐真实能力（路由/缓存免费可用；Pro 解除请求上限 + 年付省 17%）

---

## 执行步骤（04 复制运行）

```powershell
$ErrorActionPreference = "Stop"
$work = "C:\ai24x01"
$EXP  = "bb0d2ec"
Set-Location $work

git status -sb | Select-Object -First 15
git fetch origin
git pull --ff-only origin master
if ($LASTEXITCODE -ne 0) {
  git fetch gitee
  git pull --ff-only gitee master
}
$HEAD = (git rev-parse --short=12 HEAD).Trim()
"HEAD=$HEAD"
git merge-base --is-ancestor $EXP HEAD
if ($LASTEXITCODE -ne 0) { throw "HEAD 不含 $EXP" }

# 关键文件
@(
  "p\open\api\byok.py",
  "p\open\api\services.py",
  "p\open\api\config.py",
  "web\js\console.js",
  "api\token_plans.py"
) | ForEach-Object {
  if (-not (Test-Path $_)) { throw "缺 $_" }
}

# 内容标记
$byok = Get-Content "p\open\api\byok.py" -Raw -Encoding UTF8
if ($byok -notlike "*ByokEntitlementError*") { throw "缺 ByokEntitlementError" }
if ($byok -notlike "*assert_byok_entitlement*") { throw "缺 assert_byok_entitlement" }
$tp = Get-Content "api\token_plans.py" -Raw -Encoding UTF8
if ($tp -notlike "*Best start*") { throw "缺 Best start 推荐标" }

# env 行级确认（禁止 Cursor Write / 整文件覆盖；仅改这一键）
$envPath = "C:\ai24x01\p\open\api\.env"
$utf8 = New-Object System.Text.UTF8Encoding $false
if (Test-Path $envPath) {
  $raw = [System.IO.File]::ReadAllText($envPath)
  if ($raw -match '(?m)^\s*BYOK_ENFORCE_FREE_CAP\s*=\s*0\b') {
    $raw2 = [regex]::Replace($raw, '(?m)^\s*BYOK_ENFORCE_FREE_CAP\s*=.*$', 'BYOK_ENFORCE_FREE_CAP=1')
    [System.IO.File]::WriteAllText($envPath, $raw2, $utf8)
    "env flipped BYOK_ENFORCE_FREE_CAP 0→1"
  } elseif ($raw -notmatch '(?m)^\s*BYOK_ENFORCE_FREE_CAP\s*=') {
    $nl = if ($raw.EndsWith("`n") -or $raw.EndsWith("`r`n")) { "" } else { "`n" }
    [System.IO.File]::AppendAllText($envPath, "${nl}BYOK_ENFORCE_FREE_CAP=1`n", $utf8)
    "env appended BYOK_ENFORCE_FREE_CAP=1"
  } else {
    "env BYOK_ENFORCE_FREE_CAP already set (leave as-is if 1)"
  }
} else {
  Write-Host "无 p/open/api/.env，依赖代码默认 True" -ForegroundColor Yellow
}

# 重启 open-api（BYOK 在此进程）
$svcNames = @("AI24X-open-api", "AI24X-open", "ai24x-open-api")
$restarted = $false
foreach ($n in $svcNames) {
  $s = Get-Service -Name $n -ErrorAction SilentlyContinue
  if ($s) {
    Restart-Service $n -ErrorAction Stop
    Start-Sleep -Seconds 6
    "restarted $n"
    $restarted = $true
    break
  }
}
if (-not $restarted) {
  # PM2 兜底
  pm2 restart open-api --update-env 2>$null
  if ($LASTEXITCODE -ne 0) { pm2 list }
  Write-Host "已尝试 pm2；请确认 open 监听进程已吃到新代码" -ForegroundColor Yellow
}

# 可选：core 静态/推荐目录也经 api 时重启
try { Restart-Service AI24X-core -ErrorAction SilentlyContinue; Start-Sleep -Seconds 5 } catch {}

# 公网抽检
$h = Invoke-RestMethod -Uri "https://api.ai24x.com/health" -TimeoutSec 20
"health commit=$($h.commit)"

# 静态：www console 脚本版本戳
$www = (Invoke-WebRequest -UseBasicParsing "https://www.ai24x.com/console.html").Content
if ($www -notlike "*console.js?v=20260827*") { Write-Host "www console 缓存戳可能未更新（CDN？）" -ForegroundColor Yellow }

$op = (Invoke-WebRequest -UseBasicParsing "https://open.ai24x.com/console.html").Content
if ($op -notlike "*20260827e*" -and $op -notlike "*20260827d*") {
  Write-Host "open console 缓存戳检查放宽通过/失败见人工" -ForegroundColor Yellow
}

Write-Host "✅ bb0d2ec BYOK gate + dual recommend deployed" -ForegroundColor Green
```

---

## 回滚

1. `git checkout 28cd44c -- p/open/api/byok.py p/open/api/services.py p/open/api/config.py p/open/api/byok_plans.py api/token_plans.py web/ p/open/web/`
2. 若改过 `.env`：`BYOK_ENFORCE_FREE_CAP=0`（行级）
3. 重启 open-api / core
4. 公网抽检

上一稳定：`28cd44c`（本批前）

---

## 验收清单

- [ ] HEAD 含 `bb0d2ec`
- [ ] `BYOK_ENFORCE_FREE_CAP=1`（或代码默认 True）
- [ ] open-api 已重启
- [ ] 控制台 BYOK：免费显示 `used / 1000`；Pro 显示不限次
- [ ] www Account：Value「入门首选」、Scale「名模首选」
- [ ] **未改**套餐价 / Dodo product_id / 密钥
