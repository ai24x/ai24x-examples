# 【指令】更新部署验收：22c86f8（VIP点名页优化 + 后台二次确认 + 管理审计）

> 来源：司令（Codex）→ 指挥（主脑）→ 副脑04 执行。本次为前端+API 批量更新。

## 第一步 · 拉码 + 重启
- git pull → 期望 HEAD = 22c86f8（远程 master 最新，含 375 insertions 批量改动）
- AI24X-core 重启 → health 200

## 第二步 · 验收清单
① /v1/models：vip_picks 24 款；响应无 channels/cost_in/openrouter_id 字段（供应链保密不回归）；超值包白名单模型 value_pack_ok=true 保持
② 超值包非 VIP 点名：未开通 VIP 的用户请求白名单模型（如 vip-ds-flash）应可调用（allow_names 逻辑）；非白名单仍 403/需要 VIP
③ vip-picks 页面（www.ai24x.com/models/vip-picks.html）：
   - 页面无 OpenRouter / 竞品平台字样
   - 名模列表上方出现「官方直连对比条」（含 GPT-5.6 Terra 输出省 39% 类文案）
   - GPT 系模型行有官方对照徽章（绿「输出省 X%」/橙「+X%」）
   - 预估消耗列不换行
   - 英文版超值包 banner 显示英文模型名（Xiaomi MiMo Pro / Zhipu GLM-5.2 / Qwen Max）
④ 后台 token-admin.html：改价/系统开关/仓库费率/密钥/立即巡检 5 处操作弹二次确认框（不点确认不执行）
⑤ 管理写操作审计：POST /v1/admin/price/refresh 后，进程日志出现 [ADMIN-AUDIT] 行（含脱敏 body 与 admin_key 前缀，密钥字段 ***）；生产日志由进程管理器捕获
⑥ 支付开关：GET /v1/billing/plans → pay.enabled 应为 true（生产若有 Creem test key 则 creem_ready 视配置）

## 第三步 · 回滚预案
- git revert 22c86f8 + 重启 core 即可全量回退（前端静态文件同步回滚）

## 汇报格式
完成后通过飞书汇报：①拉码 HEAD ②重启 health ③验收逐项 PASS/FAIL ④发现的回归或需主脑决策事项 ⑤回滚预案已记录。