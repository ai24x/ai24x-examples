# 需求类型：②需副脑办(副脑04)
# 需求描述：部署 www + open 控制台 Billing UI（套餐排序、Card 支付图标排版）
# 验收标准：公网 console.js?v=20260828i；www Token 套餐置顶；open BYOK 置顶；支付弹窗 Card 图标与文字同行
# 期望时间：push 后立即 deploy04

> **目标提交：`9c5a5fb`**  
> 执行：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\deploy04.ps1 "docs\营销\收发\指令\20260828-1905-04更新-console计费UI-9c5a5fb.ps1"`

## 变更摘要

- `web/js/console.js` + `web/console.html`：Token → Markets → BYOK 排序；`billingProducts`；支付弹窗 `pay-btn-title-row`
- `p/open/web/js/console.js` + `console.html`：BYOK 优先；`renderPlans` zh 修复；同款支付弹窗结构 + CSS
- 缓存：`console.js?v=20260828i`（www + open）

## 服务

- `Restart-Service AI24X-core`（www/api）
- `Restart-Service AI24X-open-api`（open）

## 群确认（04 必发）

```
✅ 04更新完成｜Console计费UI｜EXP=9c5a5fb HEAD=<12位> health=<12位>｜www+open 20260828i 公网通过
```
