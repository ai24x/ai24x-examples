# 【04 放行】支付回跳动态化 ee8daf0 · 按原文案发指挥部群回执

> 司令确认：部署本体全部 PASS；step4 脚本比较缺陷（Python 多行输出在 PowerShell 为数组）已由 04 手动补验覆盖，helper 行为与验收口径一致。**放行，发群回执。**

## 副脑04 执行（整段复制运行）
```powershell
$ErrorActionPreference = "Stop"
Set-Location C:\ai24x01

"--- 1) 复验关键结论（不触网支付）---"
$t = Get-Content "C:\ai24x01\api\token_pay_service.py" -Raw -Encoding UTF8
"mark_helper=$($t.Contains('_origin_console_url'))"
"mark_open_return=$($t.Contains('https://open.ai24x.com/console.html'))"
$h = python -c "import sys; sys.path.insert(0,r'C:\ai24x01\api'); from token_pay_service import _origin_console_url; print(_origin_console_url('https://open.ai24x.com')); print(_origin_console_url('https://www.ai24x.com')); print(_origin_console_url(None))" | Out-String
$h = $h.Trim()
"helper_lines=$($h -replace "`r?`n", ' | ')"
if (-not $h.Contains('https://open.ai24x.com/console.html')) { Write-Host "!! helper 异常" -ForegroundColor Red; exit 1 }

"--- 2) 发指挥部群回执（04 应用）---"
$groupMsg = "【AI24X 部署回执】支付回跳动态化 ee8daf0 成功：目标 ee8daf06ff56 已生效（HEAD=9a7444933428）、8002 health OK、helper 行为=open 站下单付完回 open 控制台自动确认/www 与未知来源保持默认、公网 health commit 一致、plans CORS open 放行 200。问题项：无（step4 脚本比较缺陷已手动补验，非业务问题）"
python api\feishu_notify.py --config <openclaw.json> --group $groupMsg --at
if ($LASTEXITCODE -ne 0) { Write-Host "!! 群回执发送失败" -ForegroundColor Red; exit 1 }

Write-Host "=== 放行回执已发群 OK ===" -ForegroundColor Green
```

## 回执（04 完成后回传司令）
- ✅ 已完成项：helper 复验 / 群回执已发
- ⚠️ 问题项：无
