# 【04 更新】Markets P0：技术健康评分 + Discover 美股扫描页 + 免费额度引导 · 目标 03f03b1ee50f

> 通道：司令直连 04（deploy04.ps1 同步 600s）。验收全 PASS 后自动发指挥部群回执；有问题先回传司令，不发群。

## 背景
- 按任务书《AI行情官国际版（markets.ai24x.com）优化任务书》先做 P0：①技术健康评分 0-100 接进 UI + AI Brief；②美股发现/筛选器（对标掘金）；③AI 点评免费额度（3 次/天）用完弹升级引导、未登录先引导登录。
- 本轮改动（03f03b1ee50f）：
  - **screener.py（新）**：90 只美股/ETF 池、5 种技术模式扫描（Bottom volume surge / Breakout on volume / Uptrend building / MACD momentum building / Pullback holding near MAs），0-100 评分复用 a1 scoring 引擎并翻译英文标签/风险，全量扫描约 90-100s、15 分钟缓存 `data/cache/screener_us.json`。
  - **main.py**：新增 `/api/score`（免费，仅本地指标不耗 AI 额度）与 `/api/screener`（mode/limit 参数）；`/api/signals` 响应新增 `score` 字段。
  - **ai_brief.py**：brief 响应新增 `score`；缓存 key 升 `brief_v2_*` 自动失效旧缓存。
  - **app.html**：图表区新增「Technical Health Score」评分条（分数 + 强弱标签 + 60 日位置/量能/风险标签 + 跳 Discover）；AI Brief 顶部先出分数再出文字；免费额度用完弹 toast + 升级引导；导航新增 Discover。
  - **screener.html（新）**：Discover 发现页（模式 chips + 评分卡片 + 点击开图新窗口 + 中英切换 + 移动端单列）。
  - **index.html**：导航新增 Discover。
  - QA：`_qa_p0_20260818.js`（本地 13/13 PASS）+ `_qa_brief_score.py`（PASS，AAPL score=20.4）。
- **合规**：全站英文、无 buy/sell/hold/target/advice/signal/recommend/tips/picks/broker/trade 等措辞（已扫描），数据 15 分钟延迟免责 + educational purposes only 保留。

## 注意事项
- 本批只动 **markets 后端 py + markets 静态**：静态同步 `C:\sites\markets.ai24x.com`（screener.html + app.html + index.html，先备份 .bak）；重启 **AI24X-markets-api(18012)**。**不涉及 www/core（8002）**，无需重启 AI24X-core。
- HEAD 校验用「目标前缀 + merge-base + 关键路径 diff --quiet 与目标一致」（指令文档自身也是新提交）。
- **screener 首次全量扫描约 90-100s**：验收脚本的 `/api/screener` 请求必须 `-TimeoutSec 150`，勿用 40s（会误报超时）。
- 所有 Invoke-WebRequest/-RestMethod 带 `-UseBasicParsing`；PowerShell 字符串断言用单引号。
- 若 04 本地有 markets 相关脏改动（尤其 app.html/main.py），先备份到 `C:\Users\Administrator\ops\dirty-*-20260818-p0` 再 checkout，勿覆盖丢失。

## 副脑04 执行脚本（整段复制运行）
```powershell
# === AI24X 更新部署验收 · 目标 03f03b1ee50f（Markets P0）===
$ErrorActionPreference = "Stop"
$REPO = "C:\ai24x01"
$SRV_MK  = "AI24X-markets-api"
$EXP  = "03f03b1ee50f"
$SITES = "C:\sites\markets.ai24x.com"
$TAG = "20260818-p0"

Set-Location $REPO

"--- 0) 本批路径如有本地改动：备份后 checkout ---"
$keyFiles = @(
  "p/markets/api/server/app/main.py",
  "p/markets/api/server/app/ai_brief.py",
  "p/markets/api/server/app/screener.py",
  "p/markets/web/app.html",
  "p/markets/web/index.html",
  "p/markets/web/screener.html"
)
foreach ($f in $keyFiles) {
    $dirty = git status --short -- $f
    if ($dirty) {
        $bn = ($f -replace "/", "_")
        Copy-Item "$REPO\$f" "C:\Users\Administrator\ops\dirty-$bn-$TAG" -Recurse -Force
        git checkout -- $f
        if ($LASTEXITCODE -ne 0) { Write-Host "!! checkout 失败: $f" -ForegroundColor Red; exit 1 }
        "dirty backup + checkout: $f"
    } else {
        "clean: $f"
    }
}

"--- 1) 拉取并校验目标提交 ---"
git pull
$HEAD = (git rev-parse --short=12 HEAD).Trim()
"HEAD=$HEAD"
git merge-base --is-ancestor $EXP HEAD
if ($LASTEXITCODE -ne 0) { Write-Host "!! 目标 $EXP 不在 HEAD 历史中" -ForegroundColor Red; exit 1 }
git diff --quiet $EXP HEAD -- p/markets/api/server/app/main.py p/markets/api/server/app/ai_brief.py p/markets/api/server/app/screener.py p/markets/web/app.html p/markets/web/index.html p/markets/web/screener.html
if ($LASTEXITCODE -ne 0) { Write-Host "!! 关键路径在 $EXP 之后仍有改动，停止" -ForegroundColor Red; exit 1 }
"target_verified=ok"

"--- 2) 同步 markets 静态到 nginx 服务目录（先备份）---"
foreach ($f in @("app.html", "index.html", "screener.html")) {
    if (Test-Path "$SITES\$f") { Copy-Item "$SITES\$f" "$SITES\$f.bak-$TAG" -Force }
    Copy-Item "$REPO\p\markets\web\$f" "$SITES\$f" -Force
}
$sa = Get-Content "$SITES\app.html" -Raw -Encoding UTF8
$si = Get-Content "$SITES\index.html" -Raw -Encoding UTF8
$ss = Get-Content "$SITES\screener.html" -Raw -Encoding UTF8
"sites_app_score_strip=$($sa.Contains('score-strip'))"
"sites_app_discover=$($sa.Contains('/screener.html'))"
"sites_index_discover=$($si.Contains('/screener.html'))"
"sites_screener_exists=$($ss.Contains('Bottom volume surge'))"
if (-not ($sa.Contains('score-strip') -and $sa.Contains('/screener.html') -and $si.Contains('/screener.html') -and $ss.Contains('Bottom volume surge'))) { Write-Host "!! sites 静态文件内容缺失" -ForegroundColor Red; exit 1 }

"--- 3) 重启 markets (18012) ---"
Restart-Service $SRV_MK -Force
$okM = $false
for ($i = 0; $i -lt 15; $i++) {
    Start-Sleep -Seconds 5
    try { $hm = Invoke-RestMethod -Uri "http://127.0.0.1:18012/health" -TimeoutSec 10 -UseBasicParsing; $okM = $true; break } catch {}
}
if (-not $okM) { Write-Host "!! markets /health 未就绪" -ForegroundColor Red; exit 1 }
"markets-health=$($hm.status)"

"--- 4) 本地验收：评分接口 / signals 带 score / screener（首次约 90-100s）---"
$sc = Invoke-RestMethod -Uri "http://127.0.0.1:18012/api/score?symbol=AAPL&count=200" -TimeoutSec 40 -UseBasicParsing
"local_score_code=$($sc.code) score=$($sc.data.score) summary=$($sc.data.summary)"
if ($sc.code -ne 0 -or $sc.data.score -le 0 -or $sc.data.score -gt 100 -or -not $sc.data.ok) { Write-Host "!! /api/score 验收失败" -ForegroundColor Red; exit 1 }

$sg = Invoke-RestMethod -Uri "http://127.0.0.1:18012/api/signals?symbol=AAPL&period=day&count=200" -TimeoutSec 40 -UseBasicParsing
"local_signals_has_score=$($null -ne $sg.data.score)"
if ($sg.code -ne 0 -or $null -eq $sg.data.score -or $sg.data.score.score -le 0) { Write-Host "!! /api/signals 未带 score" -ForegroundColor Red; exit 1 }

$sw = [System.Diagnostics.Stopwatch]::StartNew()
$sr = Invoke-RestMethod -Uri "http://127.0.0.1:18012/api/screener?mode=all&limit=5" -TimeoutSec 150 -UseBasicParsing
$sw.Stop()
"local_screener_elapsed=$([math]::Round($sw.Elapsed.TotalSeconds,1))s count=$($sr.data.items.Count)"
if ($sr.code -ne 0 -or $sr.data.items.Count -lt 1) { Write-Host "!! /api/screener 验收失败" -ForegroundColor Red; exit 1 }
$sr2 = Invoke-RestMethod -Uri "http://127.0.0.1:18012/api/screener?mode=Uptrend%20building&limit=5" -TimeoutSec 40 -UseBasicParsing
"local_screener_mode_count=$($sr2.data.items.Count)"
if ($sr2.code -ne 0) { Write-Host "!! /api/screener mode 过滤失败" -ForegroundColor Red; exit 1 }

Write-Host "=== 部署成功 ✅ ===" -ForegroundColor Green
```

## 完成后群回执（固定步骤）
- ✅ 已完成项：HEAD=03f03b1ee50f（merge-base+path diff 校验通过）、markets /health OK、/api/score（AAPL score=xx）、/api/signals 带 score、/api/screener 返回 N 条、sites 静态含 score-strip/Discover/screener.html、群回执已发。
- ⚠️ 问题项：逐条列出（无则写「无」）。
- 公网验收：`https://markets.ai24x.com/app.html?symbol=AAPL` 看评分条；`https://markets.ai24x.com/screener.html` 看 Discover 卡片；`https://markets.ai24x.com/api/score?symbol=AAPL` 返回 score。公网访问请从 04 服务器侧或海外节点验证（本机→公网常超时）。

## 回滚预案
- `git revert 03f03b1ee50f` + 重启 AI24X-markets-api（或恢复 `C:\sites\markets.ai24x.com\*.bak-20260818-p0` + 重启）。
