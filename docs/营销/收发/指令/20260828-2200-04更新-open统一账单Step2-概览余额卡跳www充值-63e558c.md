# 需求类型：②需副脑办(副脑04)
# 需求描述：open 站统一账单 Step2 —— 控制台 Overview「Paid balance」余额卡改为「AI Gateway credits 主站统一管理」引导卡，open 不再显示/操作 credits 余额，充值引导直达 www；BYOK/今日免费/API Key 卡保留
# 验收标准：open.ai24x.com console Overview 含 creditsOvCta 引导且不含 stat-balance；locales.js?v=20260828m / console.js?v=20260828l；open /health commit=63e558c
# 期望时间：push 后立即 deploy04

> **目标提交：`63e558c`**

## 变更摘要

- `p/open/web/console.html`：Overview `#stat-balance` / `#stat-balance-sub` / `#vp-active-line` 余额卡替换为「AI Gateway credits」信息卡（`#creditsOvCta` 新窗跳 www#billing）
- `p/open/web/js/console.js`：`#creditsOvCta` 本地环境感知（127.0.0.1 → `http://127.0.0.1:8000/console.html#billing`）；旧余额写入均有判空保护，删除元素后安全
- `p/open/web/config/locales.js`：新增 `page.console.stat.creditsTitle/creditsSub/creditsCta` 键（zh+en）
- 版本号：locales.js 全站 **20260828m**（k→m）/ console.js **20260828l**（j→l）；base.css 未改（仍 20260828l）不 bump

## 服务

- `Restart-Service AI24X-open-api`（open 18080，FastAPI mount 静态；**www core 8002 不动**）

## 执行步骤

1. 备份+清理脏改动（仅限本次涉及路径，防止 pull 冲突）：
   ```powershell
   $bk = "C:\Users\Administrator\ops\.bak-open-unified-billing-step2-20260828"
   New-Item -ItemType Directory -Force -Path $bk | Out-Null
   git -C C:\ai24x01 status --short -- p/open/web | ForEach-Object {
     $rel = ($_ -replace '^\S+\s+','').Trim()
     if ($rel) {
       $src = Join-Path "C:\ai24x01" $rel
       if (Test-Path -LiteralPath $src) {
         $dst = Join-Path $bk ($rel -replace '[\\/]','__')
         Copy-Item -LiteralPath $src -Destination $dst -Force
         git -C C:\ai24x01 checkout -- $rel
       }
     }
   }
   ```
2. `git -C C:\ai24x01 pull`
3. HEAD 校验：`git -C C:\ai24x01 merge-base --is-ancestor 63e558c HEAD` → 返回 0 即通过（无输出=成功）
4. `Restart-Service AI24X-open-api -Force`，等待 10s
5. 公网验收（全部必须通过，逐条回执）：
   - `https://open.ai24x.com/health` → commit 以 `63e558c` 开头
   - `https://open.ai24x.com/console.html` → 200，且含 `creditsOvCta`，且**不含** `id="stat-balance"`
   - console.html 含 `locales.js?v=20260828m` + `console.js?v=20260828l`
   - 本地仓库扫残留：`git -C C:\ai24x01 grep -n "locales.js?v=20260828k\|console.js?v=20260828j" -- p/open/web` → 无输出
   - www 回归（应无变化）：`https://www.ai24x.com/console.html` 200

## 群确认（04 必发）

```
✅ 04更新完成｜open统一账单Step2-概览余额卡跳www充值｜EXP=63e558c HEAD=<12位> health=<12位>｜locales 20260828m/console 20260828l 公网通过
```
