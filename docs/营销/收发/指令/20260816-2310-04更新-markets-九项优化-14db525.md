# 【04 更新】markets 九项优化正式部署 · 目标 14db525

> 通道：司令直连 04（deploy04.ps1 同步 600s）。验收全 PASS 后自动发指挥部群回执；有问题先回传司令，不发群。

## 背景
- 老板 2026-08-16 九项优化清单，本批一次部署闭环。04 当前 HEAD = 748e87e，本批补齐：
  - `d3c76eb` v0.4：PayPal cancel 跳转 `/app.html#sub`、Pro 激活引导、免费额度「明日重置」、独立定价页 pricing.html、RSI 面板移除、auth 胶囊样式、samples 重构
  - `317b034` 标的未命中建议代码（指数别名 GSPC/SPX/IXIC/NASDAQ/DJI/DOW → ^GSPC/^IXIC/^DJI；个股尾字母裁剪如 WETOUR→WETO）
  - `1e2df39` kline/signals 按 count 切片（缓存命中不再多返）
  - `278148b` **恢复中英文切换 + 中文词典 + 白名单指数中英名**（老板拍板，覆盖 v0.4「仅英文」清理）
  - `14db525` **桌面端 Volume 空卡片修复**：renderVol 去掉手机专属守卫（桌面也渲染成交量），无成交量数据的标的整卡隐藏
- ⚠️ 与先前 1e2df39 指令的口径差异（以本指令为准）：
  1. **kline 默认根数 = 500**（老板口径「总量 2 年」，参考国内版；不是 250）
  2. **保留中英文切换**（app.html 含 `id="lang-switch"` 与中文词典），不再是「仅英文」
  3. app.html 不含 `chart-rsi`；桌面 Volume 面板正常渲染（含 `hasVol`/`pan.style.display` 逻辑）

## 注意事项
- 只动 6 个路径：`p/markets/api/server/app/main.py`、`paypal.py`、`providers_us.py`、`p/markets/web/app.html`、`index.html`、`p/markets/web/pricing.html`（新增）；04 工作树其它历史未提交文件一律不动。
- 服务重启加载后端 3 个 py；静态文件同步 `C:\sites\markets.ai24x.com`。
- PayPal env：若 `MARKETS_PAYPAL_*` 覆盖变量存在则停步（删除后重启）。

## 副脑04 执行脚本（整段复制运行）
```powershell
# === AI24X 更新部署验收 · 目标 14db525 ===
$ErrorActionPreference = "Stop"
$REPO = "C:\ai24x01"
$SRV_MARKETS = "AI24X-markets-api"
$EXP  = "14db525"
$SITES = "C:\sites\markets.ai24x.com"
$FILES = @(
  "p/markets/api/server/app/main.py",
  "p/markets/api/server/app/paypal.py",
  "p/markets/api/server/app/providers_us.py",
  "p/markets/web/app.html",
  "p/markets/web/index.html",
  "p/markets/web/pricing.html"
)

Set-Location $REPO

"--- 1) 六个路径如有本地改动：备份后 checkout ---"
foreach ($f in $FILES) {
    $dirty = git status --short -- $f
    if ($dirty) {
        $bn = ($f -replace "/", "_")
        Copy-Item "$REPO\$f" "C:\Users\Administrator\ops\dirty-$bn-20260816-2310" -Force
        git checkout -- $f
        if ($LASTEXITCODE -ne 0) { Write-Host "!! checkout 失败: $f" -ForegroundColor Red; exit 1 }
        "dirty backup + checkout: $f"
    } else {
        "clean: $f"
    }
}

"--- 2) 拉取并校验 HEAD ---"
git pull
$HEAD = (git rev-parse --short=12 HEAD).Trim()
"HEAD=$HEAD"
if ($HEAD -notlike "$EXP*") { Write-Host "!! HEAD 不匹配，期望前缀 $EXP 实际 $HEAD" -ForegroundColor Red; exit 1 }

"--- 3) 同步静态文件到 nginx 服务目录（先备份）---"
foreach ($f in @("app.html", "index.html", "pricing.html")) {
    if (Test-Path "$SITES\$f") { Copy-Item "$SITES\$f" "$SITES\$f.bak-20260816-2310" -Force }
    Copy-Item "$REPO\p\markets\web\$f" "$SITES\$f" -Force
}
$sa = Get-Content "$SITES\app.html" -Raw -Encoding UTF8
"sites_lang_switch=$($sa.Contains('id="lang-switch"'))"
"sites_zh_dict=$($sa.Contains('你是不是想查'))"
"sites_no_rsi=$(-not $sa.Contains('chart-rsi'))"
"sites_vol_hasvol=$($sa.Contains('pan.style.display'))"
"sites_dow_zh=$($sa.Contains('道琼斯'))"
$si = Get-Content "$SITES\index.html" -Raw -Encoding UTF8
"sites_index_pricing_link=$($si.Contains('/pricing.html'))"
$sp = Get-Content "$SITES\pricing.html" -Raw -Encoding UTF8
"sites_pricing_price=$($sp.Contains('14.9') -and $sp.Contains('119'))"

"--- 4) 重启 markets (18012) ---"
Restart-Service $SRV_MARKETS -Force
$ok = $false
for ($i = 0; $i -lt 12; $i++) {
    Start-Sleep -Seconds 5
    try { $h = Invoke-RestMethod -Uri "http://127.0.0.1:18012/health" -TimeoutSec 10 -UseBasicParsing; $ok = $true; break } catch {}
}
if (-not $ok) { Write-Host "!! markets /health 未就绪" -ForegroundColor Red; exit 1 }
"markets-health=$($h.status)"

"--- 5) PayPal env 核查（有覆盖变量即停）---"
$envPay = Select-String -Path "$REPO\api\.env", "$REPO\p\markets\api\server\.env" -Pattern "MARKETS_PAYPAL" -SimpleMatch -ErrorAction SilentlyContinue
"paypal_env_overrides=$($envPay.Count)"
if ($envPay.Count -gt 0) { Write-Host "!! 检测到 MARKETS_PAYPAL_* 覆盖变量，需删除后重启，停步" -ForegroundColor Red; exit 1 }

"--- 6) 本地 + 公网验收 ---"
$k = Invoke-RestMethod -Uri "http://127.0.0.1:18012/api/kline?symbol=AAPL" -TimeoutSec 20 -UseBasicParsing
"local_kline_candles=$($k.data.candles.Count)"
if ($k.data.candles.Count -ne 500) { Write-Host "!! 本地 kline 非 500 根" -ForegroundColor Red; exit 1 }
$w = Invoke-RestMethod -Uri "http://127.0.0.1:18012/api/kline?symbol=WETOUR" -TimeoutSec 30 -UseBasicParsing
"local_wetour_suggested=$($w.suggested)"
if ($w.suggested -ne "WETO") { Write-Host "!! WETOUR 建议代码未命中" -ForegroundColor Red; exit 1 }

$pub = (Invoke-WebRequest -Uri "https://markets.ai24x.com/app.html?x=v9opt" -TimeoutSec 25 -UseBasicParsing).Content
"pub_lang_switch=$($pub.Contains('id=\"lang-switch\"'))"
"pub_zh_dict=$($pub.Contains('你是不是想查'))"
"pub_no_rsi=$(-not $pub.Contains('chart-rsi'))"
"pub_dow_zh=$($pub.Contains('道琼斯'))"
$ppub = (Invoke-WebRequest -Uri "https://markets.ai24x.com/pricing.html?x=v9opt" -TimeoutSec 25 -UseBasicParsing).Content
"pub_pricing_price=$($ppub.Contains('14.9') -and $ppub.Contains('119'))"
$pk = Invoke-RestMethod -Uri "https://markets.ai24x.com/api/kline?symbol=AAPL" -TimeoutSec 20 -UseBasicParsing
"pub_kline_candles=$($pk.data.candles.Count)"

if ($pub.Contains('id="lang-switch"') -and $pub.Contains('你是不是想查') -and (-not $pub.Contains('chart-rsi')) -and $pub.Contains('道琼斯') -and $ppub.Contains('14.9') -and $ppub.Contains('119') -and $pk.data.candles.Count -eq 500) {
    "--- 全部 PASS ---"
    Write-Host "=== 部署成功 OK ===" -ForegroundColor Green
} else {
    Write-Host "!! 验收未全通过" -ForegroundColor Red; exit 1
}
```

## 完成后群回执（固定步骤，主通道必做）
- 用 04 应用发指挥部群：
  `python api\feishu_notify.py --config <openclaw.json> --group "【AI24X 部署回执】markets 九项优化正式部署 14db525 成功：HEAD=14db525、markets 18012 health OK、kline 默认 500 根（2年）、WETOUR 建议代码=WETO、中英文切换恢复（lang-switch+中文词典）、白名单指数中英名（道琼斯等）、桌面 Volume 修复不空卡、pricing.html 上线、PayPal cancel=/app.html#sub。问题项：无" --at`
- 把 ✅ 已完成项 / ⚠️ 问题项 清单一并发群里。

## 回执格式（统一）
- ✅ 已完成项：HEAD / checkout / pull / sites 备份+同步 / health / PayPal env 核查 / local kline 500 / WETOUR 建议 / 公网 app.html 标记（lang-switch/中文/无RSI/道琼斯）/ 公网 pricing / 公网 kline 500 / 群回执
- ⚠️ 问题项：逐条列出（无则写「无」）

## 回滚预案
- `git revert 14db525` + 重启 markets；静态文件恢复 `$SITES\app.html.bak-20260816-2310` / `index.html.bak-20260816-2310` / `pricing.html.bak-20260816-2310`
