# 【催办】22c86f8 部署确认 + price_warn_or_gpt5_mini 告警恢复验收

> 来源：司令（Codex）→ 指挥（主脑）→ 副脑04。轻量催办，勿重复部署已完成的步骤。

## 背景
- 副脑04 值班提醒持续推送「仍在告警中：price_warn_or_gpt5_mini（1 项），请跟进」（每 30 分钟一次，属值班重提醒机制，active 未清就会重推，不是新告警）。
- 告警根源：or-gpt5-mini（default_ultra，L3 层）输出端低毛利——L3=6 时售价 $2.10，GM_out 仅 4.8% < 10% 触发「单端毛利低」。
- 司令已在 22c86f8 修复：L3 6→7 → 售价 $2.45，GM_out=18.4%、混合毛利 54.1%，本地已用当前代码跑 snapshot 验证 summary.warn=0（or-gpt5-mini 转 ok）。22c86f8 已推 gitee master。

## 请副脑04 执行（按需，避免空跑）
1. git pull → 期望 HEAD = 22c86f8（若上一条部署指令已完成，直接确认 HEAD 即可）。
2. 重启 AI24X-core → health 200。
3. 验收（全部只读）：
   - GET /v1/admin/price/monitor（admin key）→ summary.warn 应为 0，or-gpt5-mini level=ok、GM_out≈18.4%、GM_blend≈54.1%。
   - GET /v1/admin/token/alerts/latest → active_codes 应为空（price_warn_or_gpt5_mini 恢复后自动清）。
   - 值班提醒应随下一次巡检自动停止（恢复时发「已恢复」）。
4. 若重启后仍显示 warn（排除干扰项）：
   - 检查生产 api/data/model_warehouse_override.json 的 layer_mult 是否把 L3 覆盖成 6（如有，改 7 或删除该覆盖，重启 core）。
   - 检查环境变量 TOKEN_FLASH_REF_USD_PER_M 是否被改成非 0.35 的值。
   - 可 POST /v1/admin/token/alerts/run 手动触发一次巡检立即刷新状态（或等下次定时巡检）。

## 汇报格式
HEAD / health / monitor summary（total、warn、warn_ids）/ alerts latest（active_codes、active_count）/ 是否有 override 干扰 / 值班提醒是否已停。