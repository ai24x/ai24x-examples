# 【04 更新】Markets 周 VIP 套餐 $9.9/周 + console 统一开通 + 充值改名套餐 · 目标 c9434b3e9c71

> 通道：司令直连 04（deploy04.ps1 同步 600s）。验收全 PASS 后自动发指挥部群回执；有问题先回传司令，不发群。

## 背景
- 老板指令：「AI行情官 开通 VIP 套餐 增加个 周 VIP 套餐，和其它套餐能否类似 token 套餐充值，放在 console.html#overview 合适位置给用户选择统一开通；充值名称改成套餐」。
- 本轮改动（c9434b3e9c71，52 文件）：
  1. **后端 billing.py**：`PLANS` 新增 `weekly {usd:9.9, days:7}`——PayPal checkout/capture/续订叠加逻辑全部自动生效，无需改 paypal.py/main.py。
  2. **console（www）**：`console.html#overview` 的 AI24X Markets 卡内新增「周 $9.9 / 月 $24.9 / 年 $199」三档套餐按钮，点按钮直连 markets checkout → PayPal 跳转（复用 `doMarketsCheckout`，消息区 `.mk-checkout-msg` 多卡共用）；Billing 面板 Markets Pro 卡补 `btn-mk-week`；导航/面板标题 `充值→套餐`、`Billing→Plans`（locales.js 中英双语）；余额引导按钮 `去充值→去选套餐`。
  3. **markets app.html**：订阅面板补 `btn-sub-week`（$9.9/周）+ i18n 中英 + `?plan=weekly#sub` 深链高亮。
  4. **markets 定价/首页**：pricing.html 改 4 卡布局（Free / Weekly / Monthly / Yearly，响应式 4→2→1 列）；index.html 价格行与引导文案补 $9.9/week。
  5. **版本号纪律**：全站 43 页共享资源统一 `v=20260818i`（旧 g/h 残留 0）。
  6. QA：新增 `_qa_weekly_plan_20260817.js`（24 项全 PASS）；`_qa_vip_guide` 43/43、`_qa_console_vip` 7/7、`_qa_www` 23/23、`_qa_oauth` 25/25 全过。

## 注意事项
- 本批动 **markets 后端 py + markets 静态（3 个页面）+ www web/**：markets 静态同步 `C:\sites\markets.ai24x.com`（app.html + index.html + **pricing.html**，先备份 .bak）；重启 **AI24X-markets-api(18012)**；www 由仓库 web/ 提供，git pull 后重启 **AI24X-core(8002)**。
- HEAD 校验用「目标前缀 + merge-base + 关键路径 diff --quiet 与目标一致」（指令文档自身也是新提交）。
- ⚠️ PowerShell 双引号会把 `$9.9` 的 `$9` 当变量吞掉：含价格断言一律用**单引号字面量** `'$9.9'`。
- 版本号断言用 `-SimpleMatch`（`?` 是正则量词会误报 FAIL）。
- 所有 Invoke-WebRequest/-RestMethod 带 `-UseBasicParsing`。

## 副脑04 执行脚本（整段复制运行）
```powershell
# === AI24X 更新部署验收 · 目标 c9434b3 ===
$ErrorActionPreference = "Stop"
$REPO = "C:\ai24x01"
$SRV_MK  = "AI24X-markets-api"
$SRV_WWW = "AI24X-core"
$EXP  = "c9434b3e9c71"
$SITES = "C:\sites\markets.ai24x.com"
$TAG = "20260817-c9434b3"

Set-Location $REPO

"--- 0) 本批路径如有本地改动：备份后 checkout ---"
$keyFiles = @(
  "p/markets/api/server/app/billing.py",
  "p/markets/web/app.html",
  "p/markets/web/index.html",
  "p/markets/web/pricing.html",
  "web/js/console.js",
  "web/config/locales.js",
  "web/console.html"
)
foreach ($f in $keyFiles) {
    $dirty = git status --short -- $f
    if ($dirty) {
        $bn = ($f -replace "/", "_")
        Copy-Item "$REPO\$f" "C:\Users\Administrator\ops\dirty-$bn-$TAG" -Recurse -Force
        git checkout -- $f
        if ($LASTEXITCODE -ne 0) { Write-Host "!! checkout 失败: $f" -ForegroundColor Red; exit 1 }
        "dirty backup + checkout: $f"
    } else {
        "clean: $f"
    }
}
# 43 页 web html 版本号：有本地改动同样备份 + checkout
git status --porcelain -- web/*.html | ForEach-Object {
    $p = ($_.Substring(3)).Trim()
    if ($p) {
        $bn = ($p -replace "/", "_")
        Copy-Item "$REPO\$p" "C:\Users\Administrator\ops\dirty-$bn-$TAG" -Force
        git checkout -- $p
        "dirty backup + checkout: $p"
    }
}

"--- 1) 拉取并校验目标提交 ---"
git pull
$HEAD = (git rev-parse --short=12 HEAD).Trim()
"HEAD=$HEAD"
git merge-base --is-ancestor $EXP HEAD
if ($LASTEXITCODE -ne 0) { Write-Host "!! 目标 $EXP 不在 HEAD 历史中" -ForegroundColor Red; exit 1 }
git diff --quiet $EXP HEAD -- p/markets/api/server/app/billing.py p/markets/web/app.html p/markets/web/index.html p/markets/web/pricing.html web/js/console.js web/config/locales.js web/console.html
if ($LASTEXITCODE -ne 0) { Write-Host "!! 关键路径在 $EXP 之后仍有改动，停止" -ForegroundColor Red; exit 1 }
"target_verified=ok"

"--- 2) 同步 markets 静态到 nginx 服务目录（先备份）---"
foreach ($f in @("app.html", "index.html", "pricing.html")) {
    if (Test-Path "$SITES\$f") { Copy-Item "$SITES\$f" "$SITES\$f.bak-$TAG" -Force }
    Copy-Item "$REPO\p\markets\web\$f" "$SITES\$f" -Force
}
$sa = Get-Content "$SITES\app.html" -Raw -Encoding UTF8
$si = Get-Content "$SITES\index.html" -Raw -Encoding UTF8
$sp = Get-Content "$SITES\pricing.html" -Raw -Encoding UTF8
"sites_app_week=$($sa.Contains('btn-sub-week') -and $sa.Contains('sub.week') -and $sa.Contains('weekly'))"
"sites_index_week=$($si.Contains('9.9/week'))"
"sites_pricing_week=$($sp.Contains('grid-4') -and $sp.Contains('9.9'))"
if (-not ($sa.Contains('btn-sub-week') -and $si.Contains('9.9/week') -and $sp.Contains('grid-4'))) { Write-Host "!! sites 静态文件内容缺失" -ForegroundColor Red; exit 1 }

"--- 3) 重启 markets (18012) + core (8002) ---"
Restart-Service $SRV_MK -Force
Restart-Service $SRV_WWW -Force
$okM = $false
for ($i = 0; $i -lt 15; $i++) {
    Start-Sleep -Seconds 5
    try { $hm = Invoke-RestMethod -Uri "http://127.0.0.1:18012/health" -TimeoutSec 10 -UseBasicParsing; $okM = $true; break } catch {}
}
if (-not $okM) { Write-Host "!! markets /health 未就绪" -ForegroundColor Red; exit 1 }
"markets-health=$($hm.status)"
$okW = $false
for ($i = 0; $i -lt 12; $i++) {
    Start-Sleep -Seconds 5
    try { $hw = Invoke-RestMethod -Uri "http://127.0.0.1:8002/health" -TimeoutSec 10 -UseBasicParsing; $okW = $true; break } catch {}
}
if (-not $okW) { Write-Host "!! core /health 未就绪" -ForegroundColor Red; exit 1 }
"www-health=$($hw.status) commit=$($hw.commit)"
if ($hw.commit -notlike "$EXP*") { Write-Host "!! core 服务未生效 commit=$($hw.commit)" -ForegroundColor Red; exit 1 }

"--- 4) 本地验收：周套餐后端 / console 开通 / 版本号 ---"
$bp = Get-Content "$REPO\p\markets\api\server\app\billing.py" -Raw -Encoding UTF8
"backend_weekly=$($bp.Contains('"weekly": {"usd": 9.9, "days": 7'))"
if (-not $bp.Contains('"weekly": {"usd": 9.9, "days": 7')) { Write-Host "!! billing.py 周套餐缺失" -ForegroundColor Red; exit 1 }

$cl = Get-Content "$REPO\web\console.html" -Raw -Encoding UTF8
$cj = Get-Content "$REPO\web\js\console.js" -Raw -Encoding UTF8
$lc = Get-Content "$REPO\web\config\locales.js" -Raw -Encoding UTF8
"console_ov_week=$($cl.Contains('btn-ov-week') -and $cl.Contains('btn-ov-month') -and $cl.Contains('btn-ov-year'))"
"console_bill_week=$($cl.Contains('btn-mk-week'))"
"console_js_weekly=$($cj.Contains('doMarketsCheckout(""weekly"")'))"
"locales_zh_plans=$($lc.Contains('"page.console.nav.billing": "套餐"'))"
"locales_en_plans=$($lc.Contains('"page.console.nav.billing": "Plans"'))"
if (-not ($cl.Contains('btn-ov-week') -and $cl.Contains('btn-mk-week') -and $cj.Contains('doMarketsCheckout(""weekly"")') -and $lc.Contains('"page.console.nav.billing": "套餐"') -and $lc.Contains('"page.console.nav.billing": "Plans"'))) { Write-Host "!! console 周套餐验收失败" -ForegroundColor Red; exit 1 }

$oldV = 0; $newV = 0
Get-ChildItem "$REPO\web" -Recurse -Filter *.html | ForEach-Object {
    $s = Get-Content $_.FullName -Raw -Encoding UTF8
    if ($s.Contains('locales.js?v=20260818g') -or $s.Contains('console.js?v=20260818h')) { $oldV++ }
    if ($s.Contains('locales.js?v=20260818i') -and $s.Contains('shell.js?v=20260818i')) { $newV++ }
}
"version_old=$oldV version_new_i=$newV"
if ($oldV -ne 0 -or $newV -lt 40) { Write-Host "!! 版本号验收失败" -ForegroundColor Red; exit 1 }

"--- 5) 公网验收 ---"
$pubA = Invoke-WebRequest -Uri "https://markets.ai24x.com/app.html?x=wc9434b3" -TimeoutSec 25 -UseBasicParsing
$pubI = Invoke-WebRequest -Uri "https://markets.ai24x.com/?x=wc9434b3" -TimeoutSec 25 -UseBasicParsing
$pubP = Invoke-WebRequest -Uri "https://markets.ai24x.com/pricing.html?x=wc9434b3" -TimeoutSec 25 -UseBasicParsing
"pub_app_week=$($pubA.Content.Contains('btn-sub-week') -and $pubA.Content.Contains('sub.week'))"
"pub_index_week=$($pubI.Content.Contains('9.9/week'))"
"pub_pricing_week=$($pubP.Content.Contains('grid-4') -and $pubP.Content.Contains('9.9'))"
$pubC = Invoke-WebRequest -Uri "https://www.ai24x.com/console.html?x=wc9434b3" -TimeoutSec 25 -UseBasicParsing
$pubH = Invoke-WebRequest -Uri "https://www.ai24x.com/index.html?x=wc9434b3" -TimeoutSec 25 -UseBasicParsing
"pub_console_i=$($pubC.Content.Contains('locales.js?v=20260818i') -and $pubC.Content.Contains('console.js?v=20260818i') -and $pubC.Content.Contains('btn-ov-week'))"
"pub_index_i=$($pubH.Content.Contains('locales.js?v=20260818i') -and $pubH.Content.Contains('shell.js?v=20260818i'))"

if ($pubA.Content.Contains('btn-sub-week') -and $pubI.Content.Contains('9.9/week') -and $pubP.Content.Contains('grid-4') -and $pubC.Content.Contains('btn-ov-week') -and $pubC.Content.Contains('locales.js?v=20260818i') -and $pubH.Content.Contains('locales.js?v=20260818i')) {
    "--- 全部 PASS ---"
    Write-Host "=== 部署成功 OK ===" -ForegroundColor Green
} else {
    Write-Host "!! 验收未全通过" -ForegroundColor Red; exit 1
}
```

## 完成后群回执（固定步骤，主通道必做）
- 用 04 应用发指挥部群：
  `python api\feishu_notify.py --config <openclaw.json> --group "【AI24X 部署回执】Markets 周VIP套餐 + console 统一开通 + 充值改名套餐 c9434b3 成功：HEAD=c9434b3、markets/core health OK、billing.py weekly(9.9美元/7天)、console Overview 周/月/年三档直付、充值→套餐(zh)/Billing→Plans(en)、markets app 订阅面板周套餐+深链、pricing 4卡含Weekly、全站版本号 20260818i 残留0、公网验收通过。问题项：无" --at`

## 回执格式（统一）
- ✅ 已完成项：HEAD / checkout / pull / sites 静态同步(app+index+pricing) / markets+core 重启 / billing weekly / console 三档开通+改名 / app 周套餐 / pricing 4卡 / 版本 20260818i / 公网验收 / 群回执
- ⚠️ 问题项：逐条列出（无则写「无」）

## 回滚预案
- `git revert c9434b3` + 重启 markets(18012) + core(8002)；markets 静态恢复 `C:\sites\markets.ai24x.com\*.bak-20260817-c9434b3`。
