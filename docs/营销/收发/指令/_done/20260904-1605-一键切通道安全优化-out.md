# 一键切通道安全优化

> 2026-09-04

## 新增
1. **审计**：`api/data/hero_apply_audit.jsonl` + pricing_audit
2. **切后毛利变差**：GM1:4 下降 ≥5pt 或级别变差 → `risk_warning` + 巡检 `hero_gm_risk_*`（24h）
3. **持续待办**：同建议连续满 `HERO_PENDING_HOURS`（默认 2h）→ 页内条 + 巡检 `hero_pending_*`（**不自动切**）
4. **熔断禁切**：目标通道熔断 → 一键拒绝；`avoid_unstable` 改选未开路通道
5. 档位已是 OpenRouter → 按钮灰掉

## 管理台
供应链监控：持续待确认 / 切后风险 / 最近一键；确认框带说明。

## env（可选）
- `HERO_PENDING_HOURS=2`
- `HERO_GM_RISK_DROP=5`
