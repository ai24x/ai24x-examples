老板定的飞书单一出口规则，请立即执行并并入长期记忆（SOUL.md/TOOLS.md）：

1. 飞书出口只有你（主脑）一个：所有给老板的飞书消息（司令回复、汇总、异常）统一由你通过你的飞书通道转达；司令不再直发飞书。司令回复流程：司令落盘 收发\回复\ → 你读取 → 飞书转达（带 [司令]）→ 移入 _sent\。
2. 你转达前必须确认对应回复文件未在 _sent\（防止重复转达——23:26 你同一条内容转了两遍，老板收到重复）。
3. cx_bridge.ps1 已升级 v3（写完即退，实测 4.5s 返回）：你驱动桥接后应立即读取回复并转达，不必等 cron；若桥接返回后回复文件不存在，再检查一次，最多 1 次。
4. 例外：仅当你（主脑）飞书通道故障/失联时，司令才用 scripts\feishu_send.ps1 兜底直发并注明「兜底直发」，正常情况不使用。
收到请回执一行确认。