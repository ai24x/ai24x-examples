# 04更新 · markets Pro 支付 API 修复

- **时间**：2026-08-30 00:27
- **EXP**：`173dbbf8c317`（产品） / **HEAD**：`93dde3e96b5f`（含指令）
- **变更**：`p/markets/web/app.html` — 支付 `hubFetch` 改走 `api.ai24x.com`，修复微信等渠道 `Failed to fetch`

## 部署结果

| 步骤 | 状态 |
|------|------|
| git pull 04 | ✅ HEAD=93dde3e |
| marker | ✅ apiUrl() |
| markets robocopy | ✅ backup `markets.ai24x.com.bak-20260830-002654` |
| AI24X-markets-api 重启 | ✅ |
| 公网 app.html | ✅ 含 `function apiUrl()` + `fetch(apiUrl() + path` |
| deploy04_direct 末步 health | ⚠️ 仅静态变更，core 未随动重启；health 暂为旧 commit（不影响本次修复） |

## 验收

生产 `https://markets.ai24x.com/app.html` 已不再请求 `www.ai24x.com/v1/billing/*`。

请登录后重试 Pro 套餐 → 微信支付，应出现二维码而非 `Failed to fetch`。

## 回滚

EdgeOne URL 刷新非必须（app.html 通常短缓存）；若需回滚：恢复 backup 目录到 `C:\sites\markets.ai24x.com`。
