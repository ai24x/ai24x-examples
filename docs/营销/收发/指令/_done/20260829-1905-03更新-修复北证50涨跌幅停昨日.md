# 【03 更新】修复 AI复盘 北证50 涨跌幅停在昨日

> 通道：司令直连 03（deploy03.ps1）｜ 目标提交 **EXP=`473cac65d387`**
> 03 主机：123.207.199.238 · 仓库 `C:\ai24x01` · 服务 `AI24X-a1-api`（NSSM，8001）· 无 D 盘（备份落 `C:\backup\ai24x_a\`）
> a1 `/health` 无 commit 字段：验收用「文件标记 + 进程新建时间 + 公网 API」

## 背景
公网 AI复盘「今日大盘」北证50 仍显示 08-27（+0.65% / 1074.96），上证等已是 08-28；本地归档正确（−1.04% / 1063.80）。根因：北证指数东财易 RST、空成功包堵缓存，新浪偶发缺最新一根；且 03 归档未与本地最终版对齐。本包修链路兜底；**收盘数据另用 `sync_final_to_03.ps1` 从本地权威同步**（deploy 不带数据）。

## 执行步骤（PowerShell，整段复制运行）
```powershell
$ErrorActionPreference = "Stop"
$REPO = "C:\ai24x01"
$SRV  = "AI24X-a1-api"
$EXP  = "473cac65d387"

Set-Location $REPO

"--- 0) 备份本地脏改动再还原（防止 pull 冲突）---"
$FILES = @(
  "p\a1\api\server\app\providers.py",
  "p\a1\api\server\app\daily_report.py"
)
$BK = "C:\backup\ai24x_a\$(Get-Date -Format yyyyMMdd)_pre_bj50_$EXP"
New-Item -ItemType Directory -Force -Path $BK | Out-Null
foreach ($x in $FILES) {
  if (Test-Path "$REPO\$x") {
    $chg = git -C $REPO status --porcelain -- $x
    if ($chg) { Copy-Item "$REPO\$x" "$BK\$(Split-Path $x -Leaf).bak" -Force; git -C $REPO checkout -- $x }
  }
}
"备份目录: $BK"

"--- 1) 拉码 + HEAD 校验 ---"
git pull
$HEAD = (git rev-parse --short=12 HEAD).Trim()
"HEAD=$HEAD"
git merge-base --is-ancestor $EXP HEAD
if ($LASTEXITCODE -ne 0) { Write-Host "!! HEAD 不含目标提交 $EXP" -ForegroundColor Red; exit 1 }

"--- 2) 文件标记检查 ---"
$f1 = Get-Content "$REPO\p\a1\api\server\app\providers.py" -Raw -Encoding UTF8
$f2 = Get-Content "$REPO\p\a1\api\server\app\daily_report.py" -Raw -Encoding UTF8
$okPri = $f1.Contains('items = ["eastmoney", "sina"] + rest')
$okCache = $f1.Contains('禁止缓存空成功包')
$okRetry = $f2.Contains('sina:lag-retry')
"mark_priority=$okPri mark_cache=$okCache mark_retry=$okRetry"
if (-not ($okPri -and $okCache -and $okRetry)) {
  Write-Host "!! 文件标记缺失（priority=$okPri cache=$okCache retry=$okRetry）" -ForegroundColor Red
  exit 1
}

"--- 3) 重启 AI24X-a1-api (8001) ---"
$beforePid = (Get-CimInstance Win32_Service -Filter "Name='$SRV'").ProcessId
"svc_pid_before=$beforePid"
Restart-Service $SRV -Force
$ok = $false
$afterPid = $beforePid
for ($i = 0; $i -lt 20; $i++) {
  Start-Sleep -Seconds 5
  try {
    $svc = Get-CimInstance Win32_Service -Filter "Name='$SRV'"
    if ($svc) { $afterPid = $svc.ProcessId }
    $null = Invoke-RestMethod -Uri "http://127.0.0.1:8001/health" -TimeoutSec 10 -UseBasicParsing
    if ($afterPid -gt 0 -and $afterPid -ne $beforePid) { $ok = $true; break }
  } catch {}
}
"svc_pid_after=$afterPid  health_ok=$ok"
if (-not $ok) {
  Write-Host "!! Restart-Service 未真正重启（PID 未变），兜底 nssm restart" -ForegroundColor Yellow
  & nssm restart $SRV 2>$null
  if ($LASTEXITCODE -ne 0) { & "C:\Program Files\NSSM\nssm.exe" restart $SRV 2>$null }
  for ($i = 0; $i -lt 20; $i++) {
    Start-Sleep -Seconds 5
    try {
      $svc2 = Get-CimInstance Win32_Service -Filter "Name='$SRV'"
      if ($svc2) { $afterPid = $svc2.ProcessId }
      $null = Invoke-RestMethod -Uri "http://127.0.0.1:8001/health" -TimeoutSec 10 -UseBasicParsing
      if ($afterPid -gt 0 -and $afterPid -ne $beforePid) { $ok = $true; break }
    } catch {}
  }
}
if (-not $ok) { Write-Host "!! 服务仍未重启（before=$beforePid after=$afterPid），需人工处理" -ForegroundColor Red; exit 1 }

"--- 4) 公网 API：北证50 日期与上证对齐（同步归档后应通过）---"
$hist = Invoke-RestMethod -Uri "https://a.ai24x.com/api/report/history" -TimeoutSec 20 -UseBasicParsing
$day = $hist.items[0].date
$rep = Invoke-RestMethod -Uri ("https://a.ai24x.com/api/report/" + $day) -TimeoutSec 20 -UseBasicParsing
$ix = $rep.indexes.indexes
if (-not $ix) { $ix = $rep.indexes }
$bj = $ix.'北证50'
$sh = $ix.'上证指数'
"day=$day bj_date=$($bj.last_date) bj_pct=$($bj.pct) sh_date=$($sh.last_date) sh_pct=$($sh.pct)"
$bjD = [string]$bj.last_date
$shD = [string]$sh.last_date
$bjOk = ($bjD.Length -ge 10 -and $shD.Length -ge 10 -and $bjD.Substring(0,10) -eq $shD.Substring(0,10) -and $null -ne $bj.pct)
"bj50_aligned=$bjOk"
if (-not $bjOk) {
  Write-Host "!! 北证50 仍未与上证同日——请确认本机已跑 sync_final_to_03.ps1" -ForegroundColor Yellow
}

"--- 5) 回执 ---"
"OK EXP=$EXP HEAD=$HEAD bj50_aligned=$bjOk day=$day"
```

## 回执模板（群）
```
✅ 03更新完成｜北证50涨跌幅兜底｜EXP=473cac65d387 HEAD=<12位>｜bj50_aligned=<true/false> day=<YYYYMMDD>
```

## 司令侧必须另做（数据）
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File p\a1\scripts\sync_final_to_03.ps1
```
（本地权威归档 → 03；deploy 不带收盘数据）
