# 【04 更新】BYOK Phase2：用量日趋势 + Redis 缓存 + 续费提醒（cc094e4）

> 通道：司令直连 04（deploy04.ps1）｜ 目标提交 **cc094e4**（含 `1f2cfca` Phase2 + BYOK 分组 i18n）  
> 04 主机：43.160.246.30 · 仓库 `C:\ai24x01`  
> 回执：`docs/营销/收发/回复/20260828-0015-04更新-BYOK-Phase2图表缓存续费-cc094e4-out.md`；有异常才上报  
> **禁止**：改价、改 Dodo product_id、轮换密钥、`git add -A` / `reset --hard`、整文件覆盖 `.env`

# 需求类型：②需副脑办(副脑04)
# 需求描述：部署 BYOK Phase2（图表 / Redis 缓存 / 续费提醒）并注册日更邮件任务
# 验收标准：见下文
# 期望时间：立即（夜间）

---

## 变更要点

1. **用量图表**：`GET /v1/byok/usage/daily` + open 控制台日趋势（费用/Tokens/请求）
2. **Redis 缓存**：`REDIS_URL` 可达则多实例共享请求缓存；不可用自动回落内存（`cache_stats.backend`）
3. **续费提醒**：订阅 `days_left≤7` 控制台横幅；邮件脚本 7/3/1 天；管理口 `POST /v1/admin/byok/renewal-remind`
4. www 用量图：深色网格 + 最大值标注（小改）

---

## 执行步骤（04）

```powershell
$ErrorActionPreference = "Stop"
$work = "C:\ai24x01"
$EXP  = "cc094e4"
Set-Location $work

# 同名回执挡 pull 时先挪 .local
Get-ChildItem "docs\营销\收发\回复\*1f2cfca*" -ErrorAction SilentlyContinue | ForEach-Object {
  Move-Item $_.FullName ($_.FullName + ".local") -Force -ErrorAction SilentlyContinue
}

git fetch origin
git pull --ff-only origin master
if ($LASTEXITCODE -ne 0) { throw "pull failed" }
$HEAD = (git rev-parse --short=12 HEAD).Trim()
"HEAD=$HEAD"
git merge-base --is-ancestor $EXP HEAD
if ($LASTEXITCODE -ne 0) { throw "HEAD 不含 $EXP" }

$byok = Get-Content "p\open\api\byok.py" -Raw -Encoding UTF8
if ($byok -notlike "*_redis_client*") { throw "缺 Redis cache" }
if ($byok -notlike "*def usage_daily*") { throw "缺 usage_daily" }
if (-not (Test-Path "p\open\api\byok_renewal.py")) { throw "缺 byok_renewal.py" }

# Redis：仅确认，不改密码；无 Redis 则 backend=memory 亦可上线
$envOpen = "C:\ai24x01\p\open\api\.env"
if (Test-Path $envOpen) {
  $raw = [System.IO.File]::ReadAllText($envOpen)
  if ($raw -match '(?m)^\s*REDIS_URL\s*=') { "REDIS_URL present" } else { "REDIS_URL absent -> memory fallback OK" }
}

# 重启 open-api（优先服务名）
$restarted = $false
foreach ($n in @("AI24X-open-api","AI24X-open","ai24x-open-api")) {
  $s = Get-Service -Name $n -ErrorAction SilentlyContinue
  if ($s) { Restart-Service $n -ErrorAction Stop; Start-Sleep 6; "restarted $n"; $restarted=$true; break }
}
if (-not $restarted) { pm2 restart open-api --update-env 2>$null; "pm2 open-api attempted" }
try { Restart-Service AI24X-core -ErrorAction SilentlyContinue; Start-Sleep 4 } catch {}

# 注册续费邮件日任务（幂等）
powershell -NoProfile -ExecutionPolicy Bypass -File "C:\ai24x01\scripts\register_byok_renewal_cron.ps1"

# dry-run 续费（不发信）
Set-Location "C:\ai24x01\p\open\api"
python scripts_byok_renewal_remind.py 2>&1 | Select-Object -Last 20

# 公网
$h = Invoke-RestMethod "https://api.ai24x.com/health" -TimeoutSec 20
"health=$($h.commit)"
$op = (Invoke-WebRequest -UseBasicParsing "https://open.ai24x.com/console.html").Content
if ($op -notlike "*20260828b*" -and $op -notlike "*20260828a*") { Write-Host "open console cache bump soft-warn" -ForegroundColor Yellow }

Write-Host "OK Phase2 1f2cfca" -ForegroundColor Green
```

---

## 回滚

1. `git checkout bb0d2ec -- p/open/api/byok.py p/open/api/byok_routes.py p/open/api/email_smtp.py p/open/api/main.py p/open/web/ web/console.html web/js/console.js`
2. 删除 `p/open/api/byok_renewal.py`（可选）
3. 重启 open-api / core
4. `Unregister-ScheduledTask -TaskName AI24X-BYOK-RenewalRemind -Confirm:$false`（可选）

---

## 验收

- [ ] HEAD 含 `cc094e4`
- [ ] open-api 已重启
- [ ] open console `?v=20260828b`
- [ ] `/v1/byok/cache/stats`（登录后）含 `backend`
- [ ] 计划任务 `AI24X-BYOK-RenewalRemind` 已注册
- [ ] 未改价 / 未改密钥 / 未整文件覆盖 `.env`
