# 【04 更新】www 主站剥离 Token 聚合前台 · AI行情官国际版主线 · 开发者下沉 open.ai24x.com · 目标 2926108

> 通道：司令直连 04（deploy04.ps1 同步 600s）。验收全 PASS 后自动发指挥部群回执；有问题先回传司令，不发群。

## 背景与定调（老板已确认）
- www.ai24x.com 统一为「AI行情官国际版」产品主页（赚现金流），剥离全部 Token 聚合前台元素；Token 能力下沉子项目 open.ai24x.com。
- 对外只说一句「看懂美股，3 分钟」；幕后叙事必须出现 **Powered by the AI24X autonomous agent fleet**。
- 国内完全分离：a.ai24x.com 独立运营，国际主站零入口（页脚/产品页已移除）。
- 官网仅英文（合规红线）：隐藏语言选择器、i18n 强制 en（`?lang=zh` 不再生效）；markets 应用不受影响（保留中英切换）。

## 本轮改动（代码目标 292610865af7 = 988307d6e711（主站剥离）+ a2d64500d95c/292610865af7（locales 全语言清死键 footer.link.marketwatch，全站 0 引用），纯 www 静态 + 前端共享 JS）
- **shell.js**：导航去 Console/Register/Developer 下拉 → `Markets · Pricing · Product · Help · About · Developer(→open.ai24x.com 单外链) · Log in`；登录态动态显示 Account + Sign out（鉴权走 cookie/localStorage，点退出清会话回 login）；页脚去 a.ai24x.com/refer/console/api 入口，底部加 open.ai24x.com + agent fleet 行；隐藏右上角语言选择器。
- **i18n.js**：www 强制英文（FORCE_EN=true）。
- **locales.js**：导航/页脚/首页/定价/产品/关于/帮助/注册/登录/console 文案全部切 Markets 主线；去国内引用。
- **index.html**：三步引导改「搜索 → 看图 → 点 AI 读懂」；底部 Powered by the AI24X autonomous agent fleet（含支撑句）。
- **pricing.html**：4 卡 Markets（Free $0 / Pro 周 $9.9 / 月 $24.9 / 年 $199 省33% 推荐）；删 token 套餐区/GPT 对比表/DeepSeek 峰谷；开发者区精简为 open.ai24x.com 单链接。
- **console.html**：导航收敛为 Overview/Billing/Account/Support/Logout（去 keys/transactions/playground 入口，面板代码保留可深链）；Overview 加开发者卡 → open.ai24x.com；Billing token 区标注已迁 open.ai24x.com（过渡期保留）。
- **product/help/about**：去 A股/API/token 内容；about 加 agent fleet；help 开发者 FAQ 整块收敛为 open.ai24x.com 单链接。
- **api.html / docs.html / models/index.html / guides/index.html**：改为 open.ai24x.com 跳转 stub（meta refresh 0）。
- **paypal.html**：改为 Markets Pro PayPal 指南（去 token 充值步骤与中文摘要）。
- **版本号**：locales/shell/console/i18n 全站统一 `v=20260818j`（39 页引用，旧版 20260818i / i18n 20260812g 残留 0）。
- **QA**：`_qa_www_20260817.js`（24 项，导航单链接/4卡定价/英文强制/fleet）、`_qa_vip_guide`（44 项）、`_qa_weekly_plan`、`_qa_oauth_www`、`_qa_console_vip`、`_qa_www_theme`（含 4 个跳转 stub 校验）本地全绿；另新增 `_qa_signout` 验证登录态退出（临时脚本已删，行为已并入主套件）。

## 注意事项
- 本批**纯 www web/ 静态**：git pull + 重启 **AI24X-core(8002)** 即生效；无需动 markets 静态/服务。
- HEAD 校验用「目标前缀 + merge-base + 关键路径 diff --quiet 与目标一致」（指令文档自身也是新提交）。
- 所有 Invoke-WebRequest/-RestMethod 带 `-UseBasicParsing`；PowerShell 字符串断言用单引号（`'$9.9'` 的 `$` 才不会被吞）。
- 版本号断言：`locales\.js\?v=20260818j`、`shell\.js\?v=20260818j`、`console\.js\?v=20260818j`、`i18n\.js\?v=20260818j`（`?` 用 `\.\?` 或 `-SimpleMatch`，勿裸用正则）。

## 副脑04 执行脚本（整段复制运行）
```powershell
# === AI24X 更新部署验收 · 目标 2926108 ===
$ErrorActionPreference = "Stop"
$REPO = "C:\ai24x01"
$SRV_WWW = "AI24X-core"
$EXP  = "292610865af7"
$TAG = "20260817-2926108"

Set-Location $REPO

"--- 0) 本批路径如有本地改动：备份后 checkout ---"
$keyPaths = @(
  "web/js/shell.js",
  "web/js/i18n.js",
  "web/config/locales.js",
  "web/console.html",
  "web/index.html",
  "web/pricing.html",
  "web/product.html",
  "web/help.html",
  "web/about.html",
  "web/login.html",
  "web/register.html",
  "web/paypal.html",
  "web/api.html",
  "web/docs.html",
  "web/models/index.html",
  "web/guides/index.html"
)
foreach ($f in $keyPaths) {
    $dirty = git status --short -- $f
    if ($dirty) {
        $bn = ($f -replace "/", "_")
        Copy-Item "$REPO\$f" "C:\Users\Administrator\ops\dirty-$bn-$TAG" -Force
        git checkout -- $f
        if ($LASTEXITCODE -ne 0) { Write-Host "!! checkout 失败: $f" -ForegroundColor Red; exit 1 }
        "dirty backup + checkout: $f"
    } else {
        "clean: $f"
    }
}
# web 目录 html 版本号批量：有本地改动同样备份 + checkout
git status --porcelain -- web | ForEach-Object {
    $p = ($_.Substring(3)).Trim()
    if ($p -and $p -match '\.(html|js)$' -and $p -notmatch '^\?') {
        $bn = ($p -replace "/", "_")
        Copy-Item "$REPO\$p" "C:\Users\Administrator\ops\dirty-$bn-$TAG" -Force
        git checkout -- $p
        "dirty backup + checkout: $p"
    }
}

"--- 1) 拉取并校验目标提交 ---"
git pull
$HEAD = (git rev-parse --short=12 HEAD).Trim()
"HEAD=$HEAD"
git merge-base --is-ancestor $EXP HEAD
if ($LASTEXITCODE -ne 0) { Write-Host "!! 目标 $EXP 不在 HEAD 历史中" -ForegroundColor Red; exit 1 }
git diff --quiet $EXP HEAD -- web
if ($LASTEXITCODE -ne 0) { Write-Host "!! web 目录在 $EXP 之后仍有改动，停止" -ForegroundColor Red; exit 1 }
"target_verified=ok"

"--- 2) 重启 core (8002) ---"
Restart-Service $SRV_WWW -Force
$okW = $false
for ($i = 0; $i -lt 12; $i++) {
    Start-Sleep -Seconds 5
    try { $hw = Invoke-RestMethod -Uri "http://127.0.0.1:8002/health" -TimeoutSec 10 -UseBasicParsing; $okW = $true; break } catch {}
}
if (-not $okW) { Write-Host "!! core /health 未就绪" -ForegroundColor Red; exit 1 }
"www-health=$($hw.status) commit=$($hw.commit)"
if ($hw.commit -notlike "$EXP*") { Write-Host "!! core 服务未生效 commit=$($hw.commit)" -ForegroundColor Red; exit 1 }

"--- 3) 本地验收（仓库文件 + 8002 静态）---"
$sh = Get-Content "$REPO\web\js\shell.js" -Raw -Encoding UTF8
$i18 = Get-Content "$REPO\web\js\i18n.js" -Raw -Encoding UTF8
$idx = Get-Content "$REPO\web\index.html" -Raw -Encoding UTF8
$pri = Get-Content "$REPO\web\pricing.html" -Raw -Encoding UTF8
$con = Get-Content "$REPO\web\console.html" -Raw -Encoding UTF8
$api = Get-Content "$REPO\web\api.html" -Raw -Encoding UTF8
$doc = Get-Content "$REPO\web\docs.html" -Raw -Encoding UTF8
$mdl = Get-Content "$REPO\web\models\index.html" -Raw -Encoding UTF8
$gdi = Get-Content "$REPO\web\guides\index.html" -Raw -Encoding UTF8
$loc = Get-Content "$REPO\web\config\locales.js" -Raw -Encoding UTF8

"nav_no_console_register=$(-not $sh.Contains('nav(""console.html"", ""nav.console"", ""console"")') -and -not $sh.Contains('nav(""register.html"", ""nav.register"", ""register"")'))"
"nav_dev_single_link=$($sh.Contains('nav(""https://open.ai24x.com"", ""nav.developer"", ""developer"")'))"
"nav_login_state=$($sh.Contains('function loginNav') -and $sh.Contains('nav-signout'))"
"nav_no_lang_select=$(-not $sh.Contains('id=""lang-select""'))"
"footer_fleet=$($sh.Contains('Powered by the AI24X autonomous agent fleet'))"
"www_force_en=$($i18.Contains('FORCE_EN = true'))"
"index_fleet=$($idx.Contains('Powered by the AI24X autonomous agent fleet'))"
"index_ask_ai=$($idx.Contains('page.index.how.s3t'))"
"pricing_weekly=$($pri.Contains('$9.9') -and $pri.Contains('page.pricing.proWeekly'))"
"pricing_dev_open=$($pri.Contains('open.ai24x.com') -and -not $pri.Contains('tokenPlansGrid'))"
"pricing_no_token=$(-not $pri.Contains('page.pricing.saveTitle') -and -not $pri.Contains('peakSection'))"
"console_nav_compact=$(-not $con.Contains('data-console-panel=""keys""') -and -not $con.Contains('data-console-panel=""playground""'))"
"console_dev_open=$($con.Contains('page.console.dev.open') -and $con.Contains('open.ai24x.com'))"
"redirect_stubs=$($api.Contains('open.ai24x.com') -and $doc.Contains('open.ai24x.com') -and $mdl.Contains('open.ai24x.com') -and $gdi.Contains('open.ai24x.com'))"
"locales_no_cn_ref=$(-not $loc.Contains('footer.link.marketwatch'))"

$oldI = 0; $newJ = 0
Get-ChildItem "$REPO\web" -Recurse -Filter *.html | ForEach-Object {
    $s = Get-Content $_.FullName -Raw -Encoding UTF8
    if ($s -match 'locales\.js\?v=20260818i' -or $s -match 'shell\.js\?v=20260818i' -or $s -match 'i18n\.js\?v=20260812g') { $oldI++ }
    if ($s -match 'locales\.js\?v=20260818j' -and $s -match 'shell\.js\?v=20260818j' -and $s -match 'i18n\.js\?v=20260818j') { $newJ++ }
}
"version_old=$oldI new_j_pages=$newJ"
if (-not ($sh.Contains('nav(""https://open.ai24x.com"", ""nav.developer"", ""developer"")') -and $sh.Contains('nav-signout') -and -not $sh.Contains('id=""lang-select""') -and $sh.Contains('Powered by the AI24X autonomous agent fleet') -and $i18.Contains('FORCE_EN = true') -and $idx.Contains('Powered by the AI24X autonomous agent fleet') -and $pri.Contains('page.pricing.proWeekly') -and -not $pri.Contains('tokenPlansGrid') -and -not $con.Contains('data-console-panel=""keys""') -and $api.Contains('open.ai24x.com') -and -not $loc.Contains('footer.link.marketwatch') -and $oldI -eq 0 -and $newJ -ge 39)) {
    Write-Host "!! 本地验收未全通过" -ForegroundColor Red; exit 1
}
"local_check=ok"

"--- 4) 公网验收 ---"
$pubH = Invoke-WebRequest -Uri "https://www.ai24x.com/index.html?x=w2926108" -TimeoutSec 25 -UseBasicParsing
$pubP = Invoke-WebRequest -Uri "https://www.ai24x.com/pricing.html?x=w2926108" -TimeoutSec 25 -UseBasicParsing
$pubA = Invoke-WebRequest -Uri "https://www.ai24x.com/api.html?x=w2926108" -TimeoutSec 25 -UseBasicParsing
$pubC = Invoke-WebRequest -Uri "https://www.ai24x.com/console.html?x=w2926108" -TimeoutSec 25 -UseBasicParsing
"pub_index_fleet=$($pubH.Content.Contains('Powered by the AI24X autonomous agent fleet')) pub_index_j=$($pubH.Content.Contains('locales.js?v=20260818j'))"
"pub_pricing_open=$($pubP.Content.Contains('open.ai24x.com') -and $pubP.Content.Contains('page.pricing.proWeekly')) pub_pricing_no_token=$(-not $pubP.Content.Contains('tokenPlansGrid'))"
"pub_api_redirect=$($pubA.Content.Contains('open.ai24x.com') -and $pubA.Content.Contains('http-equiv=""refresh""'))"
"pub_console_compact=$(-not $pubC.Content.Contains('data-console-panel=""keys""')) pub_console_j=$($pubC.Content.Contains('console.js?v=20260818j'))"

if ($pubH.Content.Contains('Powered by the AI24X autonomous agent fleet') -and $pubH.Content.Contains('locales.js?v=20260818j') -and $pubP.Content.Contains('page.pricing.proWeekly') -and -not $pubP.Content.Contains('tokenPlansGrid') -and $pubA.Content.Contains('open.ai24x.com') -and -not $pubC.Content.Contains('data-console-panel=""keys""')) {
    "--- 全部 PASS ---"
    Write-Host "=== 部署成功 OK ===" -ForegroundColor Green
} else {
    Write-Host "!! 公网验收未全通过" -ForegroundColor Red; exit 1
}
```

## 完成后群回执（固定步骤，主通道必做）
- 用 04 应用发指挥部群：
  `python api\feishu_notify.py --config <openclaw.json> --group "【AI24X 部署回执】主站剥离Token聚合·AI行情官国际版主线 2926108 成功：HEAD=2926108、core(8002) health OK、导航去Console/Register+Developer→open.ai24x.com、首页3分钟三步引导+agent fleet、pricing 4卡(周9.9/月24.9/年199)+无token套餐、api/docs/models/guides跳转open、console收敛账户+Markets Pro、官网强制英文、全站版本20260818j(39页无残留)、locales死键清除、公网验收通过。问题项：无" --at`

## 回执格式（统一）
- ✅ 已完成项：HEAD / pull / core 重启 / 导航与页脚 / 首页 fleet / pricing 4卡 / console 收敛 / 开发者中心跳转 / 官网英文 / 版本号 20260818j / 公网验收 / 群回执
- ⚠️ 问题项：逐条列出（无则写「无」）

## 回滚预案
- `git revert 2926108` + 重启 AI24X-core(8002)；纯静态，无 markets/DB 影响。
