# 【04 更新】www Markets 套餐主打 + Developer 下沉 + 深链订阅高亮 · 目标 2d11113

> 通道：司令直连 04（deploy04.ps1 同步 600s）。验收全 PASS 后自动发指挥部群回执；有问题先回传司令，不发群。

## 背景
- 老板 2026-08-17 凌晨指令：**Markets 套餐主打，API token 套餐下沉**（后续 open.ai24x.com 承接，走金融技术指标接口差异化，模型 API 仍通用），按建议优化完善后部署 04。
- 本轮改动（2d11113d6f59）：
  - **导航重构**（shell.js）：Markets 置顶 + Developer 下拉（API / Models / Guides / Docs），去掉 Home；`data-markets` 链接改为仅重写 markets 根地址，**深链原样保留**（修复升级按钮 `app.html#sub` / `?plan=yearly#sub` 被改丢的问题）
  - **pricing.html**：Markets 三卡（Free $0 / Pro 月付 $24.9 / Pro 年付 $199「省 33% 最划算」推荐橙色边框），按钮深链 markets app；开发者区下沉改名「For developers · Token API」+ dev-links 三链接
  - **index.html**：hero 主按钮深链 `app.html`（Start free / 免费开始）+ 底部开发者一条线
  - **product.html**：Markets 卡全宽置顶 + 「升级 Pro」按钮 + 开发者分隔标题
  - **help.html**：FAQ/schema/页面残留 `$14.9/$119` → `$24.9/$199`
  - **markets app.html**：已登录免费用户显示「升级」入口（auth-upgrade）；`?plan=yearly#sub` 深链滚动 + 高亮对应档位（修复 `btn-sub-yearly` id 拼接 bug，改为 `btn-sub-year`，8 秒高亮 + 行情加载后重触发）；i18n upgrade 键
  - 新增 QA 脚本 `p/markets/scripts/_qa_www_20260817.js`（本机 23/23 全 PASS，含深链/高亮/中英/价格/导航）
- 共享资源 locales.js / shell.js / base.css 均已改，**全站统一升 v=20260818c**（43 页），旧版 20260818b 残留应为 0。

## 注意事项
- 本批动 www 静态 `web/`（pull 即生效，重启 core 8002）+ markets 静态 `p/markets/web/app.html`（需同步 `C:\sites\markets.ai24x.com\app.html` + 重启 markets 18012）；**无后端 py 改动**。
- PowerShell 断言一律单引号字面量，`$24.9` / `locales.js?v=` 不要被当变量/正则。
- HEAD 校验用「目标提交前缀 + 关键路径 diff --quiet 与目标一致」（指令文档自身也是新提交，不能用精确 HEAD 前缀比对——用 merge-base 方式）。
- 04 远程默认 cmd，deploy04.ps1 已按 `powershell -NoProfile -ExecutionPolicy Bypass -File` 驱动；所有 Invoke-WebRequest/-RestMethod 带 `-UseBasicParsing`。

## 副脑04 执行脚本（整段复制运行）
```powershell
# === AI24X 更新部署验收 · 目标 2d11113 ===
$ErrorActionPreference = "Stop"
$REPO = "C:\ai24x01"
$SRV_WWW = "AI24X-core"
$SRV_MK  = "AI24X-markets-api"
$EXP  = "2d11113d6f59"
$SITES = "C:\sites\markets.ai24x.com"

Set-Location $REPO

"--- 1) 本批路径如有本地改动：备份后 checkout ---"
foreach ($f in @("web", "p/markets/web/app.html")) {
    $dirty = git status --short -- $f
    if ($dirty) {
        $bn = ($f -replace "/", "_")
        Copy-Item "$REPO\$f" "C:\Users\Administrator\ops\dirty-$bn-20260817-0030" -Recurse -Force
        git checkout -- $f
        if ($LASTEXITCODE -ne 0) { Write-Host "!! checkout 失败: $f" -ForegroundColor Red; exit 1 }
        "dirty backup + checkout: $f"
    } else {
        "clean: $f"
    }
}

"--- 2) 拉取并校验目标提交 ---"
git pull
$HEAD = (git rev-parse --short=12 HEAD).Trim()
"HEAD=$HEAD"
if ($HEAD -notlike "$EXP*") { Write-Host "!! HEAD 不匹配，期望前缀 $EXP 实际 $HEAD" -ForegroundColor Red; exit 1 }
git diff --quiet $EXP HEAD -- web p/markets/web
if ($LASTEXITCODE -ne 0) { Write-Host "!! web/markets 在 $EXP 之后仍有改动，停止" -ForegroundColor Red; exit 1 }
"target_verified=ok"

"--- 3) 同步 markets app.html 到 nginx 服务目录（先备份）---"
if (Test-Path "$SITES\app.html") { Copy-Item "$SITES\app.html" "$SITES\app.html.bak-20260817-0030" -Force }
Copy-Item "$REPO\p\markets\web\app.html" "$SITES\app.html" -Force
$sa = Get-Content "$SITES\app.html" -Raw -Encoding UTF8
"sites_flash_plan=$($sa.Contains('function flashPlan'))"
"sites_upgrade_entry=$($sa.Contains('id="auth-upgrade"'))"
"sites_plan_btn_fix=$($sa.Contains('plan === ''yearly'' ? ''year'' : ''month'''))"
if (-not ($sa.Contains('function flashPlan') -and $sa.Contains('id="auth-upgrade"') -and $sa.Contains('plan === ''yearly'' ? ''year'' : ''month'''))) { Write-Host "!! sites app.html 内容缺失" -ForegroundColor Red; exit 1 }

"--- 4) 重启 core (8002) + markets (18012) ---"
Restart-Service $SRV_WWW -Force
Restart-Service $SRV_MK -Force
$okW = $false; $okM = $false
for ($i = 0; $i -lt 15; $i++) {
    Start-Sleep -Seconds 5
    if (-not $okW) { try { $hw = Invoke-RestMethod -Uri "http://127.0.0.1:8002/health" -TimeoutSec 10 -UseBasicParsing; $okW = $true } catch {} }
    if (-not $okM) { try { $hm = Invoke-RestMethod -Uri "http://127.0.0.1:18012/health" -TimeoutSec 10 -UseBasicParsing; $okM = $true } catch {} }
    if ($okW -and $okM) { break }
}
if (-not $okW) { Write-Host "!! core /health 未就绪" -ForegroundColor Red; exit 1 }
if (-not $okM) { Write-Host "!! markets /health 未就绪" -ForegroundColor Red; exit 1 }
"core-health=$($hw.status) commit=$($hw.commit)"
"markets-health=$($hm.status)"

"--- 5) 本地验收 ---"
$pp = (Invoke-WebRequest -Uri "http://127.0.0.1:8002/pricing.html?x=l2d11113" -TimeoutSec 20 -UseBasicParsing).Content
"local_pricing_v18c=$($pp.Contains('locales.js?v=20260818c') -and $pp.Contains('shell.js?v=20260818c'))"
"local_pricing_cards=$($pp.Contains('$24.9') -and $pp.Contains('$199'))"
"local_pricing_deeplink=$($pp.Contains('app.html?plan=yearly#sub'))"
"local_pricing_dev=$($pp.Contains('id="devSection"') -and $pp.Contains('id="tokenPlansGrid"'))"
if (-not ($pp.Contains('locales.js?v=20260818c') -and $pp.Contains('$24.9') -and $pp.Contains('$199') -and $pp.Contains('app.html?plan=yearly#sub') -and $pp.Contains('id="devSection"'))) { Write-Host "!! pricing 验收失败" -ForegroundColor Red; exit 1 }

$pi = (Invoke-WebRequest -Uri "http://127.0.0.1:8002/index.html?x=l2d11113" -TimeoutSec 20 -UseBasicParsing).Content
"local_index_startfree=$($pi.Contains('hero.cta.free') -and $pi.Contains('app.html'))"
"local_index_devline=$($pi.Contains('page.index.devLine'))"
if (-not ($pi.Contains('hero.cta.free') -and $pi.Contains('app.html') -and $pi.Contains('page.index.devLine'))) { Write-Host "!! index 验收失败" -ForegroundColor Red; exit 1 }

$oldVer = @(Get-ChildItem "$REPO\web" -Recurse -Filter *.html | Select-String -Pattern 'locales.js?v=20260818b' -SimpleMatch -List)
"local_old_ver_residual=$($oldVer.Count)"
if ($oldVer.Count -ne 0) { Write-Host "!! 旧版本号 20260818b 残留" -ForegroundColor Red; exit 1 }

$ma = (Invoke-WebRequest -Uri "http://127.0.0.1:18012/app.html?x=l2d11113" -TimeoutSec 20 -UseBasicParsing).Content
"local_markets_flash=$($ma.Contains('function flashPlan'))"
"local_markets_upgrade=$($ma.Contains('id="auth-upgrade"'))"
"local_markets_btnfix=$($ma.Contains('plan === ''yearly'' ? ''year'' : ''month'''))"
if (-not ($ma.Contains('function flashPlan') -and $ma.Contains('id="auth-upgrade"') -and $ma.Contains('plan === ''yearly'' ? ''year'' : ''month'''))) { Write-Host "!! markets app 验收失败" -ForegroundColor Red; exit 1 }

"--- 6) 公网验收 ---"
$pub = (Invoke-WebRequest -Uri "https://www.ai24x.com/pricing.html?x=w2d11113" -TimeoutSec 25 -UseBasicParsing).Content
"pub_pricing_cards=$($pub.Contains('$24.9') -and $pub.Contains('$199'))"
"pub_pricing_deeplink=$($pub.Contains('app.html?plan=yearly#sub'))"
"pub_pricing_v18c=$($pub.Contains('locales.js?v=20260818c'))"
$pubI = (Invoke-WebRequest -Uri "https://www.ai24x.com/?x=w2d11113" -TimeoutSec 25 -UseBasicParsing).Content
"pub_index_startfree=$($pubI.Contains('hero.cta.free'))"
"pub_index_v18c=$($pubI.Contains('locales.js?v=20260818c'))"
$pubM = (Invoke-WebRequest -Uri "https://markets.ai24x.com/app.html?x=w2d11113" -TimeoutSec 25 -UseBasicParsing).Content
"pub_markets_flash=$($pubM.Contains('function flashPlan'))"
"pub_markets_btnfix=$($pubM.Contains('plan === ''yearly'' ? ''year'' : ''month'''))"

if ($pp.Contains('locales.js?v=20260818c') -and $pp.Contains('$24.9') -and $pp.Contains('app.html?plan=yearly#sub') -and $pi.Contains('hero.cta.free') -and $oldVer.Count -eq 0 -and $ma.Contains('function flashPlan') -and $ma.Contains('id="auth-upgrade"') -and $pub.Contains('$24.9') -and $pub.Contains('app.html?plan=yearly#sub') -and $pub.Contains('locales.js?v=20260818c') -and $pubI.Contains('hero.cta.free') -and $pubM.Contains('function flashPlan')) {
    "--- 全部 PASS ---"
    Write-Host "=== 部署成功 OK ===" -ForegroundColor Green
} else {
    Write-Host "!! 验收未全通过" -ForegroundColor Red; exit 1
}
```

## 完成后群回执（固定步骤，主通道必做）
- 用 04 应用发指挥部群：
  `python api\feishu_notify.py --config <openclaw.json> --group "【AI24X 部署回执】www Markets 主打 + Developer 下沉 + 深链订阅 2d11113 成功：HEAD=2d11113d6f59、core+markets health OK、pricing 三卡 24.9/199 深链、旧版本号残留 0、markets 深链高亮与升级入口上线、版本 20260818c。问题项：无" --at`

## 回执格式（统一）
- ✅ 已完成项：HEAD / checkout / pull / app.html 同步 sites / core+markets 重启 / pricing 三卡+深链 / index Start free / 旧版本号残留 0 / markets 深链高亮 / 版本 20260818c / 公网验收 / 群回执
- ⚠️ 问题项：逐条列出（无则写「无」）

## 回滚预案
- `git revert 2d11113` + 重启 core(8002)；markets app.html 恢复 `C:\sites\markets.ai24x.com\app.html.bak-20260817-0030` + 重启 markets(18012)（本轮无后端 py 变更）。
