# 20260823 复盘/掘金 本地与生产标的对齐 - 归档数据同步记录

> 联动提交：c9e103f（算法批次：周线中期确认 + 资金榜动态候选 + 合规化文案，03 已部署 ✅）
> 本记录覆盖「代码部署之后」的归档数据同步动作，纯运维操作，无代码变更。

## 一、根因（两层叠加）
1. 本地 5 文件有未提交算法批次（周线中期确认因子 +8、资金榜动态候选、合规化文案），生产跑旧代码。
2. 归档不一致：生产 08-21 历史主推 = 强达电路（旧算法旧归档），本地 = 普源精电；
   导致 08-23 科创主线 kc 扫描中强达电路凭「昨日主推延续 +3」进入 picks，本地则是思泰克。

## 二、修复动作（已完成）
1. 算法批次 c9e103f 双推 gitee/origin，deploy03.ps1 派发完成（HEAD=c9e103f、文件标记全过、
   PID 8824→3100、health 200、公网 v97 周线逻辑全过），备份 `C:\backup\ai24x_a\20260823_pre_c9e103f`。
2. 03 数据备份：`C:\backup\ai24x_a\20260823_pre_sync\`（bj_scan_history.json + 19 个 08-20/08-21/08-23 归档）。
3. 同步本地 `bj_scan_history.json`（整文件）与 08-20/08-21 归档（主版本 + 时间戳版本）到 03；
   删除 03 上旧算法生成的 `2026-08-21-*-2123.json`。
4. 03 重扫全市场（`_rescan_all.py`，run_scan_dedup force，零上游 HTTP）：
   结果 hs 2 / kc 2 / bj 2 / bj_all 4 / all 2，与本地数量一致。
5. 修正 kc 主归档：`2026-08-23-kc.json` 用独立 kc 扫描产出的 `-2342` 版本覆盖（强达→思泰克）。
6. 重启 03 `AI24X-a1-api`（sc stop/start，PID 996→3704），加载新日缓存。

## 三、验证结果（03 本机 HTTP，VIP token）
- hs：600395 盘江股份(64) / 600360 华微电子(58)
- kc：688388 嘉元科技(70) / 301568 思泰克(57)
- bj：920161 龙辰科技(66) / 920181 赛英电子(62)
- bj_all：920161 龙辰科技(66) / 920471 美邦科技(63) / 920181 赛英电子(62) / 920425 乐创技术(57)
- all：688388 嘉元科技(70) / 600395 盘江股份(62)
- 5/5 市场与本地完全一致；cached=True、date=2026-08-23、无 stale。

## 四、坑位记录
- 03 曾有一个部署遗留 `_tmp_verify_b8657af.py` 进程（PID 7072）卡住，会抢写 `_rescan_picks.json`
  导致全市场重扫结果被旧进程覆盖（kc 误显强达）；taskkill 后重扫恢复正常。
- SSH 内联 python -c 引号嵌套在 cmd 下必坏，一律写 .py → scp → 前台跑。
- `_save_archive` 主版本已存在时改存 `{date}-{market}-{HHMM}.json`，force 重扫不会覆盖主归档，需手动修正主文件。
- 本地签发的 JWT 在生产 401（jwt_secret 不同）；验证用 03 本地 `create_token` 对 VIP 用户（quota.plan 非 free/anon）签发。

## 五、待办
- 无（临时文件已清理：本机 + 03 `_*` 脚本/json/token/log 全删；token 文件 `_tok.txt/_vip_tok.txt` 已删）。
- 明天 15:10 自动扫描为收盘定型口径，今日产物不受影响；如再出现本地/生产不一致，先比 `bj_scan_history.json` 最近条目。
