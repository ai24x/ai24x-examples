---
description: 用户说「04更新」时自动推 Gitee 并派发副脑04任务指令
alwaysApply: true
---

# 副脑04 部署派发协议

当用户说 **「04更新」**、**「副脑04更新」**、**「派发04」** 或在本轮已完成本地改动并要求上线 04 时：

## 必须完成的动作（按序）

1. **确认有可部署的 git commit**（无则先完成改动并 commit）
2. **写任务指令**：`docs/营销/收发/指令/YYYYMMDD-HHMM-04更新-<简述>-<短SHA>.md`
   - 模板字段：`需求类型：②需副脑办(副脑04)` + 需求描述 + 验收标准
   - 正文含：目标提交 SHA、变更摘要、04 可复制 PowerShell、nginx 项（若有）、回滚、验收清单
   - 复杂任务同步写回执模板：`docs/营销/收发/回复/<同名>-out.md`（可先发空壳待 04 填）
3. **commit 指令文件**（若尚未提交）
4. **push 到 Gitee**：`git push gitee master`（必要时 `git push origin master`）
5. **告知用户**：已 push 的 SHA、指令文件路径、04 需做的 nginx/GSC 要点

## 默认范围

- **副脑04** = 国际生产 `43.160.246.30`（www + open + API）
- 静态：`web/`、`p/open/web/`；API：`api/` + 重启 `AI24X-core`
- **禁止**在规则流程中 Write 覆盖生产 `.env`

## 指令命名

`20260828-1810-04更新-GSC收录优化-d23b51d.md` 格式：`日期-时间-04更新-简述-短SHA`
