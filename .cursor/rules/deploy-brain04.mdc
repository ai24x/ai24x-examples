---
description: 副脑04 生产更新全流程（git 双推 + deploy04.ps1 派发 + 回执收尾）
alwaysApply: true
---

# 副脑04 生产更新流程（AI24X）

> **前置规范**：所有派发必先遵守 `docs/营销/收发/AGENTS.md` →「📌 更新指令统一规范」（落盘即提交、**gitee+origin 双推**、推送后自检）。副脑只拉 **origin**，只推 gitee = 未送达。

当用户说 **「04更新」**、**「副脑04更新」**、**「派发04」** 或要求上线国际生产时，**必须走完整流程**，不能只 push Gitee + 写 .md。

## 一、全流程（按序，缺一不可）

1. 本地改代码 → 本地 QA 全绿
2. **重大项**：关键页面清单给老板 → 老板回复「放行」后才可双推（首页/导航/定价/支付/登录/账户/视觉/多页批量）
3. `git add <本次文件>` — **严禁** `git add -A` / `git add .`
4. `git commit` → **双推（缺一不可）**：
   ```powershell
   git push gitee master
   git push origin master   # 副脑04 只拉此 remote
   ```
5. **推送后自检**：`git status` 无未提交指令；`git log origin/master -1` 确认已同步
6. 写 **部署脚本** `docs/营销/收发/指令/YYYYMMDD-HHMM-04更新-<简述>-<短SHA>.ps1`（UTF-8 **带 BOM**）
7. 写配套说明 `.md`（可选，记录 nginx/人工项）
8. **commit 指令** → 再双推 + 自检
9. **`deploy04.ps1` 派发**（同步 600s，主通道）：
   ```powershell
   powershell -NoProfile -ExecutionPolicy Bypass -File scripts\deploy04.ps1 "docs\营销\收发\指令\<指令名>.ps1"
   ```
10. 遇 `EmbeddedAttemptSessionTakeoverError`：scp 已送达可先查 04 会话是否在跑，再重试
11. **04 必须在指挥部飞书群发精简确认**（见下方模板；openclaw 跑完脚本后执行，不可省略）
12. `scp` 拉回执 → 更新 `memory/daily/YYYY-MM-DD.md` → 显式 add 回执+记忆 → 双推收尾 + 自检

**03更新** 同理：遵守 AGENTS.md 双推规范 + `deploy03.ps1`；03 也只拉 **origin**。

## 二、04 环境事实

| 项 | 值 |
|---|---|
| SSH | `Administrator@43.160.246.30`，本机免密，需静态路由 `route add 43.160.246.30 mask 255.255.255.255 192.168.2.1 metric 1` |
| 仓库 | `C:\ai24x01`，**只有 origin**（无 gitee） |
| 远端 shell | cmd；远程 PowerShell 用 `powershell -File xxx.ps1` |
| AI24X-core | NSSM **8002** = www + api.ai24x.com |
| AI24X-markets-api | **18012**；静态同步 `C:\sites\markets.ai24x.com` |
| AI24X-open-api | **18080**；独立 venv + 库 `ai24x_open_cn` |
| 公网验证 | **在 04 上** `Invoke-WebRequest -UseBasicParsing`（本机公网不稳） |

## 三、部署 .ps1 写法要点

1. **UTF-8 BOM**：中文脚本必须 BOM，否则 04 PS5.1 按 GBK 误读
2. **HEAD 校验**：`git merge-base --is-ancestor $EXP HEAD`；health 用完整 12 位 hash，**禁止** `-notlike "$EXP*"` 前缀匹配
3. **04 pull**：`git fetch origin` + `git pull --ff-only origin master`（不是 gitee）
4. 所有 `Invoke-WebRequest`/`Invoke-RestMethod` 带 **`-UseBasicParsing`**
5. 改生产配置先 `Copy-Item` 备份 `.bak-时间戳`
6. www：pull + **Restart-Service AI24X-core**（纯静态也要重启，health 带 commit）
7. markets：同步 `C:\sites\markets.ai24x.com` + 重启 markets-api
8. open：pull + 重启 AI24X-open-api
9. 版本号：`locales.js`/`api.js`/`console.js`/`shell.js` 的 `?v=` 全站统一升，与内容同批提交
10. 脚本末尾：`Write-Host "DONE EXP=$EXP HEAD=$HEAD"`
11. **脚本末尾或配套 .md 必须写明群确认要求**（04 openclaw 执行完 PowerShell 后发送）：

## 四-b、指挥部群精简确认（04 必发）

部署脚本跑通后，04 openclaw **立即**在指挥部飞书群发 **一条**（勿刷屏、勿贴长日志）：

**成功：**
```
✅ 04更新完成｜<简述，≤15字>｜EXP=<短SHA> HEAD=<12位> health=<12位>｜公网验收通过
```

**失败 / 部分完成：**
```
⚠️ 04更新未完成｜<简述>｜原因：<一句话>｜已回滚/待人工：<nginx|.env|…>
```

**示例：**
```
✅ 04更新完成｜GSC收录优化｜EXP=d23b51d HEAD=ea57ea806c61 health=ea57ea806c61｜公网验收通过｜待办：nginx 301+GSC
```

- 本机（Cursor）**不代发**群消息；只负责写进 `.ps1`/`.md` 并让 `deploy04.ps1` 派发
- 用户问「04 做完了吗」→ 查群消息 + `ops/deploy04-*.log` + `health.commit` + 回执文件

## 四、与 03 的差异

| | 03 | 04 |
|---|---|---|
| 脚本 | `deploy03.ps1` | `deploy04.ps1` |
| 主机 | `123.207.199.238` | `43.160.246.30` |
| 服务 | AI24X-a1-api **8001** | AI24X-core **8002** |
| health | 无 commit 字段，看进程时间 | 有 commit，用 merge-base 验 |

## 五、派发后如何确认完成

1. 本机 `ops/deploy04-*.log` 无 throw，末尾有 `DONE EXP=...`
2. **指挥部飞书群**有 04 的 ✅/⚠️ 精简一行（必查）
3. `docs/营销/收发/回复/<任务>-out.md`（scp 拉回）
4. 公网：`api.ai24x.com/health` 的 commit 含目标 SHA（www 静态看页面 marker）

## 六、常见 partial deploy 原因

- 只 push 未 `deploy04.ps1`
- 04 指令写 `git pull gitee`（04 无 gitee remote）
- pull 成功但未 `Restart-Service` → health 仍是旧 commit
- markets 只 pull 未 copy 到 `C:\sites`
- nginx 手工项未做（GSC 301/404 类）
- 只写 `.md` 未写可执行的 `.ps1`
