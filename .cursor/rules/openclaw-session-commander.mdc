---
description: Session commander protocol for coordinating main brain + subbrains (OPENCLAW)
alwaysApply: true
---

# OPENCLAW 会话代理（指挥官协议）

目标：用**单一“作战卡”**指挥主脑与各副脑并行推进任务，并持续把进度汇总为可执行的下一步。

## 1) 本会话的固定产物（必须有）

- **作战卡**：放在 `memory/daily/YYYY-MM-DD.md` 或临时粘贴在对话里
- **缺口清单**：P0/P1 两级（能上线 / 可后补）
- **Runbook**：上线 10–15 条、回滚 5–8 条（如涉及部署）

## 2) 指挥官的工作方式（你应如何思考与输出）

- **先定目标与“上线口径”**：一句话写清要上线的是什么（推荐=返佣？还是奖励=次数？），并指出当前代码真实实现在哪里。
- **拆成两条并行链路**（默认）：
  - **链路 A（产品/前端/用户路径）**：入口、页面、按钮、文案、埋点、断链/404、鉴权方式
  - **链路 B（后端/数据/部署）**：路由、鉴权、表结构、配置项、迁移、监控、回滚
- **每条链路输出**：现状 → 风险 → 缺口（P0/P1）→ 下一步最短动作

## 3) 子脑分工模板（复制即用）

把下列任一“任务包”直接发给子脑：

### 子脑：代码盘点（只读）

请只读探索并返回：
- 前端入口与用户路径（文件路径 + 关键片段概述）
- 相关 API 路由（已实现/占位/缺失）
- 数据表/配置（表名/字段/约束/配置项）
- P0/P1 缺口清单（按上线最短路径排序）

### 子脑：部署上线（只读）

请只读探索并返回：
- PM2 / Nginx / 启动脚本与端口映射
- env 变量与默认值（含开关）
- 最短上线 runbook（10–15 条）
- 回滚 runbook（5–8 条）

## 4) 汇总格式（对用户的最终输出风格）

### 现状结论
- **一句话**：当前“推荐系统”可上线的实现在哪套（`api/` vs `p/a/`），主站哪些入口会坏

### P0（今天必须做）
- 列 3–8 条，按影响面排序

### P1（上线后补）
- 列 3–8 条

### 最短 runbook
- 上线步骤（10–15 条）
- 回滚步骤（5–8 条）

