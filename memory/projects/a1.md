# AI 行情官｜灯塔版（现行 p/a1）

> 更新：2026-08-27｜域名：`a.ai24x.com`｜目录：`p/a1/`｜掘金数据口径见 **DEC-0012**

## 定位

- A股 K线智能标注 / 技术形态可视化工具（合规叙事：数据可视化，非荐股）
- 登录 → 搜标的 → 日/周/月K + 均线 + 信号 + MACD；次数/VIP/邀请/支付后端化
- **VIP 复盘页** `gd.html`：七栏目掘金扫描（沪深/科创/北证/全市场/MACD/回踩/10元下）

## 掘金 gd：本地 vs 03 数据（DEC-0012，必守）

- **本机 = 数据权威**；03 `AI24X_BJ_AUTO_SCAN=0`（禁自动重扫）
- **收盘数据同步**：`p/a1/scripts/sync_final_to_03.ps1`（deploy03 不带数据）
- **改代码**：显式 git add → 双推 → 03 pull + 重启；或 `scripts/deploy03.ps1` / ps1→scp→ssh -File
- **完整流程**：`.cursor/rules/deploy-brain03-a1.mdc`（用户说「03更新」即按此执行）
- 详见：`memory/decisions/DEC-0012-掘金复盘本地权威与03数据同步.md`

## 关键决策（当前有效）

- **现行工程只认 `p/a1`**；生产 PM2 `a-api-8001` → **8001**（本地仍 `18001` 静态 + `18011` API，勿强行改成 8001）
- **配额**：免费 10日/50周；VIP 150日/500周（月¥99 / 年¥999 / 体验¥9.9·7天·限购1）
- **板块搜索**：BK 优先、THS 第二；列表须标注「东财板块 / 同花顺xxxxxx」；**禁止把 THS 插到默认第一项**
- **映射**：`886078`↔`90.BK0963`（商业航天）、`881121`↔`90.BK1036`（半导体）；同名不同编制
- **信号缓存**：`CACHE_VERSION=5`；缓存文件名禁止 `:`（Windows 会写成 NTFS ADS）；启动删 `data/signal_cache/ths`
- **东财 BK 公网信号**：生产常拉不到东财；图可 JSONP；BK **固定** `chart candles → POST /api/signals/compute`；`time` 必须序列化为 `YYYY-MM-DD`（不可 `String({year,month,day})`）
- **THS**：TuShare `ths_daily`；拼音搜索需 `pypinyin`；当日可滞后

## 入口

| 环境 | 前端 | API |
|------|------|-----|
| 本地 | `http://127.0.0.1:18001/` | `18011` |
| 生产 | `https://a.ai24x.com/` | 同域 `/api/*` → 8001 |

页面：`index.html` / `demo.html` / `account.html` / `partner.html` / `feedback.html`；SW **v22**

## 2026-07-16 已验证修复

- 拼音：`gyyc`→高压氧舱、`syht`→商业航天、`bdt`→半导体
- 公网 BK 完整信号（不再只有金/等）
- 华为首页「获取验证码」窄屏可见
- `shell.js` 本地 `/api/me` 指 18011

## 下一步（建议）

1. 持续观察公网 BK 稳定性
2. 同花顺指标广场发布与转化确认
3. 首页/SEO 合规话术收敛
