## 日期

- 日期：2026-04-18
- 当前阶段：AI 行情官（`p/a`）本地/预演联调与运维文档收口

## 今日结论（最重要的 3 条）

- **`paid_provider=off` 表示未启用付费路由**，不是进程未启动；命中 `tencent:*` / `eastmoney:*` 说明在用公共源。付费优先需 `paid_provider=tushare` 且配置有效 token，顺序里写 `paid,tencent,eastmoney,sina`。
- **`SECRET_KEY` 是服务端会话/签名密钥**，与 TuShare 等第三方 token 无关；数据源 token 走对应环境变量或后台 `tushare_token`。
- **管理后台 `admin_ui`** 已改为中文为主、英文字段名小字对照；数据源区有字段说明与快速说明。

## 已完成

- 删除与 `部署-副脑01-...` 完全重复的 `docs/部署-副脑02-预演到副脑03-生产发布流程（Windows）.md`，并修正索引与引用。
- `p/a/api/server/app/admin_ui.py`：行情监控摘要中文化；数据源开关表单分组与选项中文说明；用户/流水/运维表头中英对照；多处 `setStatus` 与详情文案中文化。

## 进行中（明确卡点）

- **用户侧联调顺序（已确认）**：先打通 **会员注册/登录**（`p/a`：`/api/auth/request_code`、`/api/auth/login`、`/api/me`），再联调 **VIP 套餐 / 微信支付**（`/api/billing/wechat/native`、回调 `POST /api/billing/wechat/notify`、用户中心 `account.html#vip`）。

## 下一步（只列 3 件，必须可执行）

- **P0-注册**：邮箱验证码流程跑通（dev 固定码 `1234` 与 prod 邮件校验差异）；登录后 `/api/me` 与配额展示正常。
- **P1-VIP**：配齐微信商户与 `AI24X_WECHAT_*`、`AI24X_WECHAT_NOTIFY_URL`（与线上 Nginx `/api/` 反代一致），再测 Native 下单与回调开通。
- 若要用 TuShare：后台「付费数据源」选 TuShare + 填顺序 `paid,tencent,eastmoney,sina`，保存后等约 5 秒，在「行情路由监控」确认出现 `paid:tushare` 命中或合理回落。

## 风险/待确认

- 付费与公共源的「命中」依赖真实请求；无流量时路由表可能长时间无新命中属正常。
