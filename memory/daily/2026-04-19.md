## 日期

- 日期：2026-04-19
- 当前阶段：本地联调 — 手机注册仍待验证（用户 9 点后复测）

## 今日结论（最重要的 3 条）

- 短信能收到说明主站 `api` 的 `/v1/auth/sms/send` 与 OTP 内存/106 链路正常；注册失败在 `/v1/auth/register` 查库/写用户阶段。
- 曾复现：`auth_users` 表不存在时会 `OperationalError: no such table: auth_users` → 统一 500 `INTERNAL_SERVER_ERROR`；常见原因：`SKIP_DB_INIT=true`、或 SQLite 相对路径随 cwd 指到错误文件。
- 已提交代码侧加固：`api/config.py` 将相对路径 SQLite 锚定到 `api/` 目录；`p/a/web/index.html` 的 `normalizeErrMsg` 支持 `error` 字段及 Python `str(dict)` 单引号形式，避免整段 dict 糊在状态栏。

## 已完成

- SQLite `DATABASE_URL` 锚定到 `api` 包目录（减少 cwd/PM2 漂移）。
- 灯塔首页错误文案解析改进（`normalizeErrMsg`）。

## 进行中（明确卡点）

- 用户反馈：注册仍「还不行」（睡前未再跑通）；需 9 点后对照日志与环境复测。

## 下一步（只列 3 件，必须可执行）

1. 读 `api/logs/pm2-core-err.log` 中最近一次注册对应的 `Unhandled exception` / SQLAlchemy 首行错误（缺表 / 连库 / 其他）。
2. 确认 `api/.env`：`SKIP_DB_INIT=false`（或未设）；`pm2 restart core-8000`（或实际身份 API 进程名）使配置生效。
3. 浏览器再试注册；若经 `p/a` 代理，确认 `p/a/api/server/.env` 里 `AI24X_IDENTITY_API_BASE` 指向正在运行的 core（如 `http://127.0.0.1:8000`）。

## 风险/待确认

- 若 PM2 仍用旧 ecosystem/旧 cwd，可能未加载新 `config`；必要时 `pm2 delete` 后按 `ecosystem.local.cjs` 仅拉起 `core-8000`。
- `TestClient` 不用 `with` 时可能不进 lifespan → 无表；与线上 uvicorn 行为不同，排查时以真实进程日志为准。
