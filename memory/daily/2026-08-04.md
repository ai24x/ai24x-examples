# 2026-08-04 · 作战卡（控制台试调用 / shared / 鉴权）

## 现状结论

本机已修：**未勾选「用 API Key」不再偷带残留 sk**；勾选时**只带 Key、不带 JWT**（否则后端 JWT 优先导致勾选无效）。Shared 乱码后端 junk 加强；前端缓存 `?v=20260804m`。

## 昨晚已自修（无需产品决策）

- `web/js/api.js`：`preferApiKey` 三态；401 文案按是否勾选区分；导出 `logout`
- `web/js/console.js`：未登录+未勾选拦截；401 不再提「重启核心服务」；402 填 howto 文案；`remaining` 用扣费后；退出双按钮；shared `max_tokens=128`
- `web/console.html`：`btn-logout-account`；缓存 bump；试调用副文案默认「登录会话」
- `api/free_shared.py`：原文 junk 先判再 failover
- `api/services.py`：shared attribution remain 对齐扣费后快照

## 10:50 结论（试调用全员 401）

**还没好（公网）**：`https://api.ai24x.com/v1/chat/run` 对假 JWT 仍回 `无效的API Key或用户ID`；本机 `127.0.0.1:8000` 已回 `登录已失效`。  
→ **副脑04** 公网源站未拉到新 `main.py` / 未重启。指令：`docs/联调/副脑04-更新指令-试调用JWT鉴权.md`

临时绕过：勾选「用 API Key」+ 本账号 sk。

### P0 运维（立刻 · 副脑04）

1. **副脑04** 拉码（含 `api/main.py`）+ `Restart-Service AI24X-core`
2. 探测假 JWT：应变为 `登录已失效`
3. 部署静态 `?v=20260804n` 并硬刷新
4. 测：未勾选登录发送 shared / 勾选本账号 Key

### 口径修正（11:45）

- Token / www / api **正式生产 = 副脑04**；副脑03 Token **仅冷备份**
- 发版 runbook：`docs/联调/Token发版-Gitee与副脑04.md`
- 指挥中心 JSON / 岗位总纲 v1.0.6 已同步
### P1 产品（先别改代码，你拍板）

1. 试调用 401：保持会话 vs 踢出登录（余额 401 仍踢，试调用暂留）
2. 控制台是否允许「仅 Key、无登录」进页（现 gate 要 `ai24x_auth_token`）
3. Shared 全 junk 回安全短句仍扣 1 token：可否接受
4. PayPal Webhook 第二履约（规划里未做完）

## 回滚（静态）

- 静态回上一版 `?v=` 或还原三文件；API 回滚后 `pm2 restart … --update-env`
