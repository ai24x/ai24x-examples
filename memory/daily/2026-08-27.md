# Daily Log 2026-08-27

## 掘金 gd 本地/03 数据对齐（DEC-0012）→ 晚间修订

- **现象**：`a.ai24x.com/gd.html` 与本地多栏目主推不一致。
- **午间处置（已废止）**：临时关 03 自动扫 + 本机 sync；导致公网依赖人工，收盘后用户见「今日数据尚未生成」。
- **晚间雷总口径**：公网对外服务，收盘后**线上必须自动生成**各栏目。
- **已恢复**：03 NSSM `AI24X_BJ_AUTO_SCAN=1`（2026-08-27 ~20:07），health ok；今日数据此前已 sync 对齐。
- **今后**：日常靠 03 约 15:05 自动扫；`sync_final_to_03` 仅可选对齐/抢救。
- **决策文档**：`memory/decisions/DEC-0012-掘金复盘本地权威与03数据同步.md`（已改写）

---

# AI24X 平台 · 更新记录与规划摘要（2026-08-27）

> 用途：Codex 记忆 / 更新记录同步
> **04 生产 HEAD**：`249691e`（`api.ai24x.com/health` 已验）
> **关键提交链**：`b6f0b02`（产品）→ `7be296c`（仓库整理）→ `249691e`（a1 03 数据锁，含前两者）
> **上一稳定对照（本批 UX 前）**：`fb1a976`
> **部署回执**：`docs/营销/收发/回复/20260827-0015-04更新-UX硬化账单车道SEO-b6f0b02-out.md`
> **派发通道**：`scripts/deploy04.ps1` / `deploy04_async.ps1` → 04 openclaw（SSH `Administrator@43.160.246.30`）

## 一、04 已上线（2026-08-27 凌晨验收通过）

### 1. 安全 / 鉴权硬化
- `web/js/api.js` + `p/open/web/js/api.js`：`safeNextUrl`、生产 API Base 锁定、`setAlertMessage`（防 XSS alert）
- 登录/注册/找回：`novalidate` + JS i18n 校验；`next=` 跳转硬化
- `token-admin.html`：去掉明文密钥提示（**未做密钥轮换**，测试阶段暂缓）
- `ai24x.html`：移除公开 Token 管理入口

### 2. 产品文案 / UX（用户可见口径）
- **品牌叙事**：首页 Hero → **Spend Smarter**；去掉 Private/Local AI 芯片；成本/路由说明归 `#cost` 区块
- **命名统一**：托管额度 → **AI Gateway credits**（非「Token 包」混称）
- **支付展示**：Markets 描述去 Creem；Creem 入口仍隐藏；`crypto_ready` 时 **USDT 恢复**；Dodo 为主通道
- **Help / auth**：清理 SMTP/env/运维 jargon（遵守 user-facing-copy 铁律）
- **open BYOK 控制台**：全量 i18n，去掉 `BYOK_ENABLED` 等 ENV 名硬编码

### 3. 账单三车道（www `console.html#billing`）
| 车道 | 在哪买 | 履约库 | 说明 |
|------|--------|--------|------|
| AI Gateway credits（托管） | www Account | core | 主现金牛 |
| BYOK | **引导去 open**（Buy on AI Gateway →） | open 库 | www **不卖** BYOK 套餐 |
| Markets VIP | www Account | 独立 | 与 Gateway 分开 |

### 4. 定价页 / 首页 CTA
- 托管套餐：**单一主 CTA**（Top up in Account）
- Markets 周卡：**降权展示**（非主推）
- 首页副标题：**One integration…** 不塞 auto-match/成本细节（成本放 `#cost`）

### 5. SEO Phase1（www blog）
- 5 篇 Hub 已上生产：`what-is-ai-gateway` / `openai-compatible-api` / `one-api-multiple-models` / `byok-ai-gateway` / `ai-api-cost-basics`
- `blog/index.html` + `sitemap.xml` 已更新
- 规划总纲：`docs/营销/SEO自然流/Gateway-SEO词图与后期推广规划-2026-08-26.md`（50 词 + Cluster，v2.0）

### 6. markets 国际站
- 新文件 `p/markets/web/js/www-links.js`：本机 Account/Help/登录 → `127.0.0.1:8000`；公网 → www
- 各页 Account 链到 www；文案用 **Account** 非 Console

### 7. API core（需重启 AI24X-core）
- `api/pay_products.py`、`token_pay_service.py`、`main.py`：文案与 Dodo 计数；**未改价、未改 dodo_product_id**

### 公网验收锚点（抽检用）
- `www.ai24x.com` 含 **Spend Smarter**；无 Private/Local AI
- `/v1/billing/products` 含 **AI Gateway credits**；无 PayPal or Creem 文案
- `open.ai24x.com/console.html` 无 `BYOK_ENABLED`
- `markets.ai24x.com` 源码含 `www-links.js`

## 二、仓库 / 工程（7be296c，不影响 04 运行时）
- 扩 `.gitignore`：`ops/_*`、a1 缓存/bj_archive dump、markets QA 脚本与截图、`scripts/_*` 等
- 停止跟踪运行时垃圾（bj_archive JSON、markets `_qa_*`）；**本地文件仍在，只是不进 git**
- `ecosystem.local.js` 等本地误改已 restore（含 DB 密码，勿提交）
- 工作树仍有 WIP 未收：**fisher 删除、p/营销收发 归档、部分 docs/memory 修改** — 未自动 commit

## 三、国内 a1 / 03 线（249691e，与 04 同 master）
- **DEC-0012**：掘金复盘 **本机 = 数据权威**；03 禁止自动重扫
- 03 NSSM：`AI24X_BJ_AUTO_SCAN=0`
- 收盘后数据同步：`sync_final_to_03.ps1`（非 deploy03 带数据）
- 决策文档：`memory/decisions/DEC-0012-掘金复盘本地权威与03数据同步.md`

## 四、战略与规划（当前有效口径）

### 产品矩阵
| 域名 | 定位 | SEO 主责 |
|------|------|----------|
| www.ai24x.com | 品牌母站 + 转化入口 | 品牌词 + Blog Hub + 注册漏斗 |
| open.ai24x.com | **AI Gateway 开发者主站** | Gateway / OpenAI compatible / BYOK |
| markets.ai24x.com | AI 行情垂直应用 | 美股/图表长尾（**独立词表**） |
| a.ai24x.com | 国内 AI 股秘书 | 与国内运营线绑定 |

### 转化漏斗（已定稿）
SEO / 社媒痛点 → Blog / 落地页 → www 注册 或 open 拿 Key → 试用 → console#billing 充值
- 首页口号：**One API. Every AI. Pay Less.**
- 主题：**theme-blue** 全站统一；注册优先 CTA

### SEO 执行节奏
- **Phase1** ✅：5 篇 Gateway Hub（已部署）
- **Phase2**（排期）：对比文、best AI gateway、cost optimization 等（见 50 词表 `关键词库.md`）
- **禁止**：一词一篇机械灌水；Markets 词勿混进 Gateway 帖
- **待审批**：Cost Calculator 页面（规划有、未开工）

### 定价合理性（审计结论，**未改价**）
- Token/BYOK/Markets 阶梯总体合理
- 已知怪点（仅记录）：Starter 命名、VIP Pass 误购、Markets 周卡偏贵、Value Pack vs Builder — **改价需雷总审批**

## 五、红线（所有 Codex 会话默认遵守）
- **禁止**：改生产 `api/.env`、轮换密钥、改 `price_usd` / `dodo_product_id`、对外承诺新价格
- **禁止**：用户可见文案出现 SMTP/env/副脑/OpenRouter 上游等运维词
- **部署**：国际生产 = **副脑04**；用 `deploy04.ps1` + `docs/营销/收发/指令/*.md`
- **回滚 UX 批**：`git checkout fb1a976 -- api/pay_products.py api/token_plans.py api/token_pay_service.py api/main.py web/ p/open/web/ p/markets/web/` + `Restart-Service AI24X-core`

## 六、待办 / 未闭环（P0→P1）

### P0
- [ ] EdgeOne CNAME 接入（源站 IP 暴露风险）— `docs/营销/收发/指令/task-20260823-04-edgeone-checklist.md`
- [ ] 主脑出口 IP 漂移 → 04 SSH 白名单需定期同步（历史多次 banner 超时）

### P1
- [ ] 工作树整理：fisher → `p/game/fisher` 迁移删除入库、营销收发 `_done` 归档
- [ ] SEO Phase2 内容排期 + open docs/guides 深度对齐
- [ ] Cost Calculator（需审批）
- [ ] 密钥轮换 + token-admin 凭据治理（安全审计项，暂缓）

## 七、关键文档索引
| 文档 | 路径 |
|------|------|
| Gateway SEO 总纲 v2.0 | `docs/营销/SEO自然流/Gateway-SEO词图与后期推广规划-2026-08-26.md` |
| 网站目录地图 | `memory/projects/web-sitemap.md` |
| 08-26 作战日志 | `memory/daily/2026-08-26.md` |
| 08-27 a1/03 数据锁 | `memory/daily/2026-08-27.md` |
| 04 UX 部署指令 | `docs/营销/收发/指令/20260827-0015-04更新-UX硬化账单车道SEO-b6f0b02.md` |
| 用户文案铁律 | `.cursor/rules/user-facing-copy.mdc` |

**记忆一句话**：04 已在 `249691e` 完成 Gateway 转化 + 账单三车道 + SEO Phase1；BYOK 只在 open 卖；本机 a1 数据权威、03 禁自动扫；下一步主攻 SEO Phase2 与 EdgeOne，改价改密钥一律审批。

## 八、08-27 晚批：支付弹窗统一 + Dodo 商品文案（司令落地，04 已上线）

- **代码**：`0fc5c30`（支付聚合 + AI Gateway credits 文案，6 文件）+ `eb2d736`（Dodo 同步描述比对修复）；04 生产 HEAD=`eb2d736b0bd7`。
- **前端三站**：支付弹窗 Card（Dodo）+ PayPal 全宽主按钮 →「Other payment methods」分组（微信/支付宝/USDT/Mock 双列）；Dodo 用户侧文案改 Card/银行卡；markets 删 Creem 入口（含状态探测失败分支）；mock 仅本地/测试显示。`console.js?v=20260827a`（www/open 两站）。
- **Dodo live 商品**：Token 6 个标题 → `AI24X AI Gateway Credits · {title}`，描述含 `Includes N credits · valid N days`；Markets 3 个描述补 `For educational purposes only — not investment advice.`；BYOK 不变。live 商品 ID 未动。
- **Dodo 同步 bug（eb2d736）**：`_sync_one` 的 unchanged 判定原只比价格+标题，**仅描述变化不会 PATCH**（markets 合规句首轮同步被漏）；已加 `cur_desc == description` 比对 + QA 用例（`_qa_dodo_sync_20260823.py` 全绿）。教训：改描述类商品文案后必须重跑同步并探针确认 live 描述。
- **本机 SSH 到 04 的新坑（重要）**：本机运行 Meta Tunnel（Clash 类，TUN，默认路由 metric 0），SSH 原始 TCP 走代理节点时 **TCP 能连但 banner 永不回**（Connection timed out during banner exchange），Web 全通。解法：`route add 43.160.246.30 mask 255.255.255.255 192.168.2.1 metric 1`（非持久，重启失效；长期可在 Clash 加 DIRECT 规则或 `-p` 持久化）。出口 IP 154.16.27.165 是隧道出口，非真实宽带 IP。
- **回执**：`docs/营销/收发/回复/20260827-1929-04更新-支付聚合与Dodo商品文案-0fc5c30-out.md`；04 侧自动发群回执。

## 九、08-27 晚 2：open 支付弹窗图标撑爆修复（司令落地，04 已上线）

- **现象**：雷总反馈本地 18080 open `console.html#billing` 套餐弹窗图标/按钮变大、排版乱。
- **根因**：支付图标 SVG 带 `pay-ico` 类但无 width/height，open 的 `console.html` 漏了 `#modal-pay-channels .pay-ico { width:18px; height:18px }`（www 有、open 漏），SVG 按默认尺寸撑爆。markets 用的是 `.payhub-ico 24px` 不受影响。
- **修复**：`p/open/web/console.html` 补一条 18px 规则（commit `31302f7`）；新增图标尺寸 QA（`_qa_paychooser_open_icons_20260827.js`）4/4 绿（图标 18×18、主按钮全宽 398px、次要双列 195px、无 JS 报错）；open 回归 9/9 仍绿。
- **部署坑**：04 工作树有未跟踪的 04 自写回执（与远端提交同名）挡住 git pull → 挪成 `.local` 备份后 pull 成功（Fast-forward eb2d736→31302f7）；04 的 git 无 `gitee` remote（只有 origin），拉取 fallback 到 gitee 会报 "does not appear to be a git repository"，属预期，不用修。
- **04 验收**：open-api 重启、公网 `open.ai24x.com/console.html` 含 pay-ico 规则；core 未动（health 仍 eb2d736）。

## 十、08-27 晚 3：Starter 临时改价 $0.1（雷总国际卡实测，待恢复 $2）

- **变更（临时，非提交类）**：Starter（token_pack_10k）$2 → **$0.1**（price_fen 1440→72），note_en/note_zh 同步改 $0.1 文案；改动落 04 core+open 两个 gitignored `token_plans_override.json`（本地同步改，备份 `.bak-20260827-204709`）+ Dodo live 商品 PATCH（price=10 cents，描述同步 $0.1）。
- **验证**：www `api.ai24x.com/v1/billing/plans` 与 open `open.ai24x.com/v1/billing/plans` Starter 均 price_usd=0.10；Dodo live 商品 price=10 cents；core+open 已重启；Dodo sync 1 updated/0 errors。
- **回滚**：原值 $2 / 1440 fen（本地 override 同样可改回）；恢复命令 = 改两个 override 的 token_pack_10k price_usd=2.0/price_fen=1440 + 重启 core/open + 重跑 Dodo sync（或直接用备份文件覆盖）。
- **注意**：$0.1 可能触及卡组织/收单最低限额，若 Dodo 收银台拒收，需上调到 $0.5/$1 再测。
- **用户测试入口**：www console#billing 或 open console#billing → 选 Starter → Card（Dodo）→ 国际卡付 $0.10。

## 十一、08-27 晚 4：小额测试套餐（token_test_01）上线（默认隐藏，后台开关，04 已部署）

- **背景**：雷总在线测试国际卡，要求后台预留「专门小额测试套餐」，可开可关；正式售卖默认不显示。
- **实现**（`api/token_plans.py` + `p/open/api/token_plans.py`，commit `2a7bcc4`）：
  - `token_test_01`：标题 小额测试包 / Test Pack，$0.1，100 万 credits，validity 365，**enabled=False 默认停用**
  - core `_save_overrides` 新增 `_OPEN_OVERRIDE_PATH` 镜像同步——保存价表时自动写 open 的 override，**一个开关管两端**；失败仅 warn 不影响主站
  - 后台价表管理（token-admin.html 产品运营/价表）已有 enabled 开关 + 改价能力，**无需新增 UI**
  - 前端自动隐藏停用套餐（`list_public_plans` 过滤 enabled）、`get_plan` 下单拦截；Dodo sync 跳过停用套餐（`if not plan.get("enabled", True): continue`）
- **QA**：`_qa_test_pack_20260827.py` 14/14 ALL PASS（默认隐藏 / 后台开启可见可下单 / open 镜像同步 / 关闭恢复隐藏）
- **04 部署**：HEAD=`2a7bcc4f1b58`，core+open 重启，两站 plans API 均无 token_test_01（默认隐藏正常）
- **后台开启方法**：管理后台 → 产品运营 → 价表管理 → 小额测试包（Token Test Pack）→ 勾选 enabled 启用 → 保存即前台生效；Dodo 商品由 sync 自动跳过停用套餐
- **待办**：Starter $0.1 待雷总测完恢复 $2（回滚方法见第十节）

## 十二、08-27 晚 5：手机端支付同页跳转修复（54784d7 已上线 04，回执 ✅）

- **问题**：雷总反馈华为手机浏览器选「银行卡」后一直卡在「正在打开安全支付页，请稍后」，电脑端正常。
- **根因**：www/open/markets 三站支付弹窗共用「点卡同步开 about:blank 占位窗 → 异步订单返回后再导航 pay_url」模式；**iOS Safari / 华为等 Android 浏览器对「占位空白窗 + 异步导航」支持差**，占位页开了但跳转不执行 → 永久卡住（Dodo 官方也有 Safari 同款 known issue，manualRedirect 方案）。
- **修复（web/js/console.js + p/open/web/js/console.js + p/markets/web/app.html）**：新增 `isMobileCheckout()`（UA 含 Mobi/Android/iPhone 或 ≤820px 触摸屏）→ 移动端不走占位窗，订单返回后 `window.location.href = r.pay_url` **本页跳转**；桌面端保持新窗口不变。支付宝/PayPal/Creem 同步适配。回跳确认链路已就绪：www/open console.html 处理 `?dodo=1&out_trade_no=`、markets app.html 处理 `?pay=done&dodo=1`，付完自动查单确认到账。
- **版本**：console.js 内容变更 → www/open console.html `console.js?v=20260827a→20260827b`（仅这两个页面引用 console.js）；markets app.html 自包含内联无版本。
- **QA**：`_qa_mobile_dodo_20260827.js`（www 华为 UA 手机同页跳转 ✅ + 桌面新窗口 ✅）、`_qa_mobile_dodo_markets_20260827.js`（markets 手机同页跳转 ✅）、`_qa_mobile_dodo_open_20260827.js`（open iPhone UA 同页跳转 ✅）；回归 `_qa_paychooser_markets_20260827.js` 10/10、`_qa_paychooser_open_icons_20260827.js` 4/4。
- **部署**：本地 commit `54784d7` + 指令文档 `e31f32a` 双推；04 已部署（HEAD=e31f32a8bed5），三服务重启，公网三站标记/版本验证全过；回执 `docs/营销/收发/回复/20260827-2122-04更新-手机端支付跳转修复-54784d7-out.md`。
- **坑位**：部署脚本里 `$h.commit -notlike "$EXP*"` 对完整 12 位 hash 不匹配（health 返回 e31f32a8bed5），04 改用 `git merge-base --is-ancestor` 验证通过——指令文档提交在代码提交之后，HEAD 前缀校验必误报，ancestor 才是正解（呼应第三十九节）。
- **验证提示**：公网手机实测由雷总进行（华为浏览器 → www/open/markets 任一站选套餐 → 银行卡 → 应直接进 Dodo 收银台，不再卡占位页）。
