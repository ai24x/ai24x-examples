# DEC-0012：掘金/复盘（gd.html）生产自动定型与可选本地对齐

> 首订：2026-08-27（临时关 03 自动扫防漂移）  
> **修订：2026-08-27 晚 · 雷总明确：公网对外服务，收盘后线上必须自动生成各栏目数据**

## 结论（一句话）

**公网 03（`a.ai24x.com`）才是用户读的权威站**：交易日约 **15:05** 进程内自动扫，写缓存供全站读。`deploy03` 只更代码；`sync_final_to_03.ps1` 仅作**可选**对齐/抢救，不能代替线上自动定型。

## 产品口径

| 环境 | 角色 | 自动扫 |
|------|------|--------|
| **03 生产** | 公网对外、用户读这里 | **必须开** `AI24X_BJ_AUTO_SCAN=1`（默认即开） |
| 本地开发机 | 联调/验收对照 | 可开；与线上 picks 偶发不一致属正常（上游时序不同） |

前端文案：约 **15:05** 服务端自动更新（收盘定型；文案里「15:01」指可手扫门槛，自动扫在 15:05）。

## 为何曾关过自动扫（已废止）

2026-08-26 本地与 03 各自 15:05 扫 → picks 分叉；曾临时 `AI24X_BJ_AUTO_SCAN=0` + 本地 sync 对齐。  
**问题**：关自动扫后公网依赖人工 sync，收盘后用户会看到「今日数据尚未生成」——不符合对外服务。

**2026-08-27 20:07 已恢复**：03 NSSM `AI24X_BJ_AUTO_SCAN=1`，API 已重启，health ok。

## 固定流程

| 动作 | 怎么做 |
|------|--------|
| 更代码 | `scripts/deploy03.ps1` |
| **日常收盘数据** | **03 自动扫**（勿再关） |
| 可选对齐 | 本机 `sync_final_to_03.ps1`（验收对照/事故抢救） |

## 栏目与底层 market

| 前端栏目 | 底层 scan key |
|----------|---------------|
| 沪深/科创/北证/北证全市场 | hs / kc / bj / bj_all |
| MACD首红 / 10元下 | all |
| 回踩企稳 | hs |

## 相关文件

- `p/a1/api/server/app/bj_screener.py` — `_AUTO_SCAN_HHMM=(15,5)`、`AI24X_BJ_AUTO_SCAN`
- `p/a1/scripts/sync_final_to_03.ps1` — 可选同步
- `p/a1/scripts/_enable_03_auto_scan.ps1` — 03 打开自动扫
